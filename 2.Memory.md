# 2.Memory.md

## 2026-08-12 小智运行观测前端切片

- 新增 `apps/desktop/src/renderer/components/AiObservabilityWorkspace.tsx`，复用现有 telemetry、regression report、run list 和 memory trace typed preload，没有新增后端能力或第二份评分逻辑。
- 分析页现在可刷新 SQLite 快照、点击生成并保存回归报告、查看真实 gate、选择最近 run 并查看 bounded L1 轨迹。原始 prompt 和 hidden reasoning 不进 DOM，run 进 React state 前只保留五个白名单字段。
- 修复 1366×768 下下方导航被裁切的问题：`.global-nav nav` 改为内部滚动；运行列表/轨迹也限高内滚动。
- 验收：`npm run test:renderer-components` 7/7，`npm run build` 通过，`npm run test:smoke` 前端 33/33 通过；Electron 同一数据目录重启后报告仍可回读。
- 新截图：`frontend-ai-observability-1366x768.png` 和 `frontend-ai-observability-1920x1080.png`，位于 `apps/desktop/test-results/electron-e2e/`。
- 仍未完成：回归报告 Markdown/HTML 导出、失败样本自动回放、真实 provider/live teacher 证据；三元题组编辑仍未覆盖 confirmation payload。

## 定义

本文件记录 Omni-Edu Agent 当前进度、刚确认的事实和下次继续位置。每轮工作开始前必须阅读；每轮工作结束后必须追加或更新。

## 当前项目状态

### 2026-08-11 E07/E08 本轮进度

- 新增 `generate_research_outline` 与 `render_learning_mermaid` 两个 Electron main 宿主工具：研究只读本地教师知识切片，返回提纲、来源、证据边界与未知项；可视化只读知识图谱，返回受限 Mermaid。
- Router 新增 `research_workspace` / `visualization` 子意图；两者均不需要学生上下文，工具权限由 context allowlist 控制；讲义中的“概念图”不会抢占 teaching_book 路由。
- `npm run test:ai-harness` 122/122、`npm run test:deeptutor-research-visualization` 18 assertions、`npm run test:deeptutor-capability-evals` 48/48、`npm run test:ai-observability` 和 `npm run build` 通过。
- 尚未完成：联网 Deep Research、真实引用核验、HTML/SVG/Manim 渲染、真实 provider/教师样本、端到端 2 秒 SLO；本轮不能标记生产完成。

### 2026-08-11 C05 高级 block 首片

- `TeachingBlockType` 扩展 `timeline/code/deep_explanation/interactive/animation/user_note`；Markdown compiler 对前四类做 bounded typed rendering，对 interactive/animation 只输出安全占位并计入 fallback。
- 增加 `safeText` 过滤控制字符、危险 scheme 和 HTML 标签；代码只进 fenced Markdown，不执行；source anchors 仍由 book renderer 汇总。
- `npm run test:deeptutor-c05-advanced-blocks` 单轮 24 assertions，连续 3 轮通过；现有 book renderer 28/28、AI harness 122/122、observability 和 build 继续通过。
- 仍未完成高保真 PDF/DOCX、浏览器交互查看器、Manim 渲染、真实教师可用性和 provider 质量验收。

### 2026-08-11 E15 复习 scheduler 与 review queue 首片

- 新增 `review-scheduler.ts`，直接复用 DeepTutor `learning/scheduler.py` 的知识类型间隔序列、连续正确推进和错误回退规则；调度纯函数按 epoch seconds 计算，timezone 仅保留展示元数据。
- `mastery-policy.ts` 改为调用共享 scheduler；新增 `get_review_queue` local-only/read-only HostTool，路由支持 `student_diagnosis/practice_design` 的复习队列请求，输出 `omni.review.queue.v1`，不携带原始记录或答案。
- 新增 `npm run test:deeptutor-review-scheduler`（20/20）和 `npm run test:deeptutor-review-queue`（14/14），覆盖正确/错误序列、UTC/Asia/Shanghai/America/New_York 一致性、超大/畸形输入、路由、参数 schema、HostTool readback、缺学生 fail-closed；`test:deeptutor-mastery-policy` 8/8、AI harness 125/125、observability、build 通过。
- `npm run test:deeptutor-review-recovery` 已通过 12/12：真实 SQLite close/reopen 后队列 fingerprint 保持一致；同时修复普通备注标题被误当知识点的边界，free-form evidence 现在只计 unknown，不再改变队列顺序。
- Electron smoke 已通过；期间发现 quiz grade 会把 procedure 知识点错误重分类为 concept，现改为从同一 mastery snapshot 保留原始 knowledgeType，确保后续 `mastery_assess` 仍按 procedure gate blocked。
- E15 提醒切片已完成：新增 `review-reminder.ts`、`review:getReminder` typed IPC/preload 和今日工作台提醒卡片；纯函数 18/18、真实 Electron close/reopen + UI 16/16，提醒从同一 SQLite snapshot 重算，无第二队列真源、无后台 cron。
- E15 高负载闸门已通过：8 个学生 × 200 条记录、16 并发真实 SQLite snapshot/policy/reminder，纯存储 benchmark pass^3（多次运行 p95 205–430ms，重启 196–225ms），真实 Electron IPC 批次 68–109ms；均低于 2s，但仍只代表本地数据层，不代表 live provider 网络延迟。
- 当前仍未完成：外部系统通知策略、真实 provider/教师样本和发布硬件复测；本地 SQLite/IPC 高负载 SLO 已通过专门 benchmark。

### 2026-08-11 E10/E12 待作答题恢复增量

- `mastery_status` 现在从 `ai_mastery_questions` 恢复最近的 `pending/answered` 题目，返回受限 `pendingQuestion`（id/知识点/题干/options/status），不返回 `expectedAnswer` 或学生答案；SQLite close/reopen 后状态保持。
- `mastery_grade.questionId` 改为绑定学生场景下可选：省略时只回退当前学生最近一题，跨学生题目在任何状态判断前阻断；评分成功后 pending projection 消失，避免重复评分。
- `npm run test:deeptutor-mastery-pending` 14/14 通过，覆盖真实 SQLite 重启、无题号评分、答案不泄露、跨学生越权；随后 mastery policy 8/8、48-case capability eval、AI harness 125/125、observability、build 继续通过。
- 该切片只解决单题恢复与边界，不代表多题 quiz、真实模型自主选题、跨进程 AgentLoop 恢复或 live provider 质量已经完成。

### 2026-08-11 E10/E12 多题掌握度队列增量

- `mastery_quiz.questionCount` 支持 1-5 道题，主进程用 SQLite 事务批量写入 `ai_mastery_questions`；只把第一题交给当前 user-input checkpoint，后续题按 `created_at ASC` 逐题恢复。
- 同一学生已有 pending/answered 题时，新建第二组会 `pending_question_exists` blocked；`mastery_grade` 的无题号回退消费最早已回答题，close/reopen 后剩余题数和顺序稳定。
- 新增 `npm run test:deeptutor-mastery-quiz-queue` 23/23，覆盖 schema 上限、批量创建、重复队列、逐题评分、重启恢复、queue-head 跳题和 expectedAnswer 字段缺失；pending 14/14、48-case capability eval、AI harness 125/125 继续通过。
- 评分还增加 queue-head 守卫：即使后续题目被异常标记为 answered，也不能用显式题号跳过前置题；回归 smoke 已覆盖 `question_order_mismatch`。

### 2026-08-11 R13 附件来源探索增量

- 复用 DeepTutor `UnifiedContext.source_manifest`/`read_source` 的按需读取语义，新增 `attached_source_exploration` 子意图和 `explore_attached_sources` local-only read 工具；只检索当前绑定学生记录中的附件对应脱敏 OCR 片段。
- Router 在附件分析任务中不再自动加载学习记录全文或教师知识库；工具输出 `omni.attached.sources.exploration.v1`，只含 bounded source handle、文件元数据和片段，不含 localPath、原始文件或未脱敏手机号。
- `npm run test:deeptutor-attached-source` 18/18 通过，覆盖路由隔离、学生/附件越权、无匹配、脱敏和无学生澄清；AI harness 125/125、48-case capability eval、build 继续通过。
- 当前仍只覆盖本地错题图片 OCR source handle；通用 PDF/DOCX/PPTX 附件提取、跨会话 source manifest、联网 PageIndex 和真实模型自主探索尚未完成。

- 项目路径：`D:\WorkProject\EduProject`
- 当前产品方向：本地优先的教师个人知识库、题库与智能组卷工作台，服务 K-12 独立教师与小微教研团队。
- 当前最终闭环：教师资料入库后按蓝图组卷；学生错题照片经本地 OCR、教师校正和题库检索后生成针对性练习。
- 当前桌面端位置：`apps/desktop`
- 当前技术栈：Electron + React + Vite + TypeScript + SQLite。

### 2026-08-11：C04 Book Workspace

- 已完成 SQLite `teaching_books/chapters/pages/blocks/sources`、main/preload IPC、来源健康检查和 page/block 版本化重生成。
- 已新增 `inspect_teaching_book` 与 `omni.teaching.book.v1`，按需只读讲义上下文；`npm run test:deeptutor-book-workspace` 26/26、`npm run test:deeptutor-book-renderer` 24/24、`npm run test:ai-harness` 104/104、`npm run build` 通过。
- 下一步从 C05 开始：实现 text/chapter/quiz/card/figure/concept graph/prompt block renderer 与现有 document_artifacts 的显式导出边界。

## 已完成确认

### 2026-07-28：建立四份项目协作文档

已完成：

- 新增 `1.Agent.md`，定义产品说明、Agent 工作方式、代码风格和禁止事项。
- 新增 `2.Memory.md`，记录当前进度、确认事实、下次继续位置。
- 新增 `3.Learning.md`，记录本轮复盘、错误原因和下次改法。
- 新增 `4.Wiki.md`，沉淀项目背景、业务口径、架构常识和接口安全口径。
- 更新 `AGENTS.md`，加入“每轮工作前阅读四文档、每轮工作后更新四文档”的强约束。

验证：

- 使用 `Get-Content -Encoding UTF8` 读回 `1.Agent.md` 和 `2.Memory.md`，中文内容正常。
- 使用 `git status --short` 确认四份文件已创建，`AGENTS.md` 已修改。

未完成 / 风险：

- 当前工作区仍存在多处既有修改和未跟踪文件，后续提交前需要按任务归属分批整理。
- `docx/` 目录下有一组删除状态文件，本轮未处理，后续不要误删或误恢复，需先确认来源。

下次继续：

- 开始任何代码任务前先阅读 `1.Agent.md`、`2.Memory.md`、`3.Learning.md`、`4.Wiki.md`。
- 若准备提交，先拆分“底层 SQLite/sandbox 改造”“AI 控制台/UI 改动”“四文档约束”三类变更。

### 2026-07-28：preload sandbox 兼容改造与原生 SQLite 迁移

已完成：

- 将 `apps/desktop/src/main/db.ts` 从 `sql.js` 迁移到原生 `sqlite3`。
- 移除 `sql.js` / `@types/sql.js` 依赖。
- 数据库改为真实本地文件 `app.db`，不再依赖 wasm 内存库导出保存。
- SQLite 初始化启用 `journal_mode=WAL`、`foreign_keys=ON`、`busy_timeout=5000`、`synchronous=NORMAL`。
- Store 层主要读写方法改为异步 Promise 风格。
- `learning_records` 查询增加分页上限，默认 100，最大 500。
- 附件按记录批量加载，避免列表页 N+1 查询。
- FTS 不再每次启动全量重建，仅在记录存在且 FTS 为空时重建。
- preload sandbox 正式开启：`BrowserWindow.webPreferences.sandbox = true`。
- preload 构建产物改为 CommonJS：`dist/preload/index.cjs`。
- 主进程加载 preload 路径改为 `../preload/index.cjs`。
- 增加本地文件安全工具：`apps/desktop/src/main/local-file-security.ts`。
- 附件打开限制在本地数据目录内。
- 附件导入和报告写入使用数据目录边界校验。
- Electron 升级到 `43.2.0`，解决依赖审计中旧版本风险。

验证通过：

- `npm run test:smoke`
- `npm ls electron sqlite3 sql.js`
- `rg "sql\.js|SqlJs|sql-wasm|@types/sql\.js|better-sqlite3" apps\desktop -n`
- `npm audit --omit=dev --registry=https://registry.npmjs.org`
- `git diff --check`

关键验证结果：

- smoke 输出 `ok: true`。
- 30 个验证学生创建后，重启应用仍能持久化读取。
- sandbox preload 下 `window.omniEdu` 可用。
- DeepSeek 设置保存后，重启应用仍能读取。
- 依赖树中只有 `electron@43.2.0` 和 `sqlite3@6.0.1`，无 `sql.js`。
- 依赖审计结果为 `found 0 vulnerabilities`。

## 当前工作区注意事项

以下文件在当前工作区存在已修改或未跟踪状态，其中部分是本轮前已有的 UI / AI 控制台相关改动。后续不要随意回滚：

- `apps/desktop/src/renderer/App.tsx`
- `apps/desktop/src/renderer/styles.css`
- `apps/desktop/src/preload/index.ts`
- `apps/desktop/src/shared/contracts.ts`
- `apps/desktop/src/main/index.ts`
- `apps/desktop/src/main/deepseek.ts`
- `apps/desktop/scripts/deepseek-smoke.mjs`
- `docs/20_UI_REDESIGN_PLAN.md`
- `docs/21_AI_CONSOLE_KNOWLEDGE_GRAPH_PLAN.md`

本轮新增或重点相关：

- `apps/desktop/src/main/db.ts`
- `apps/desktop/src/main/local-file-security.ts`
- `apps/desktop/electron.vite.config.ts`
- `apps/desktop/scripts/electron-smoke.mjs`
- `apps/desktop/package.json`
- `apps/desktop/package-lock.json`

## 下次继续方向

优先级建议：

1. 为 SQLite 数据层补单元测试或集成测试，覆盖学生、记录、附件、报告、搜索、设置持久化。
2. 梳理 `contracts.ts` 中与分页、AI 控制台、DeepSeek 设置相关的接口契约，确认 renderer 与 main 是否完全一致。
3. 继续做本地数据安全：加密配置、API Key 本地安全存储、备份恢复、导出包完整性校验。
4. 为上百 GB 数据做下一阶段设计：分库或分区策略、附件对象存储目录规范、索引维护任务、归档策略。
5. 清理和确认 UI / AI 控制台既有改动，把深度知识库、Docling / MinerU、知识图谱计划拆成可验证任务。

### 2026-07-28：HeroUI Pro AI 控制台改造上下文压缩

已完成：

- HeroUI Pro 全局策略已落地：官网/MCP 用于识别组件；会员或 token 受限时直接复用 `D:\WorkProject\HeroUIPro\herouipro-v3\src` 本地源码。
- 当前项目已复制并使用本地 HeroUI Pro 组件：`chain-of-thought`、`chat-conversation`、`chat-message`、`chat-source`、`chat-tool`、`markdown`、`prompt-input`、`prompt-suggestion`、`chat-attachment` 等。
- 已修复 Vite 报错：`App.tsx` 不再导入不存在的 `@heroui-pro/*`，改为当前项目内源码路径。
- AI 控制台第二块已完成：左侧上下文模块与输入框下方模块合并为 5 个横向上下文 chip；右侧思考链路栏删除；空间集中给对话区。
- AI 控制台第三块已完成：PDF、Word、Markdown 产物使用真实 `ChatAttachment` 卡片；点击产物后右侧展开文档预览，对话区左收。
- 已把思考过程改为 AI 回复内部默认展开的“思考过程摘要”，展示任务识别、学生档案、学习记录、证据边界、知识来源和产物判断。
- 修复 `listReports().map(this.mapReport)` 丢失 `this` 导致的 `parseQualityChecks` 运行时报错。

验证：

- `cd D:\WorkProject\EduProject\apps\desktop && npm run build` 通过。
- Playwright 验收通过：无 Vite overlay、无 `Failed to resolve import`、无前端 console error、3 个产物附件卡片、5 个上下文模块、右侧产物面板可展开、右侧思考链路栏不存在。
- dev 服务验收地址：`http://localhost:5174/`。

未完成 / 风险：

- 当前 PDF / Word 导出按钮仍是前端产物入口和状态提示，真实文件生成管线需后续接入本地文档生成能力。
- 工作区仍有多批历史改动混在一起，提交前必须拆分归属。

下次继续：

- 下一块优先做真实文档生成：Markdown -> PDF / DOCX 的本地生成、保存路径、老师确认状态和导出结果预览。
- 继续按“每完成一块就验收一次”的节奏推进 AI 控制台。

### 2026-07-28：AI 控制台对话记录、文件夹和本地持久化

已完成：

- 直接复用本地 HeroUI Pro `chat-list-view` 和依赖的 `list-view` 源码，接入左侧对话库。
- 新增 SQLite 表：`ai_conversation_folders`、`ai_conversation_sessions`、`ai_conversation_messages`。
- 新增主进程 / preload / shared contracts 能力：列出对话工作区、创建文件夹、创建会话、读取会话消息、追加消息、移动会话到文件夹。
- AI 控制台发送时先保存 user message，再保存 assistant message；DeepSeek 失败或未配置时也会保存错误回复和思考过程摘要。
- 左侧栏支持未归档分组、文件夹分组、新建对话、新建文件夹、会话列表和拖拽归类。
- 修复 `PromptInput.Send` 点击没有进入业务提交的问题：保留 Enter 提交，同时给 Send 绑定显式 click，并用 `aiSubmitInFlightRef` 防重复提交。

验证：

- `cd D:\WorkProject\EduProject\apps\desktop && npm run build` 通过。
- Electron + Playwright 临时数据目录验收通过：UI 发送后创建 1 个 session 和 2 条 messages；创建 `Review Folder`；通过 DragEvent drop 把 session 移入 folder；关闭并重启后 session、folder 和 messages 均仍可从 SQLite 读取。

未完成 / 风险：

- 普通浏览器打开 Vite dev URL 没有 Electron preload，不能用于验证 SQLite / IPC 持久化；此类功能必须用 Electron 窗口或 smoke 脚本验收。
- Playwright 原生 `dragTo()` 没有稳定传入业务所需的 `text/plain`，验收脚本使用真实 `DragEvent` + `DataTransfer` 触发 drop handler。

下次继续：

- 可以把本轮 Electron 对话持久化验收沉淀成正式 `scripts/ai-conversation-smoke.mjs`，纳入回归。
- 下一块继续做真实文档生成和导出管线。

### 2026-07-28：小智教育 AI harness 现状审计与下一步设计

已完成：

- 使用 codebase-memory-mcp 建立当前仓库索引，确认 AI 调用链为 `runAiConsole -> ai:runDeepSeek -> runDeepSeekChat`。
- 确认“所有问题走同一流程”的直接原因：固定 system prompt、固定上下文块、固定近 30 条记录、固定知识库/图谱检索、固定工具清单和前端关键词产物判断。
- 阅读本地 Augment 与 Manus 的 prompt、Agent loop 和 modules 参考。
- 核验 Claude Code 公开材料、Anthropic context engineering/evals、OpenAI Study Mode、UNESCO 教育 AI 指南和 DeepSeek JSON/tool call 官方文档。
- 新增 `docs/22_XIAOZHI_EDU_AI_HARNESS_PLAN.md`，包含小智定位、8 类路由、上下文编译、工具权限、结构化回复、核心 system prompt、eval 门槛和实施路线。

验证：

- 当前仓库图索引结果为 923 个节点、2377 条关系。
- 现状判断已对照 `deepseek.ts`、`main/index.ts`、`App.tsx` 和 `contracts.ts`。
- 本轮未修改 AI 运行代码，未调用 DeepSeek，未改动现有 UI。

未完成 / 风险：

- `navi-deep-research` 本地命令连续两次只启动服务、未返回计划 JSON；外部研究改用官方页面和 GitHub 公开材料完成。
- 用户消息末尾的第四个本地参考路径不完整；已读取三个明确文件，并补读同目录 Manus Prompt 文件。
- 新方案尚未实现，不能把 route、真实 tool call、结构化回复或 eval 说成已接入。

下次继续：

- 优先实现 `contracts + 48 个 eval case + router dry-run`，先观察路由准确率，不直接整体替换现有回复。
- 再实现按需 context compiler、DeepSeek JSON Output、本地 schema 校验和真实只读工具循环。

### 2026-07-28：AI 控制台产物文字入口、按需思考过程和右键归档

已完成：

- PDF / Word / Markdown 产物入口改为 AI 回复内的文字链接：`PDF 文件`、`Word 文件`、`Markdown 文件`；点击文字后右侧展开对应文档预览。
- 产物入口、思考摘要、上下文来源和工具调用都保存进 `ai_conversation_messages.metadata_json`，历史对话打开后仍能点击文档文字入口。
- `buildAiTrace` 改为按需生成：问候类输入只显示任务识别和上下文边界，不调用学生档案、学习记录、附件、知识库或图谱。
- AI 回复中的 `ChatSources` 和 `ChatToolGroup` 改为读取当前消息 metadata；没有实际上下文来源或工具调用时不显示。
- 左侧会话和文件夹支持右键菜单：重命名、归档。
- SQLite 会话和文件夹增加 `archived_at`，新增重命名 / 归档 IPC；归档文件夹会同步归档其中未归档会话。
- 设置页新增“已归档 AI 对话”栏目，可以看到归档文件夹和归档对话。

验证：

- `cd D:\WorkProject\EduProject\apps\desktop && npm run build` 通过。
- Electron + Playwright 临时数据目录验收通过：`hello` 问候只有 2 个思考摘要节点且 contextSources/toolRuns 均为空；历史回复渲染 3 个文档文字入口；点击文字打开右侧产物面板；右键重命名、右键归档、设置页归档列表均通过。
- `git diff --check` 通过，仅有 Windows 行尾提示。
- `rg "@heroui-pro" apps/desktop/src -n` 无命中。

未完成 / 风险：

- 当前文档入口仍是预览和导出入口，真实 PDF / DOCX 文件生成管线尚未接入。
- 修改了 main/preload/db 后，正在运行的 Electron dev 窗口应重启一次才能加载最新 IPC 能力；renderer 已通过 HMR 更新。

下次继续：

- 将本轮临时 Electron 验收沉淀为正式回归脚本。
- 下一块继续做真实文档生成：Markdown -> PDF / DOCX 文件落地、保存路径、打开文件和导出结果预览。

### 2026-07-28：AI 文档预览空框修复与可拖拽分界线

已完成：

- 修复生成文档 Markdown 任务列表中的异常大空框：在 `apps/desktop/src/renderer/styles.css` 中为 `.markdown input[type="checkbox"]` 明确设置 14px 尺寸、去掉全局 input 的宽高继承，并隐藏任务项默认列表符号。
- 右侧文档预览面板新增 `ai-artifact-resizer` 分界线，中间对话区和右侧文档区改为四列 grid：左侧对话栏 / 中间对话 / 8px 分界线 / 右侧文档。
- 分界线支持鼠标左右拖动，文档面板宽度限制在 320px 到 720px；同时支持键盘方向键调整，便于无鼠标场景验收。
- 顺手修复 `apps/desktop/src/main/deepseek.ts` 中阻断构建的 `sourceBlock` 三元表达式换行语法问题。

验证：

- `cd D:\WorkProject\EduProject\apps\desktop && npm run build` 通过。
- Electron + Playwright 临时数据目录验收通过：写入一条带 Markdown artifact 的 AI 历史消息，点击 `Markdown file` 打开右侧预览；任务复选框实际尺寸为 `14 x 14`；右侧面板宽度从 `420px` 调整到 `468px`。

未完成 / 风险：

- 本轮只修复 Markdown 任务复选框样式泄漏和文档面板分界线；未接入真实 PDF / DOCX 文件生成流水线。
- `deepseek.ts` 里存在历史中文乱码显示问题，本轮只做最小语法修复，没有重写 prompt 文案。

下次继续：

- 将本轮 Electron + Playwright 临时验收脚本沉淀为正式 `artifact-preview` 回归脚本。
- 继续推进真实文档生成和导出文件落地。

### 2026-07-28：小智 AI harness P0 改造落地

已完成：

- 新增 `apps/desktop/src/main/ai-harness/`：`router.ts`、`tool-registry.ts`、`schema.ts`、`eval-cases.ts`、`evals.ts`。
- 共享契约新增 8 类 route、上下文策略、结构化回复 `xiazhi.reply.v1`、产物请求、harness 摘要和 eval report 类型。
- `ai:runDeepSeek` 改为先执行 router dry-run，再按 route 通过真实只读工具注册表读取最小必要上下文。
- `runDeepSeekChat` 改为动态 system prompt、DeepSeek JSON Output、本地 schema 校验；结构校验失败时返回失败，不当作成功答案展示。
- 前端 AI 产物入口改为读取 `result.artifacts`，不再根据 prompt 关键词猜 PDF / Word / Markdown。
- 前端“思考过程摘要”优先读取 `result.harness`、`result.sources` 和真实 `toolRuns`，展示 route、按需上下文、schema 校验和工具结果。
- 新增 `apps/desktop/scripts/ai-harness-eval.mjs` 与 `npm run test:ai-harness`，48 条 router eval 覆盖 8 类 route、模糊输入和隐私/安全/写入边界。

验证：

- `cd D:\WorkProject\EduProject\apps\desktop && npm run test:ai-harness` 通过：48/48，routeAccuracy=1。
- `cd D:\WorkProject\EduProject\apps\desktop && npm run build` 通过。
- `cd D:\WorkProject\EduProject\apps\desktop && npm run test:smoke` 通过：30 个学生、1 个知识资源、2 个知识切片、4 个知识节点。
- `cd D:\WorkProject\EduProject\apps\desktop && node scripts/deepseek-smoke.mjs` 因未设置 `DEEPSEEK_API_KEY` 正常跳过。
- `cd D:\WorkProject\EduProject\apps\desktop && npm audit --omit=dev --registry=https://registry.npmjs.org` 通过：`found 0 vulnerabilities`。
- `cd D:\WorkProject\EduProject && git diff --check` 通过，仅有 Windows 行尾提示。

未完成 / 风险：

- 本轮实现的是确定性 router、按需上下文、结构化输出和只读工具注册表；尚未接入 DeepSeek tool_calls Beta 或写入确认队列。
- 未进行真实 DeepSeek live 调用，因为当前 shell 未设置 `DEEPSEEK_API_KEY`。
- 当前 eval 是规则门禁，后续还需要加入模型输出质量 grader 和 teacher usability 样本。

下次继续：

- 接入真实写入前预览/确认队列：报告、题组、学生标签和知识图谱关系都必须先预览再保存。
- 增加结构化回复的模型输出回归样本，覆盖 schema fail-closed、证据 sourceId 和隐私边界。

## 每轮更新模板

### 2026-07-28：小智 Agent loop 与学生引用解析改造

已完成：

- 新增 `apps/desktop/src/main/ai-harness/agent-loop.ts`，将小智主流程改为 `route -> plan -> tool_call -> observe -> reflect -> finalize` 的可审计 loop trace。
- `AiHarnessRunSummary` 新增 `trace`，`DeepSeekContext` 会把可展示的 Agent loop 轨迹放入 user prompt，要求模型只复述外部过程摘要，不展示隐藏推理链。
- `router.ts` 用干净 UTF-8 重写，并新增“小A/某学生 + 学习进度/题目/错因”等学生引用优先规则。
- `tool-registry.ts` 用干净 UTF-8 重写，新增 `resolve_student_reference` 工具；没有手动选中学生时，可从 prompt 中解析本地学生显示名，再读取学生档案和学习记录。
- AI 输出区删除回复底部 `ChatSources` / `ChatToolGroup` 渲染块，不再显示“上下文来源 / 上下文工具”折叠组件；保留回复内部的“思考过程摘要”。
- 48 条 eval 中替换一条对抗用例：`根据小A的学习进度抽取适合他的题目` 必须路由到 `practice_design`，并要求学生解析、档案和学习记录工具。

验证：

- `cd D:\WorkProject\EduProject\apps\desktop && npm run test:ai-harness` 通过：48/48，routeAccuracy=1。
- `cd D:\WorkProject\EduProject\apps\desktop && npm run build` 通过。
- `cd D:\WorkProject\EduProject\apps\desktop && node scripts/electron-smoke.mjs` 通过：30 个学生、1 个知识资源、2 个知识切片、4 个知识节点。
- 离线 mock dry-run 通过：`根据小A的学习进度抽取适合他的题目` 路由为 `practice_design`，解析到 `小A`，读取 2 条学习记录，trace 覆盖 route/plan/tool_call/observe/reflect/finalize。
- `git diff --check` 针对本轮触达文件通过，仅有 Windows 行尾提示。

未完成 / 风险：

- 当前 loop 是确定性工具编排骨架，尚未接入模型原生 tool_calls streaming、长期状态快照、OpenTelemetry spans 或写入确认队列。
- 真实 DeepSeek live 调用未执行；本轮验证覆盖 router、工具编排、构建和 Electron smoke。
- 之前临时 sqlite dry-run 因 PowerShell 管道中文编码污染误判过一次 route；后续 Node/PowerShell 临时脚本涉及中文 prompt 时优先用 UTF-8 文件或 Unicode escape。

下次继续：

- 接入模型 tool_calls 的事件流：LLM 提议工具 -> guardrail 校验 -> 执行 -> observe -> 复盘 -> 继续/终止。
- 将 trace 持久化为可折叠事件流，并考虑导出 OpenTelemetry 兼容 span。
- 增加模型输出质量 grader：证据覆盖、无意义推断、学生隐私边界、教师可执行性。

### 2026-07-28：小智教育 Agent 完整进化方案文档整理

已完成：

- 将 `docs/22_XIAOZHI_EDU_AI_HARNESS_PLAN.md` 从早期 harness 设计稿升级为“小智教育 Agent 完整进化迭代方案”。
- 新文档明确小智定位：教师工作台操作系统级 Agent，而不是普通聊天机器人或学生端助手。
- 补充反向审查：当前仍缺完整 tool-calling、trace 事件持久化、教育质量 grader、写入确认队列、三元题组闭环、知识库/图谱增强、数据安全和中控台行动感。
- 整理目标架构：Input Normalizer、Intent Router、Agent Planner、Guardrail Engine、Tool Registry、Context Compiler、Model Tool-Call Loop、Schema Validator、Artifact Builder、Teacher Confirmation Queue、UI Renderer、Eval/Telemetry。
- 拆解 12 个后续阶段：Agent Runtime、Tool Calling、Confirmation Queue、SubIntent Router、Structured Reply v2、Education Grader、三元题组闭环、错题图片与脱敏、知识库图谱增强、文档生成、Observability、教师可用性打磨。
- 在 `1.Agent.md` 中补充规则：后续小智进化开发以该文档为总路线。

验证：

- 已用 UTF-8 读取四份根文档和原 `docs/22_XIAOZHI_EDU_AI_HARNESS_PLAN.md` 后再整理。
- 本轮主要是文档改造，未修改运行代码。

未完成 / 风险：

- 文档仍是后续开发计划，不代表 Tool Calling、Confirmation Queue、Education Grader、三元题组闭环已实现。
- 下次开始实现前仍需先检查当前代码状态和未提交改动归属。

下次继续：

- 按 `docs/22_XIAOZHI_EDU_AI_HARNESS_PLAN.md` 第 18 节开始实现：`Agent Runtime v1 + Tool Calling v1 基础设施`。

```markdown
### YYYY-MM-DD：本轮任务标题

已完成：

-

验证：

-

未完成 / 风险：

-

下次继续：

-
```

### 2026-07-28：小智 Agent Runtime v1 持久化落地
已完成：

- 按 `docs/22_XIAOZHI_EDU_AI_HARNESS_PLAN.md` 第一阶段，新增 `AiAgentRun`、`AiAgentEvent`、`AiAgentRunStatus` 契约，并让 `AiAgentTraceStep` 支持 `inputSummary` / `outputSummary`。
- SQLite 新增 `ai_agent_runs`、`ai_agent_events` 表与索引；`OmniEduStore` 新增 `startAiAgentRun`、`recordAiAgentEvent`、`completeAiAgentRun`、`getAiAgentRun`、`listAiAgentEvents`。
- `runAiAgentLoop` 改为 `emit()` 事件流：`route -> plan -> tool_call -> observe -> reflect -> finalize` 每一步既返回给 harness，也按 runId 顺序落库。
- `ai:runDeepSeek` 现在每次请求都会创建 agent run；空输入、缺少 DeepSeek API Key、模型失败和模型成功都会写入终态，不再只有内存 trace。
- 前端 `runDeepSeek` 调用带上当前 `sessionId`，后续可把 run/events 与 AI 对话线程关联。
- 新增 `apps/desktop/scripts/ai-agent-runtime-smoke.mjs` 与 `npm run test:ai-agent-runtime`，用于纯本地验证小智 runtime 事件持久化，不调用外部模型。

验证：

- `cd D:\WorkProject\EduProject\apps\desktop && npm run test:ai-agent-runtime` 通过：临时数据根中创建 1 条 run，状态 `blocked`，落库 14 条事件，包含 `route/plan/tool_call/observe/reflect/finalize`，序列连续。
- `cd D:\WorkProject\EduProject\apps\desktop && npm run test:ai-harness` 通过：48/48，routeAccuracy=1。
- `cd D:\WorkProject\EduProject\apps\desktop && npm run build` 通过。
- `cd D:\WorkProject\EduProject\apps\desktop && npm run test:smoke` 通过：Electron 临时数据根验收正常，创建 30 个学生、1 个知识资源、2 个切片、4 个知识节点。

未完成 / 风险：

- 当前仍是确定性本地 loop 编排，还没有接入模型原生 tool_calls streaming。
- `ai_agent_events` 已落库，但 UI 目前仍通过当前请求返回的 harness trace 展示；还没有做历史 run/event 独立查询界面。
- 写入类动作仍未接入确认队列，不能宣称小智能自动保存报告、题组、标签或知识图谱关系。

下次继续：

- 进入 `Tool Calling v1`：定义真实工具注册表 schema、工具权限、参数校验、工具 dry-run、工具执行器和失败恢复。
- 然后做 `Confirmation Queue v1`：所有写入动作先生成预览，老师确认后再落库。

### 2026-07-28：小智 Tool Calling v1 基础设施落地
已完成：

- `tool-registry.ts` 从“确定性上下文读取清单”升级为真实工具注册表：每个工具都有 description、JSON 参数 schema、route allowlist、effect/privacy、maxOutputChars。
- 新增模型工具契约：`AiModelToolJsonSchema`、`AiModelToolDefinition`、`AiModelToolCall`、`AiModelToolReview`。
- 新增 `getModelToolDefinitions(router)`：按当前 route 只导出允许给模型看到的工具 schema。
- 新增 `reviewModelToolCall()`：本地审核工具存在性、route allowlist、contextPolicy、effect/privacy 和参数 schema。
- 新增 `executeAiToolCall()`：审核通过才执行真实只读工具；审核失败记录 blocked，不执行；工具输出进入 bounded payload。
- `compileAiContext()` 改为复用同一套工具审核/执行器，避免 deterministic context compiler 绕过 guardrail。
- `runDeepSeekChat()` 接入 OpenAI/DeepSeek 风格 `tool_calls`：模型返回工具调用时，主进程审核、执行、写 trace/event，再把 tool result 发回模型要求最终 `xiazhi.reply.v1` JSON。
- 新增 `apps/desktop/scripts/ai-tool-calling-smoke.mjs` 与 `npm run test:ai-tool-calling`。

验证：

- `cd D:\WorkProject\EduProject\apps\desktop && npm run test:ai-tool-calling` 通过：导出 5 个 route 允许工具；允许工具 `used`；`general_qa` 越权调用学生档案被 blocked；`limit=999` 坏参数被 blocked；长学习记录输出被截断并标记 `truncated=true`。
- `cd D:\WorkProject\EduProject\apps\desktop && npm run test:ai-agent-runtime` 通过。
- `cd D:\WorkProject\EduProject\apps\desktop && npm run test:ai-harness` 通过：48/48，routeAccuracy=1。
- `cd D:\WorkProject\EduProject\apps\desktop && npm run build` 通过。
- `cd D:\WorkProject\EduProject\apps\desktop && npm run test:smoke` 通过。
- `cd D:\WorkProject\EduProject && git diff --check` 通过，仅有 Windows 行尾提示。

未完成 / 风险：

- 本轮没有执行真实 DeepSeek live tool-call 调用；模型 tool-call 代码路径已接入，但验收是本地 deterministic smoke。
- 当前工具仍全部是只读工具；写入类工具必须等 `Confirmation Queue v1` 后再接入。
- Agent trace 中模型 tool-call 事件会在 DeepSeek 阶段追加；后续可在 UI 中区分“本地预检工具”和“模型请求工具”。

下次继续：

- 进入 `Confirmation Queue v1`：新增确认队列表、确认 IPC/preload 合约、AI 产物待确认项、老师确认后执行写入并 readback。

### 2026-07-28：小智 Confirmation Queue v1 落地

已完成：

- 新增 `AiConfirmationItem`、`AiConfirmationCreateInput`、`AiConfirmationDecisionResult` 等共享契约。
- 新增 SQLite 表 `ai_confirmation_items`，记录 AI 写入确认项、状态、预览、payload、执行结果和错误。
- 主进程新增确认队列方法：创建、列表、拒绝、确认执行。
- Confirmation Queue v1 只开放 `create_review_report`，AI 报告草稿确认前不会写入 `review_reports` 或生成报告 md 文件。
- `ai:runDeepSeek` 在结构化回复包含 `report_draft` 且有当前学生时，会创建待确认项，并把 agent run 终态置为 `waiting_confirmation`。
- preload 暴露 `listAiConfirmations`、`confirmAiConfirmation`、`rejectAiConfirmation`。
- AI 页面新增“待老师确认”队列卡片，可预览、确认保存或拒绝。
- 新增 `apps/desktop/scripts/ai-confirmation-smoke.mjs` 与 `npm run test:ai-confirmation`。

验证通过：

- `npm run test:ai-confirmation`：创建确认项报告数不变；拒绝后不写库；确认后报告数 +1；确认结果 readback 到真实 `report_...`；重复确认被拒绝。
- `npm run test:ai-harness`：48/48。
- `npm run test:ai-agent-runtime`。
- `npm run test:ai-tool-calling`。
- `npm run build`。
- `npm run test:smoke`。
- `git diff --check`：仅有 Windows 行尾提示，无 whitespace error。

未完成 / 风险：

- Confirmation Queue v1 只支持 AI 复盘报告草稿，不支持写学习记录、学生标签、题组、知识图谱关系。
- `report_draft` 确认项依赖 DeepSeek 结构化回复中的 `report_draft` artifact；真实 live API 仍需在配置 API Key 后单独验收。
- 当前 UI 显示待确认项和确认/拒绝按钮，但还没有更细的差异对比编辑器。

下次继续：

- 进入 `SubIntent Router + 96 Eval`：扩展二级意图，覆盖多学生歧义、时间范围、学科、知识点和写入意图。

### 2026-07-28：小智 SubIntent Router + 96 Eval 落地

已完成：

- `AiRouterDecision` 新增 `subIntent` 和 `slots`。
- 新增 `AiSubIntent` 枚举，当前覆盖问候、能力介绍、学生进度、薄弱点、错因、订正、三元题组、相似题、作业计划、备课、课堂活动、月报、周报、资源检索、引用来源、知识图谱、设置帮助、安全边界等 27 类。
- `AiRouterSlots` 支持抽取学生引用、多学生歧义、时间范围、学科、知识点和写入意图。
- `routeAiPrompt()` 改为先抽槽位，再判 route，再判 subIntent，再生成澄清问题。
- `ai_agent_runs.sub_intent` 现在写入真实 `router.subIntent`。
- Agent loop trace 和 DeepSeek prompt 都带上 route/subIntent/slots。
- `apps/desktop/src/main/ai-harness/eval-cases.ts` 扩展到 96 条。
- `apps/desktop/src/main/ai-harness/evals.ts` 同时校验 route、subIntent、工具、slots、clarification。

验证通过：

- `npm run test:ai-harness`：96/96，routeAccuracy=1。
- `npm run test:ai-agent-runtime`。
- `npm run test:ai-tool-calling`。
- `npm run test:ai-confirmation`。
- `npm run build`。

未完成 / 风险：

- 当前 router 仍是确定性规则，不是模型分类器；后续如引入模型分类，必须保留 96 eval 作为硬门禁。
- 多学生任务当前只会触发一个关键澄清问题，还未实现双学生并行读记录和对比报告。

### 2026-07-28：小智 Structured Reply v2 落地

已完成：

- `AiStructuredReply` 从 `xiazhi.reply.v1` 升级为 `xiazhi.reply.v2`。
- v2 新增 `subIntent`、`facts`、`risks` 和 `routeCheck`。
- 本地 schema 校验新增 route-specific 规则：学生诊断必须有 facts 或 unknowns；学生推断必须有 facts 支撑；三元题组必须包含原题/相似题/变式题结构或 `exercise_set` artifact；写入型报告必须在 `teacherConfirmations` 中说明需要老师确认后保存。
- `runDeepSeekChat()` 增加一次受控 JSON repair：第一次 v2 校验失败后，要求模型只修复 JSON，不再请求工具；repair 后仍失败则返回失败。
- DeepSeek system/user prompt 已改为要求 `xiazhi.reply.v2`。
- AI 前端结构校验提示改为 `xiazhi.reply.v2`。
- 新增 `apps/desktop/scripts/ai-structured-reply-smoke.mjs` 与 `npm run test:ai-structured-reply`。

验证通过：

- `npm run test:ai-structured-reply`：合法 v2 通过；route mismatch 被拦；学生诊断缺 facts/unknowns 被拦；坏三元题组被拦；写入报告缺老师确认被拦。
- `npm run test:ai-harness`：96/96。
- `npm run test:ai-agent-runtime`。
- `npm run test:ai-tool-calling`。
- `npm run build`。

未完成 / 风险：

- 真实 DeepSeek live v2 输出和 repair 还需在 API Key 配置后单独验收。
- v2 已能校验三元题组结构，但真实题库召回与三元题组闭环仍属于后续 Phase 7。

下次继续：

- 进入 `Education Grader v1`：用规则 grader 先拦“建议上传资料”类无意义回复、证据缺口伪装、标签化语言和写入边界错误。

### 2026-07-28：小智 Education Grader v1 与三元题组闭环 v1 第一段落地

已完成：

- 新增 `apps/desktop/src/main/ai-harness/education-grader.ts`，在 `xiazhi.reply.v2` schema 通过后继续执行教育质量规则评分。
- Education Grader v1 覆盖空泛上传 fallback、学生证据缺口、标签化语言、高风险缺少处置、手机号/身份证泄露、无事实支撑推断、写入缺确认。
- `runDeepSeekChat()` 已把教育评分写入 harness，并在 grader 未通过时 fail-closed。
- 新增 `question_bank_items` 与 `exercise_sets` SQLite 表。
- 新增题库/题组共享契约：`QuestionBankItem`、`SimilarQuestionMatch`、`ExerciseSet`、`ExerciseSetItem`。
- `practice_design` 路由新增 `question_bank` 上下文和 `search_similar_questions` 工具。
- DeepSeek prompt 增加本地题库候选区块和题源边界：本地题库题与 generated 题必须区分。
- Confirmation Queue 扩展 `save_exercise_set`，AI 生成的 `exercise_set` artifact 只创建待确认项，老师确认后才写入 `exercise_sets` 并 readback。
- preload / IPC 暴露 `createQuestionBankItem`、`searchQuestionBank`、`listExerciseSets`，供后续 UI 和验收使用。
- 新增 `apps/desktop/scripts/ai-exercise-set-smoke.mjs` 与 `npm run test:ai-exercise-set`。

验证通过：

- `npm run test:ai-education-grader`：goodScore=100，bad-case 命中 `empty_upload_fallback`、`student_gap_without_audit`、`student_labeling_language`、`missing_safeguarding_mitigation`。
- `npm run test:ai-exercise-set`：本地题库命中 3 条；`search_similar_questions` 状态 `used`；确认前题组数 0；确认后题组数 1；readback 到真实 `exercise_set_...`。
- `npm run test:ai-harness`：96/96，routeAccuracy=1。
- `npm run test:ai-agent-runtime`：通过，practice route 事件流包含 `question_bank` 与 `search_similar_questions`。
- `npm run test:ai-tool-calling`：通过，导出工具包含 `search_similar_questions`。
- `npm run test:ai-confirmation`：通过，报告确认队列未回归。
- `npm run test:ai-structured-reply`：通过。
- `npm run build`：通过。
- `npm run test:smoke`：通过。
- `git diff --check`：通过，仅有 Windows 行尾提示。

未完成 / 风险：

- Phase 6 当前是确定性规则 grader v1；模型 grader、教师可用性人工评分和年级适切开放评分尚未接入。
- Phase 7 当前完成“结构化题库表 + 本地相似题召回 + 题组确认保存”的第一段；错题图片解析、脱敏上云、OCR、完整题库导入 UI、embedding 召回尚未完成。
- 真实 DeepSeek live 生成 `exercise_set -> confirmation item` 仍需在 API Key 配置后单独验收。
- 当前 `exercise_set` 保存来自结构化 artifact 和题库命中摘要，还没有专门的题组编辑器 UI。

下次继续：

- 进入 Phase 8：错题图片与脱敏链路，优先做图片导入状态、OCR/解析任务占位、脱敏文本生成和老师修正入口。
- 或继续补 Phase 7 UI：题库导入/检索面板、题组预览编辑器、确认项差异化预览。

### 2026-07-28：Phase 8 错题图片与脱敏链路 v1 第一段落地

已完成：

- 新增错题图片分析共享契约：`MistakeImageAnalysis`、`MistakeImageAnalysisInput`、`MistakeImageCorrectionInput`、`SanitizedProblemText`、`MistakeImageRedaction`。
- 新增 SQLite 表 `mistake_image_analyses`，记录学生、学习记录、附件、本地图片路径、OCR 状态、原始解析文本、脱敏文本、脱敏记录、老师修正文和错误信息。
- 复用现有附件导入能力，错题图片会复制进学生本地数据目录；主进程校验路径必须留在 managed data root 内。
- 新增 `sanitizeProblemText()`，当前覆盖手机号、邮箱、身份证号样式文本和学生姓名/显示名脱敏，同时保留题干数学内容。
- 新增 `createMistakeImageAnalysis()`：无解析文本时进入 `needs_ocr`；有解析文本时生成 `sanitized`；可选附件必须属于该学生。
- 新增 `updateMistakeImageCorrection()`：老师修正后重新脱敏，状态变为 `teacher_corrected`，并同步附件 `extracted_text`。
- 主进程 / preload 暴露 `mistakeImages:createAnalysis`、`mistakeImages:updateCorrection`、`mistakeImages:list`、`mistakeImages:sanitizeText`。
- 新增 `apps/desktop/scripts/ai-mistake-image-smoke.mjs` 与 `npm run test:ai-mistake-image`。

验证通过：

- `npm run test:ai-mistake-image`：附件导入成功；无解析文本进入 `needs_ocr`；含手机号/身份证/邮箱/学生名的文本被脱敏；老师修正后状态为 `teacher_corrected`；原始图片没有上传。
- `npm run test:ai-harness`：96/96。
- `npm run test:ai-agent-runtime`：通过。
- `npm run test:ai-tool-calling`：通过。
- `npm run test:ai-confirmation`：通过。
- `npm run test:ai-structured-reply`：通过。
- `npm run test:ai-education-grader`：通过。
- `npm run test:ai-exercise-set`：通过。
- `npm run build`：通过。
- `npm run test:smoke`：通过。
- `git diff --check`：通过，仅有 Windows 行尾提示。

未完成 / 风险：

- Phase 8 当前只完成“本地图片托管 + 解析状态 + 脱敏 + 老师修正”的数据/API 第一段；尚未接入真实 OCR 引擎。
- 尚未接入“脱敏文本 -> 模型错因分析 -> 本地相似题召回 -> 三元题组草稿”的完整自动链路。
- 错题图片分析 UI 尚未完成，当前能力主要通过 IPC/preload 和 smoke 验收。
- 当前脱敏是规则版 v1，后续还需要扩展人名、学校、地址、社交账号等更复杂 PII。
- 真实 DeepSeek live 使用脱敏文本进行错题分析仍需在 API Key 配置后单独验收。

下次继续：

- 进入 Phase 9：知识库/图谱增强，优先做资源导入状态机、chunk 质量标记、引用来源稳定化和图谱节点/边可信度边界。
- 或继续补 Phase 8 UI：错题图片分析列表、OCR 待修正入口、脱敏文本预览和“一键送小智分析”。

### 2026-07-28：Phase 9 知识库/图谱增强 v1 第一段落地

已完成：

- `TeacherResourceParseStatus` 扩展为 `imported -> queued -> parsed -> chunked -> indexed -> graph_extracted -> ready` 及失败/部分状态口径；本地可解析文本导入后当前直接落 `ready`。
- `ResourceChunk` 新增学科、年级、知识点、题型、难度、sourceTrust、containsPersonalData、qualityScore、evidenceStrength。
- `KnowledgeNode` 新增 evidenceStrength；`KnowledgeEdge` 新增 evidenceStrength 和 evidenceKind。
- `resource_chunks`、`knowledge_nodes`、`knowledge_edges` SQLite 表增加对应列，并补迁移逻辑。
- 资源导入时会从标题/heading/content 推断基础教学元数据，计算质量分，检测手机号、邮箱、身份证号样式个人信息。
- 知识图谱现在为每个 chunk 建立章节/片段节点，并在可推断时建立知识点节点、题型节点和带 evidenceKind 的关系边。
- `searchKnowledge()` 支持多 token 检索，并按个人信息边界、质量分、时间和 chunk 顺序排序。
- `search_teacher_knowledge` 工具结果现在返回 evidenceStrength、qualityScore、sourceTrust 和 metadata；含个人信息 chunk 隐藏正文预览。
- `query_knowledge_graph` 工具结果返回节点和边，并显式给出 `graph_nodes_are_background_not_direct_text_evidence` 边界。
- 新增 `apps/desktop/scripts/ai-knowledge-graph-smoke.mjs` 与 `npm run test:ai-knowledge-graph`。

验证通过：

- `npm run test:ai-knowledge-graph`：导入 2 个 markdown 资源；状态均为 `ready`；生成 2 chunks、7 nodes、7 edges；一次函数 chunk 推断出数学/八年级/一次函数/解答题/direct/qualityScore 95；含个人信息 chunk 正文预览被隐藏；图谱返回背景边界。
- `npm run test:ai-harness`：96/96。
- `npm run test:ai-agent-runtime`：通过。
- `npm run test:ai-tool-calling`：通过。
- `npm run test:ai-confirmation`：通过。
- `npm run test:ai-structured-reply`：通过。
- `npm run test:ai-education-grader`：通过。
- `npm run test:ai-exercise-set`：通过。
- `npm run test:ai-mistake-image`：通过。
- `npm run build`：通过。
- `npm run test:smoke`：通过。
- `git diff --check`：通过，仅有 Windows 行尾提示。

未完成 / 风险：

- Phase 9 当前是规则版元数据/质量/图谱第一段，不是 embedding 检索、Graph RAG 或完整知识抽取。
- PDF/DOCX/PPT 等复杂资源仍进入 `needs_parser`，真实 Docling/MinerU 解析仍未接入。
- 知识点/题型推断是轻量规则，覆盖面有限；后续需要教师确认、人工修正和更完整的 schema。
- 图谱节点/边已经有 evidenceKind/evidenceStrength，但 UI 还没有专门展示和编辑这些可信度字段。

下次继续：

- 进入 Phase 10：真实文档生成与导出，优先做 Markdown -> PDF / DOCX 文件落地、导出状态、打开文件、历史对话可重新打开产物。
- 或补 Phase 9 UI：知识库 chunk metadata 查看、隐私标记提示、图谱边 evidenceKind/evidenceStrength 展示。

### 2026-07-28：Phase 10 真实文档生成与导出 v1 落地

已完成：

- 新增共享契约：`DocumentArtifactType`、`DocumentArtifactStatus`、`DocumentArtifactExportInput`、`DocumentArtifactExportResult`。
- 新增 SQLite 表 `document_artifacts`，记录 session/message、标题、格式、文件名、mime、Markdown 正文、真实文件路径、文件大小、sha256、状态和错误信息。
- `OmniEduStore.exportDocumentArtifact()` 已支持 Markdown、PDF、DOCX 三类产物真实写出；写出后计算 hash 并 readback，失败时记录 `failed` 且不返回成功。
- PDF v1 使用本地最小 PDF writer 生成真实 `%PDF-1.4` 文件；DOCX v1 使用本地 zip+xml writer 生成包含 `word/document.xml` 的真实 docx 包；Markdown 按原文写入。
- 主进程 / preload 新增 `documents:exportArtifact`、`documents:listArtifacts`、`documents:getArtifact`、`documents:showArtifact`。
- AI 右侧产物面板的“导出”按钮已从占位提示改为真实导出；导出后显示文件路径、sha256 前缀和状态，“在文件夹中显示”通过 artifact id 由主进程读库打开。
- 新增 `apps/desktop/scripts/ai-document-export-smoke.mjs` 与 `npm run test:ai-document-export`。

验证通过：

- `npm run test:ai-document-export`：Markdown/PDF/DOCX 三种文件真实生成；文件存在且非空；PDF 魔数为 `%PDF-`；DOCX 魔数为 `PK` 且包含 `word/document.xml`；SQLite 可按 session 列出 3 个 exported artifact；空正文导出失败。
- `npm run test:ai-harness`：96/96。
- `npm run test:ai-tool-calling`：通过。
- `npm run test:ai-structured-reply`：通过。
- `npm run test:ai-knowledge-graph`：通过。
- `npm run build`：通过。
- `npm run test:smoke`：通过。
- `git diff --check`：通过，仅有 Windows 行尾提示。

未完成 / 风险：

- PDF v1 是轻量本地生成器，当前主要保证真实 PDF 文件和可审计导出状态；中文字体、分页、表格、图片、公式和复杂排版仍需后续增强。
- DOCX v1 是最小 Office Open XML 包，能保存正文并被 smoke 验证包结构；复杂样式、表格、图片和可编辑模板仍未接入。
- 历史消息 metadata 中的 artifact 导出状态只在当前面板更新；后续需要把导出结果回写到消息 metadata 或在打开历史对话时自动按 artifact id 合并 `document_artifacts` readback。
- AI live 生成的 `pdf/docx/markdown` artifact 仍需在真实 DeepSeek API Key 环境下做人工验收。

下次继续：

- 进入 Phase 11：Observability + Regression，优先做小智 run/event/artifact/confirmation 的统一回归报告、耗时/token/context 预算统计、失败样本回放和发布前一键门禁。
- 或继续补 Phase 10 v1.1：PDF 中文字体/分页、DOCX 样式模板、导出结果回写历史消息 metadata。

### 2026-07-28：README 功能展示与 UI 页面说明

已完成：

- 新增根目录 `README.md`，作为项目对外入口文档。
- README 已包含项目定位、MVP 闭环、UI 预览图、已落地能力表、UI 页面导航表、AI 中控台交互说明、本地优先与安全边界、技术栈、快速启动、验证命令和当前边界。
- README 使用 `docs/image.png` 作为当前 UI 预览图。
- README 明确区分“已落地”“v1 已落地”和“当前边界”，避免把真实 OCR、复杂 PDF/DOCX 排版、Graph RAG、完整题库导入 UI 等后续能力写成已完成。

验证：

- 新增 README 前已完成整套门禁：`test:ai-harness`、`test:ai-agent-runtime`、`test:ai-tool-calling`、`test:ai-confirmation`、`test:ai-structured-reply`、`test:ai-education-grader`、`test:ai-exercise-set`、`test:ai-mistake-image`、`test:ai-knowledge-graph`、`test:ai-document-export`、`npm run build` 全部通过。

未完成 / 风险：

- README 当前只引用已有 `docs/image.png` 一张预览图；后续如果需要更完整 UI 展示，可以补充 AI 中控台、知识库、确认队列、文档导出等多张截图。

下次继续：

- 若继续做 Phase 11 Observability / Regression，需要同步 README 的验证命令和当前能力边界。

### 2026-07-28：Phase 11 Observability / Regression v1 数据层落地

已完成：

- 新增 `ai_regression_reports` 读写闭环，报告从真实 SQLite 数据生成，不手写通过结论。
- `OmniEduStore.buildAiTelemetrySnapshot()` 汇总 `ai_agent_runs`、`ai_agent_events`、`ai_tool_runs`、`document_artifacts`、`ai_confirmation_items` 和 `ai_tasks`。
- `OmniEduStore.createAiRegressionReport()` 生成 run 终态、事件轨迹、工具轨迹、文档导出、确认队列、延迟预算和 router eval 基线 gate。
- 主进程 / preload 暴露 AI 可观测性 IPC：获取 telemetry snapshot、创建回归报告、列表读取报告、按 id 读取报告。
- 新增 `apps/desktop/scripts/ai-observability-smoke.mjs` 与 `npm run test:ai-observability`。
- README 的已落地能力与验证命令已同步补充 Observability / Regression。

验证：

- `cd D:\WorkProject\EduProject\apps\desktop && npm run test:ai-observability` 通过：1 个 run、3 条 event、2 条 tool trace、1 个 exported artifact、1 个 rejected confirmation，7 个 gate 全部 passed。
- `cd D:\WorkProject\EduProject\apps\desktop && npm run build` 通过。

未完成 / 风险：

- Phase 11 当前完成数据层、IPC 和 smoke，尚未做专门的报告 UI、失败样本回放和发布前一键门禁页面。
- token/context 指标依赖上游任务写入 usage 和 payload 摘要；缺失时只能显示未知或 warning，不能估算编造。
- `docx/` 目录仍存在一组未提交删除状态，本轮继续排除，不纳入提交。

下次继续：

- 将 Observability / Regression 报告接入 AI 页面或设置页，展示 gate、趋势和失败样本回放。
- 补充发布前一键门禁命令，把 harness、tool calling、confirmation、document export 和 observability 串成固定检查。

### 2026-07-28：Phase 11 Observability + Regression v1 落地

已完成：

- 新增共享契约：`AiTelemetrySnapshot`、`AiTelemetryLatency`、`AiRegressionGate`、`AiRegressionReport`、`AiRegressionReportInput`。
- 新增 SQLite 表 `ai_regression_reports`，用于保存回归报告标题、状态、摘要、telemetry snapshot、gate 列表和完整报告 JSON。
- `OmniEduStore.buildAiTelemetrySnapshot()` 已能聚合小智 run/event/tool/artifact/confirmation/task usage 数据，覆盖 route、model、状态、工具调用、P50/P95/avg 延迟、token 和上下文预算。
- `OmniEduStore.createAiRegressionReport()` 已能生成 fail-closed gate：运行终态、事件轨迹、工具轨迹、文档导出状态、确认队列状态、延迟预算和 router eval baseline。
- 主进程 / preload 新增 `aiObservability:getSnapshot`、`aiObservability:createRegressionReport`、`aiObservability:listRegressionReports`、`aiObservability:getRegressionReport`。
- 新增 `apps/desktop/scripts/ai-observability-smoke.mjs` 与 `npm run test:ai-observability`，可在本地创建 seed 数据、写入 run/event/tool/artifact/confirmation/task usage，再生成并读回回归报告。
- README 已补充 Observability / Regression 能力和门禁命令；`docs/22_XIAOZHI_EDU_AI_HARNESS_PLAN.md` 已把 Phase 11 标记为 v1 完成。

验证通过：

- `npm run test:ai-observability`：状态 `passed`；runCount 1；eventCount 3；toolEventCount 2；artifact exported 1；confirmation rejected 1；tokenBudget total 200；contextBudget sourceCount 1；所有 gates 通过。
- `npm run test:ai-harness`：96/96，routeAccuracy=1。
- `npm run test:ai-agent-runtime`：通过，事件阶段覆盖 route/plan/tool_call/observe/reflect/finalize，工具轨迹包含 used/blocked。
- `npm run test:ai-document-export`：Markdown/PDF/DOCX 真实文件、hash、readback 和失败路径通过。
- `npm run build`：通过。

未完成 / 风险：

- Phase 11 v1 是本地确定性 telemetry/report 能力，不等于完整线上 APM 或 OpenTelemetry dashboard。
- 当前没有专门的 Observability UI 仪表盘；报告已能通过主进程/preload API 和 SQLite 存取，后续需要在 AI 页或设置页展示。
- 回归报告当前保存在 SQLite，尚未提供 Markdown/HTML 导出。
- token budget 只统计已写入 `ai_tasks.result_json.usage` 的任务；真实 DeepSeek live usage 仍需在 API Key 环境下单独校验。
- “每次回答可查工具轨迹”在数据层/API 层已具备，历史对话 UI 对单次 run 的完整事件展开仍是后续增强。

下次继续：

- 进入 Phase 12：教师可用性打磨 v1，重点减少模板化废话、按任务控制回复长度、优化确认队列打扰和常用工作流快捷入口。
- 可并行补 Phase 11 v1.1：Observability UI、回归报告 Markdown/HTML 导出、历史会话按 runId 展开完整工具轨迹。

### 2026-07-29：Phase 12 教师可用性打磨 v1 第一段落地

已完成：

- 新增 `apps/desktop/src/main/ai-harness/usability-policy.ts`，定义 route-aware usability profile、DeepSeek 可用性指令、确定性 Usability Grader 和教师可读 Markdown formatter。
- `runDeepSeekChat()` 已在 `xiazhi.reply.v2` schema 与 Education Grader 之后继续执行 Usability Grader；未通过时 fail-closed，并把 `usabilityGrade` 写入 harness trace。
- `structuredReplyToMarkdown()` 已改为使用教师可读 formatter：普通问答/工作台帮助不机械展开“事实/证据/推断”段落；学生诊断等任务优先展示“依据/教学判断/下一步/需要老师确认/产物”。
- 可用性规则会 critical 拦截“当前为普通问答模式、无法自动切换、请手动选择左侧导航、请手动切换学生数据”等退化话术。
- AI 页快捷提示已从能力宣传改成老师常用工作流：查学生错题、生成三元题组草稿、家长沟通草稿、档案缺口清单、课堂导入、确认队列处理。
- Confirmation Queue UI 默认只展示最近 2 个待确认项，剩余项折叠提示，减少对当前对话的打断；确认前仍不写业务表。
- 新增 `apps/desktop/scripts/ai-usability-smoke.mjs` 与 `npm run test:ai-usability`，覆盖好样例通过、退化手动切模块失败、过长泛答扣分、写入缺确认失败和自然产物入口。
- README、`docs/22_XIAOZHI_EDU_AI_HARNESS_PLAN.md` 和四份根文档已同步 Phase 12 v1 第一段状态。

验证通过：

- `npm run test:ai-usability`：good profile 为 `evidence_snapshot`；`manual_module_switch_fallback`、`answer_too_long_for_route`、`too_many_next_actions`、`write_action_without_low_friction_confirmation` 均按预期触发。
- `npm run test:ai-structured-reply`：`xiazhi.reply.v2` 合法、route mismatch、三元题组结构和写入报告确认规则通过；formatter 期望已改为 `## 依据`。
- `npm run test:ai-education-grader`：通过。
- `npm run test:ai-harness`：96/96，routeAccuracy=1。
- `npm run test:ai-agent-runtime`：通过，事件阶段覆盖 route/plan/tool_call/observe/reflect/finalize。
- `npm run test:ai-observability`：状态 `passed`，7 个 gate 全部 passed。
- `npm run test:ai-document-export`：Markdown/PDF/DOCX 真实文件导出、hash/readback 和列表读取通过。
- `npm run build`：通过。
- `git diff --check`：无空白错误，只有 Windows 行尾提示。

未完成 / 风险：

- Phase 12 v1 当前是 prompt 指令 + deterministic grader + formatter + UI 降噪 + smoke，不等于真实老师人工评分平均 >= 4/5。
- 返工量下降、常见任务 1-2 轮内完成尚未用真实教师样本集和生产 telemetry 证明。
- 尚未接入模型 grader、年级适切开放评分、真实 DeepSeek live 样本回放和人工标注集。
- `docx/` 目录仍存在一组历史未提交删除状态，本轮继续排除，不纳入本次改动判断。

下次继续：

- 进入 Phase 12 v1.1：收集 20-50 条真实老师任务样本，建立人工评分表和失败样本回放；把 usability score 汇入 Observability / Regression 报告。
- 可并行补充 AI 页面中按 runId 展开 `usabilityGrade` issue 的调试入口，但不要展示隐藏思维链。

### 2026-07-29：Phase 12 v1.1 教师样本集 proxy + Usability 回归报告整合

已完成：

- 新增 `apps/desktop/src/main/ai-harness/usability-eval-cases.ts`，内置 25 条代表性教师任务 proxy 样本，覆盖 8 类 route、好样例、手动切模块退化、过长泛答、写入缺确认、模板流水线、下一步泛化和风险噪声。
- `npm run test:ai-usability` 已接入样本集，要求样本数 >= 20、全部 case 按预期通过，并继续覆盖手动切模块、过长泛答、写入缺确认和自然产物入口。
- `recordAiConsoleRun()` 已把 `harness.usabilityGrade` 摘要写入 `ai_tasks.result_json`，只保存评分和 issue 摘要，不保存隐藏推理链。
- `AiTelemetrySnapshot` 新增 `usability` 汇总：sampleCount、passedCount、failedCount、averageScore、minScore、profileCounts、issueCounts。
- `createAiRegressionReport()` 新增 `usability_quality_gate`；当 AI console 任务缺少 usabilityGrade 时 warning，存在未通过 grade 或平均分低于阈值时 failed。
- `AiRegressionReportInput` 新增 `expectedUsabilityEvalTotal`、`expectedUsabilityEvalPassed`、`minimumUsabilityAverageScore`；回归报告新增 `usability_eval_baseline`。
- `apps/desktop/scripts/ai-observability-smoke.mjs` 已 seed 一条带 usabilityGrade 的真实 AI console task，并验证 SQLite snapshot/report readback 中的 usability gate。
- 新增 `docs/23_XIAOZHI_USABILITY_EVAL_RUBRIC.md`，定义真实老师人工评分 1-5 分、必须记录字段和当前 proxy 样本边界。

验证通过：

- `npm run test:ai-usability`：25/25 proxy 样本通过，suiteAverageScore=89；覆盖 `manual_module_switch_fallback`、`answer_too_long_for_route`、`too_many_next_actions`、`write_action_without_low_friction_confirmation`。
- `npm run test:ai-observability`：回归报告状态 `passed`；新增 `usability_quality_gate` 与 `usability_eval_baseline` 均 passed；snapshot usability 为 sampleCount=1、passedCount=1、averageScore=92、profileCounts.evidence_snapshot=1。
- `npm run test:ai-structured-reply`：通过。
- `npm run test:ai-education-grader`：通过。
- `npm run test:ai-harness`：96/96。
- `npm run test:ai-agent-runtime`：通过。
- `npm run test:ai-document-export`：通过。
- `npm run build`：通过。

未完成 / 风险：

- 25 条样本是本地代表性 proxy，不是真实老师人工样本；不能据此宣称“教师人工评分平均 >= 4/5”已完成。
- 真实老师样本录入 UI、真实 DeepSeek live 样本回放、模型 grader、返工量统计和常见任务 1-2 轮完成率仍未接入。
- 当前 `usability_quality_gate` 依赖 `recordAiConsoleRun()` 记录的 `harness.usabilityGrade`；历史任务没有该字段时只会 warning，不能回填伪造评分。
- `docx/` 目录仍存在一组历史未提交删除状态，本轮继续排除，不纳入本次改动判断。

下次继续：

- Phase 12 v1.2：接入真实老师人工评分样本的录入/导入与统计，至少记录 `teacherScore`、`needsRewrite`、`roundsToUseful` 和 `mainIssueCode`。
- 或先做真实 DeepSeek live 样本回放，把 live 输出、schema、Education Grader、Usability Grader、人工评分表串起来。

### 2026-07-29：Phase 12 v1.2 教师人工评分基础设施 + Live replay 脚本已完成

已完成：

- 新增 `AiUsabilityHumanReviewInput`、`AiUsabilityHumanReview`、`AiUsabilityHumanReviewSummary` 共享契约，字段对齐 `docs/23_XIAOZHI_USABILITY_EVAL_RUBRIC.md`。
- 新增 SQLite 表 `ai_usability_reviews`，记录 `sampleId`、prompt、route、subIntent、teacherScore、needsRewrite、roundsToUseful、mainIssueCode、teacherNote、reviewedAt、run/session/model。
- `OmniEduStore` 新增 `createAiUsabilityReview()`、`listAiUsabilityReviews()`、`buildAiUsabilityReviewSummary()`，并做 1-5 分、1-12 轮等输入校验。
- 主进程 / preload 已暴露 `aiObservability:createUsabilityReview`、`aiObservability:listUsabilityReviews`、`aiObservability:getUsabilityReviewSummary`。
- `AiTelemetrySnapshot` 新增 `humanUsability` 汇总；`createAiRegressionReport()` 新增 `teacher_review_score_gate`，可按最小样本数、最低教师平均分和最大有用轮次做门禁。
- 新增 `apps/desktop/scripts/ai-usability-human-review-smoke.mjs` 与 `npm run test:ai-human-review`，用 seeded 人工评分样本验证写入、列表、summary、snapshot 和 gate readback。
- 新增 `apps/desktop/scripts/ai-live-usability-replay.mjs` 与 `npm run test:ai-live-usability`；无 `DEEPSEEK_API_KEY` 时明确 skipped，有 key 时走真实 Electron/DeepSeek 路径校验 schema、Education Grader、Usability Grader 和 loop trace。

验证通过：

- `npm run test:ai-human-review`：3 条 seeded review 写入并读回，summary sampleCount=3、averageTeacherScore=4、needsRewriteCount=0，`teacher_review_score_gate` passed。
- `npm run test:ai-usability`：25/25 proxy 样本通过，suiteAverageScore=89。
- `npm run test:ai-observability`：状态 `passed`；新增 `teacher_review_score_gate`、`usability_quality_gate`、`usability_eval_baseline` 均 passed；snapshot humanUsability sampleCount=1、averageTeacherScore=5。
- `npm run test:ai-structured-reply`：通过。
- `npm run test:ai-education-grader`：通过。
- `npm run build`：通过。
- `npm run test:ai-live-usability`：构建通过；因当前 shell 未设置 `DEEPSEEK_API_KEY`，live replay 明确 skipped，未声称 live API 已验证。

未完成 / 风险：

- v1.2 完成的是人工评分基础设施、IPC 和 seeded smoke，不等于已经收集真实老师外部评分样本。
- 人工评分 UI / CSV 导入面板仍未完成；当前 renderer 可通过 preload API 调用，但还没有专门可视化表单。
- DeepSeek live replay 脚本已就绪，但本轮没有 API Key，未执行真实线上模型样本。
- 教师人工评分平均 >= 4/5、返工量下降、常见任务 1-2 轮内完成仍需要真实老师样本集证明。

下次继续：

- Phase 12 v1.3：做人工评分 UI / CSV 导入、评分样本列表和失败样本回放入口。
- 配置 `DEEPSEEK_API_KEY` 后运行 `npm run test:ai-live-usability`，把真实 live 输出与人工评分样本串联。

### 2026-07-29：Phase 12 v1.3 人工评分 UI / CSV 导入 / 失败样本回放已完成

已完成：

- 设置页新增“小智质量评审”面板，展示人工样本数、平均评分、需重写数量和平均有用轮次。
- 新增单条人工评分录入表单，字段覆盖 `sampleId`、prompt、route、subIntent、teacherScore、needsRewrite、roundsToUseful、mainIssueCode、teacherNote、model、reviewedAt。
- 新增 CSV / TSV 粘贴与文件读取入口；renderer 使用浏览器 FileReader，不直接访问 Node 文件 API；导入时逐条调用 preload `createAiUsabilityReview()`。
- 新增失败样本回放列表：低分、`needsRewrite=true` 或 `mainIssueCode != none` 的样本会显示在设置页，可一键放回 AI 输入框重新运行。
- 新增 `apps/desktop/scripts/ai-usability-review-ui-smoke.mjs` 与 `npm run test:ai-human-review-ui`，通过真实 Electron UI 验证表单保存、CSV 导入、失败样本回放和 SQLite summary readback。
- README、`docs/22_XIAOZHI_EDU_AI_HARNESS_PLAN.md`、`docs/23_XIAOZHI_USABILITY_EVAL_RUBRIC.md` 和四份根文档已同步 v1.3 口径。

验证通过：

- `npm run test:ai-human-review-ui`：Electron 设置页真实交互通过；sampleCount=2、averageTeacherScore=4、needsRewriteCount=1、失败样本已回放到 AI 输入框。
- `npx tsc --noEmit`：通过。

未完成 / 风险：

- v1.3 让老师可以录入和导入评分，但本轮仍没有外部真实老师样本；seeded/UI smoke 只证明链路正确。
- 失败样本回放当前是“放回 AI 输入框重新运行”，尚未自动把旧回复、新回复和再次评分绑定成完整对比实验。
- 尚未实现评分趋势图、模型 grader、真实 DeepSeek live API Key 回放结果和外部老师评分数据集。

下次继续：

- Phase 12 v1.4：真实外部老师样本采集、失败样本二次评分对比、live DeepSeek replay 绑定 review rows、模型 grader / 年级适切开放评分。
- 如果只补工程能力，可先做 Observability UI，把 `humanUsability`、`teacher_review_score_gate` 和失败样本列表展示到回归报告页面。

### 2026-07-29：Phase 12 v1.4 before/after replay experiment 基础设施已完成

已完成：

- 新增 `AiUsabilityReplayExperimentInput`、`AiUsabilityReplayExperiment`、`AiUsabilityReplaySummary` 共享契约。
- 新增 `ai_usability_replay_experiments` SQLite 表和 created/before/after 索引；实验记录绑定两条真实 `ai_usability_reviews`，scoreDelta、roundsDelta、issue transition 和 improved 通过 join 回读计算。
- 新增 Store 方法 `createAiUsabilityReplayExperiment()`、`listAiUsabilityReplayExperiments()`、`buildAiUsabilityReplaySummary()`。
- `AiTelemetrySnapshot` 新增 `usabilityReplay`；`createAiRegressionReport()` 新增 `usability_replay_improvement_gate`。
- 主进程 / preload 新增 `createAiUsabilityReplayExperiment`、`listAiUsabilityReplayExperiments`、`getAiUsabilityReplaySummary`。
- 设置页“小智质量评审”新增回放实验 KPI、失败样本“设为 before”、保存 after 人工评分后自动创建 before/after 实验、实验列表和再次回放入口。
- 新增 `apps/desktop/scripts/ai-usability-replay-smoke.mjs` 与 `npm run test:ai-replay`。
- `apps/desktop/scripts/ai-usability-review-ui-smoke.mjs` 已扩展：真实 Electron UI 覆盖表单保存、CSV 导入、回放到 AI 输入、选择 before、保存 after、replay summary/list readback。
- `apps/desktop/scripts/ai-observability-smoke.mjs` 已 seed replay experiment，让新 `usability_replay_improvement_gate` 与其他 gates 全部 passed。
- README、`docs/22_XIAOZHI_EDU_AI_HARNESS_PLAN.md`、`docs/23_XIAOZHI_USABILITY_EVAL_RUBRIC.md` 和四份根文档已同步 v1.4 口径。

验证通过：

- `npm run test:ai-replay`：experimentCount=1、improvedCount=1、averageScoreDelta=3、averageRoundsDelta=3，`usability_replay_improvement_gate` passed。
- `npm run test:ai-human-review-ui`：Electron 设置页真实交互通过；sampleCount=3、averageTeacherScore=4、needsRewriteCount=1、replayExperimentCount=1、replayAverageScoreDelta=3。
- `npm run test:ai-observability`：11 个 gates 全部 passed，包含 `usability_replay_improvement_gate`。
- `npm run test:ai-human-review`、`npm run test:ai-usability`、`npm run test:ai-harness` 均通过；其中 `test:ai-human-review` 的 reportStatus 为 warning，因为该脚本只验证人工评分基础设施，不 seed replay 实验。

未完成 / 风险：

- v1.4 仍是 seeded before/after 工程链路，不等于外部真实老师样本验收。
- 真实 DeepSeek live 输出尚未在本机通过 API Key 执行并绑定 after review；`npm run test:ai-live-usability` 无 key 时仍只能 skipped。
- 模型 grader、评分趋势图、年级适切开放评分、真实返工量下降统计和常见任务 1-2 轮完成率仍未完成。

下次继续：

- Phase 12 v1.5：收集外部真实老师样本，运行 live DeepSeek replay，并把 live 输出、模型版本、promptVersion、token usage 和 after review 绑定到 replay experiment。
- 建立模型 grader / 年级适切开放评分，并用真实样本复核 seeded replay gate 是否仍可信。

### 2026-07-29：Phase 12 v1.5 模型 grader proxy + 年级适切评分基础设施已完成

已完成：

- 新增 `AiModelGraderMode`、`AiModelGradeInput`、`AiModelGrade`、`AiModelGradeSummary` 共享契约，明确区分 `deterministic_proxy` 与未来 `llm_judge`。
- 新增 `apps/desktop/src/main/ai-harness/model-grader.ts`，提供本地 deterministic proxy grader，覆盖 evidence、actionability、safety、gradeAppropriateness、concision、teacherControl 六个维度。
- 新增 SQLite 表 `ai_model_grades`，记录 prompt、answerMarkdown、route、subIntent、targetGrade、被评模型、grader 模型、graderMode、六维分数、overall、passed、issueCodes 和 rationale。
- `OmniEduStore` 新增 `createAiModelGrade()`、`listAiModelGrades()`、`buildAiModelGradeSummary()`。
- 主进程 / preload 新增 `createAiModelGrade`、`listAiModelGrades`、`getAiModelGradeSummary`。
- `AiTelemetrySnapshot` 新增 `modelGrader` 汇总；`createAiRegressionReport()` 新增 `model_grader_quality_gate`。
- 设置页“小智质量评审”新增模型 grader KPI 与样本列表。
- 新增 `apps/desktop/scripts/ai-model-grader-smoke.mjs` 与 `npm run test:ai-model-grader`。
- README、`docs/22_XIAOZHI_EDU_AI_HARNESS_PLAN.md`、`docs/23_XIAOZHI_USABILITY_EVAL_RUBRIC.md` 和四份根文档已同步 v1.5 口径。

验证通过：

- `npm run test:ai-model-grader`：sampleCount=2、averageOverallScore=5、averageGradeAppropriatenessScore=5，坏样本命中 `manual_module_switch_fallback`、`unsafe_student_label`、`missing_evidence_boundary`、`missing_actionable_next_step`，`model_grader_quality_gate` passed。
- `npm run test:ai-observability`：状态 passed，包含 `model_grader_quality_gate` 且所有 seeded gates 通过。
- `npm run test:ai-human-review-ui`：Electron 设置页真实交互通过，并验证模型 grader 面板可见。
- `npm run test:ai-replay`、`npm run test:ai-human-review`、`npm run test:ai-usability`、`npm run test:ai-harness` 均通过；其中 `test:ai-human-review` 的 reportStatus 仍可能为 warning，因为该脚本只验证人工评分基础设施，不 seed replay/model-grade 完整门禁。

未完成 / 风险：

- v1.5 是 deterministic proxy 和工程落点，不等于真实 LLM-as-judge 裁判已执行。
- 外部真实老师样本集、真实 DeepSeek live 输出绑定 after review、promptVersion/token usage 绑定、评分趋势图和真实返工量下降统计仍未完成。
- 年级适切评分当前是启发式维度，后续需要真实年级/学科样本与老师人工复核校准。

下次继续：

- Phase 12 v1.6：外部真实老师样本 + live DeepSeek 绑定 + 真实 LLM-as-judge。
- 将 live 输出、模型版本、promptVersion、token usage、人工 after review 和 `ai_model_grades` 样本绑定到同一条 replay 证据链。
- 做评分趋势图和最终对抗性审查，验证真实老师平均分、返工轮次和常见任务 1-2 轮完成率。

### 2026-07-29：Phase 12 v1.6 live evidence binding 工程第一段已完成

已完成：

- 扩展 replay 契约和 readback：`AiUsabilityReplayExperiment` 暴露 `beforeRunId`、`afterRunId`，`AiUsabilityReplaySummary` 汇总 `liveLinkedCount`。
- 扩展模型 grader 契约和 SQLite：`ai_model_grades` 新增 `run_id`、`session_id`、`prompt_version`、`total_tokens`，summary 汇总 `runLinkedCount`、`tokenKnownCount` 和 `promptVersionCounts`。
- `recordAiConsoleRun()` 已把 `harness.agentRunId` 写入 `ai_tasks.result_json`，作为任务记录反查 agent run 的稳定入口。
- `createAiRegressionReport()` 新增 `live_evidence_binding_gate`，支持最小 live-linked replay 数和 run-linked model grade 数门禁。
- `gradeAiReplyWithModelProxy()` 支持传入同一轮 run/session/promptVersion/token usage；未来真实 `llm_judge` 可复用同一写入口。
- 设置页“小智质量评审”已展示 replay live 绑定数量、model grader run 绑定数量，并在样本列表展示 runId、promptVersion、totalTokens。
- 新增 `apps/desktop/scripts/ai-live-evidence-binding-smoke.mjs` 与 `npm run test:ai-live-evidence`，seed 同一条 runId 贯穿 after review、replay、model grade、telemetry 和 regression gate。
- README、`docs/22_XIAOZHI_EDU_AI_HARNESS_PLAN.md`、`docs/23_XIAOZHI_USABILITY_EVAL_RUBRIC.md` 和四份根文档已同步 v1.6 口径。

验证通过：

- `npm run test:ai-live-evidence`：`live_evidence_binding_gate` passed；`liveLinkedReplay=1`、`runLinkedModelGrades=1`、`tokenKnownModelGrades=1`。
- `npm run test:ai-observability`：13 个 gates 全部 passed，包含 `live_evidence_binding_gate`、`model_grader_quality_gate`、`teacher_review_score_gate` 和 replay gate。
- `npm run test:ai-model-grader`：模型 grader proxy 读写、summary 和 gate 继续通过。
- `npm run test:ai-replay`：before/after replay experiment 读写和改善 gate 继续通过。
- `npm run test:ai-human-review-ui`：真实 Electron 设置页质量评审面板、人工评分、CSV 导入、失败回放和模型 grader 面板继续通过。

未完成 / 风险：

- v1.6 是 seeded/live-ready 工程绑定，不等于真实 DeepSeek API 已执行；没有 `DEEPSEEK_API_KEY` 时 `npm run test:ai-live-usability` 仍只能 skipped。
- 外部真实老师样本、真实返工量下降、常见任务 1-2 轮完成率、评分趋势图和真实 LLM-as-judge 仍未完成。
- `live_evidence_binding_gate` 当前证明 run 证据链不断裂；它不能独立证明回答质量达到老师满意。
- `docx/` 目录仍存在一组历史未提交删除状态，本轮继续排除，不纳入本次改动判断。

下次继续：

- Phase 12 v1.7：接入外部真实老师评分样本、真实 `DEEPSEEK_API_KEY` live replay、真实 `llm_judge` 写入 `ai_model_grades` 和评分趋势图。
- 用真实样本同时验证平均教师分、返工轮次、replay 改善率、model grader 分数、token/latency 预算和常见任务 1-2 轮完成率。

### 2026-08-10：DeepTutor v1.5.11 源码调研与融合设计完成

已完成：

- 本地检查 `D:\WorkProject\DeepTutor`，确认工作区干净，基线为 v1.5.11 / `456f9c24226e008f1ff07a7e3455d7b4d39f6221`。
- 建立 DeepTutor 与 EduProject 代码图谱，追踪 `ChatOrchestrator`、`AgentLoop`、Capability/Tool Registry、StreamBus、TurnRuntime、Deep Solve、Deep Question、Deep Research、Visualize 和 Mastery Path。
- 确认当前小智“固定流程”的直接原因：`runAiAgentLoop()` 预执行 route 全部允许工具，`runDeepSeekChat()` 只处理一次、最多 4 个工具调用，然后要求模型不再请求工具。
- 新增 `docs/24_DEEPTUTOR_OMNI_EDU_INTEGRATION_DESIGN.md`，给出模块映射、Electron 宿主 + Python sidecar 架构、双向协议、数据流、复用边界、分阶段路线、48 条新增 Capability eval 和对抗性审查。
- 新增 `docs/25_DEEPTUTOR_FEATURE_INVENTORY_AND_PRODUCTION_MATRIX.md`，按生产模块粒度盘点 77 项能力：17 项整体集成、38 项二次开发、8 项裁剪替换、14 项延后/不集成；逐项记录源码证据、融合边界、关键验收，并补齐 SLO、安全、异常 UI、部署和测试 DoD。
- 官方 GitHub、v1.5.11 release、论文和 Apache-2.0 License 已核对；论文结果只作为方向参考，未写成 Omni-Edu 已验证效果。

当前结论：

- 复用 DeepTutor 的 Capability/Tool 两层模型、agent loop、流式事件、solve/question/research/visualize/mastery 内核。
- 不迁移 Next.js UI、PocketBase/多用户、Partners/IM、第二套会话数据库和默认宽工具面。
- Electron main 继续作为工具审核、隐私、确认、SQLite 和审计的唯一控制面；Python sidecar 不得直接读取业务数据。

DT-0 实施进展（2026-08-10，本轮）：

- 已将 DeepTutor v1.5.11 固定 commit `456f9c24226e008f1ff07a7e3455d7b4d39f6221` 机械 vendor 到 `python/vendor/deeptutor`，并保留 `DEEPTUTOR_LICENSE`、`DEEPTUTOR_THIRD_PARTY_NOTICES.md`、`DEEPTUTOR_UPSTREAM.md`。
- 已创建 `python/omni_edu_deeptutor_bridge`，实现版本化 NDJSON handshake/start/cancel/shutdown、输入大小/能力/预算校验和 terminal event。
- sidecar 的 chat dry-run 已实际调用 vendor 的 `deeptutor.agents.chat.agent_loop.AgentLoop`、`UnifiedContext`、`StreamBus`，Fake OpenAI stream 只用于离线协议验收，不代表真实模型已接入。
- Electron main/preload 已接入 sidecar handshake/start/cancel/stop；Electron smoke 已真实启动 sidecar 并核验上游 commit。
- HostToolProxy 双向第一段已接入：sidecar 可发 `xiazhi.host_tool.request.v1`，Electron main 复用 `routeAiPrompt()` + `executeHostToolRequest()`，返回 bounded `xiazhi.host_tool.result.v1`；`npm run test:deeptutor-host-proxy` 和 Electron host-proxy smoke 已通过。
- 新增 `deeptutor-agent-tool-call-smoke.mjs`：上游 AgentLoop 原生解析 fake provider 的 `tool_calls`，通过 DeepTutor `dispatch_tool_calls` 调用受控 `_HostProxyTool`，再经 Electron 返回 blocked result；`npm run test:deeptutor-agent-tool-call` 已通过（8 个事件、nativeToolCall=true、blockedByHost=true）。
- `npm run test:deeptutor-bridge` 和 `node scripts/electron-smoke.mjs` 通过；`npm run build` 通过。
- Electron smoke 现在还会启动 `hostToolAuto` chat turn，从真实 Electron→main→sidecar 路径回读 native `tool_call` 与 blocked `observe` 证据。

未完成 / 风险：

- 当前 chat dry-run 使用真实 AgentLoop，deep_solve/mastery_path dry-run 使用上游 Capability 创建的 AgenticChatPipeline + AgentLoop；deep_question/deep_research/visualize 已在 manifest 中声明，但尚未接入 HostToolProxy 和真实模型/业务工具。
- sidecar 事件已由 Electron main 的 `persistDeepTutorEvent()` 按 turn 串行写入 `ai_agent_events`；`done` 事件会闭合 `ai_agent_runs`，preload 暴露 `onDeepTutorEvent`、`getAiAgentRun`、`listAiAgentEvents`，renderer 已订阅安全摘要并去重。
- 事件链路对抗性验收已加入连续序列、终态、`<think>`/`<thinking>` 隐藏思维标记、secret-like 文本和 blocked HostToolProxy observation 断言；Electron smoke 已从 SQLite readback 验证。
- Fake OpenAI stream 只为确定性测试服务；真实 DeepSeek/模型配置、流式 token、工具调用和本地 SQLite evidence readback 仍需下一阶段完成。
- Python 运行时目前依赖可发现的本机 Python 与 `openai` 包；正式打包需增加受支持 Python 3.11–3.13 运行时和依赖清单，不能把 Python 3.14 本地验证当成部署承诺。
- 文档中的 2 秒响应指 run 创建、首个可见事件或常规问答首 token，不表示研究、讲义、动画等长任务能在 2 秒内最终完成；相关性能尚未实测。
- Python 最小依赖、Windows 打包体积、HostToolProxy 崩溃恢复和 capability checkpoint 仍需 DT-0 spike 验证。
- Phase 12 v1.7 的真实老师/live/llm_judge 工作仍未完成，不能被 DeepTutor 设计任务替代。

下次继续：

- 将 native tool-call parity fixture 替换为真实模型配置，补齐审核/执行/拒绝的工具轨迹和崩溃恢复。
- 将普通问答页面提交入口切到 sidecar runtime，再接 deep_question/deep_research/visualize 的真实上游 pipeline。
- 真实模型接入前继续用 vendor AgentLoop 的 deterministic fake stream 做回归，避免把网络不稳定误判为 loop 逻辑通过。

### 2026-08-11：DeepTutor 事件证据闭环与对抗性验收

- `apps/desktop/src/main/index.ts` 已将 sidecar event 映射为现有 `AiAgentTraceStep`，按 turn 使用 promise queue 串行写入 `ai_agent_events`；终态 `done` 会更新 `ai_agent_runs`。
- preload/renderer 已接入 `ai:deepTutorEvent` 订阅、`getAiAgentRun` 和 `listAiAgentEvents` readback；底部独立“上下文工具”组件仍不恢复，工具/来源进入运行时间线。
- `npm run test:deeptutor-bridge` 通过：10 个 response、2 个协议错误、8 个事件，并验证连续 sequence、终态和隐藏思维标记/提示词不泄露。
- `npm run test:deeptutor-host-proxy` 通过：5 个事件，blocked HostToolProxy request/result 往返成功。
- `npm run test:deeptutor-agent-tool-call` 通过：8 个事件，证明上游 AgentLoop 原生 tool-call 经过 dispatcher 和宿主拒绝结果后仍能完成终态。
- 按 `eval-harness` 新增 `.claude/evals/deeptutor-native-tool-call.md` 与 `.log`；native tool-call smoke 已连续 3 次通过（pass^3=1.00）。
- 新增主进程持钥匙的 ModelProxy：`xiazhi.model.request.v1` / `xiazhi.model.result.v1`，sidecar 不接收 API Key；`npm run test:deeptutor-model-proxy` 通过，Electron smoke 验证无 Key 时 run 为 failed 且 evidence 不含 `sk-`。
- `npm run build` 与 `node scripts/electron-smoke.mjs` 通过；Electron smoke 从 SQLite 回读 DeepTutor run/event，验证 finalize、blocked observe、连续序列和 secret-like 文本过滤。
- 这仍是 DT-0 第一段证据闭环，不代表真实模型、真实业务工具、普通 AI 页面全量迁移或 deep_question/deep_research/visualize 已生产可用。

### 2026-08-11：普通问答入口已接入 DeepTutor（本地 fail-closed 验收）

- `apps/desktop/src/renderer/App.tsx` 的 AI 提交入口现在调用 preload 的 `runDeepTutorConsole`，由 Electron main 组装 router、按需上下文、system prompt、HostTool schema 并启动 DeepTutor sidecar。
- console 结果从 `ai_agent_events` 的 `finalize/result` 解析结构化回复；失败时优先读取具体的 failed event，避免通用 `done` 覆盖 `MISSING_MODEL_CREDENTIAL` 等可行动错误。
- `node scripts/electron-smoke.mjs` 已通过：无 `DEEPSEEK_API_KEY` 时 console 入口返回 `ok=false`、持久化 runId 和可行动凭证提示；这不是 live provider 通过。
- 下一步仍是把只读业务工具接入真实模型并完成 deep_question/deep_research/visualize 等 Capability 的逐模块 eval，不得因为入口已切换而扩大完成口径。
- console 成功路径现在会把 AgentLoop 的 tool_call/observe 事件折叠为 `toolRuns`，并把结构化 evidence 映射为 `sources`；这让 UI 使用真实运行证据，不再依赖旧的预置 trace。
- 修复 `_HostProxyTool` 对宿主成功状态的误判：`xiazhi.host_tool.result.v1` 的成功值是 `used`，不是 `succeeded`；Electron smoke 已用真实 seed student 验证 `get_student_profile` 为 `used` observation。
- 发现并修复 DeepSolve/Mastery 适配层并发 monkey-patch 竞争：临时注入 upstream `AgenticChatPipeline` 时使用 bridge 级互斥保护，Electron smoke 同时启动 chat/deep_solve 回合后均能闭合。

### 2026-08-11：R09 可暂停—恢复协议

- 新增 `AiAgentRunStatus = waiting_input`、`AiCapabilityCheckpoint` 和 `ai_capability_checkpoints` 表；主进程在 `ask_user` 请求到达时先写 checkpoint/guardrail，再把运行置为 `waiting_input`。
- sidecar 使用 `xiazhi.user_input.request.v1` 等待结果；主进程通过 `deepTutorSubmitUserInput` 做 requestId、turnId、长度/数量校验，成功后 resolve checkpoint、恢复 run 并回写 observe 证据。
- Electron smoke 已通过：checkpoint pending → waiting_input → answered → succeeded，重复提交被拒绝，事件序列连续，checkpoint readback 为 resolved；构建与四个 DeepTutor smoke 均通过。
- 同一 smoke 已覆盖取消路径：取消等待会将 checkpoint 标为 `cancelled`、run 收敛为 `blocked`，不会留下悬挂 `waiting_input`。
- 当前边界：checkpoint 状态已持久化，但等待者 resolver 仍在当前主进程内存；进程重启恢复/迁移需要下一轮实现，不能宣称跨重启继续同一个 AgentLoop。
### 2026-08-11 Deep Question capability adapter

- `bridge.py` now runs the vendored `DeepQuestionCapability` pipeline for `deep_question` and emits bounded upstream result evidence.
- Main process converts valid `qa_pair` results into a `save_exercise_set` pending confirmation linked to `runId/sessionId/studentId`; renderer receives `ai:deepTutorConfirmation` via preload.
- `data/user/settings/main.yaml` and `agents.yaml` are non-secret minimal upstream fixtures. Dry-run evidence is not live-provider evidence.
- Electron smoke passed the successful path and invalid `num_questions` fail-closed path; production remains gated on live provider, 48 eval, recovery, performance, and teacher review.
- DeepResearch dry-run now returns a bounded outline preview/config; Visualize dry-run now returns a validated Mermaid result through the upstream analysis/code/review pipeline. Both remain offline evidence until live provider and artifact security gates pass.
- Mastery Path first slice now builds a bounded `masterySnapshot` from Omni `learning_records` and runs vendored DeepTutor policy functions in the sidecar. Explicit JSON attempt evidence is required; unstructured records remain unknown. Electron smoke verifies mixed attempts stay below the 0.9 gate.
- Mastery 第一条真实工具边界已落地：`apps/desktop/src/main/ai-harness/mastery-snapshot.ts` 统一生成显式证据快照；新增只读 `mastery_status` 注册表/执行器/路由白名单，仅返回知识点准确率、证据计数和 `rawRecordsIncluded=false`，不返回原始记录正文。
- `python/omni_edu_deeptutor_bridge/bridge.py` 的 HostToolProxy tool_result 事件现在只追加 bounded `resultKeys` 与可验证的 `rawRecordsIncluded`，避免把完整工具 payload 复制进事件审计。
- Electron smoke 新增 `mastery_path + mastery_status` HostToolProxy 真实 round-trip，并验证 used observation、连续事件和 raw-record redaction；构建、bridge contract、Electron smoke 均通过。
- Mastery 互动第一条闭环已完成：新增 `ai_mastery_questions` 私有表与主进程 CRUD/状态转换；`mastery_quiz` 由宿主按薄弱知识点创建本地可评分选择题，sidecar 自动进入 `ask_user` checkpoint；提交答案后仅写入 question row，`mastery_grade` 校验答案并写回显式 `mastery_attempt` 学习记录。
- Electron smoke 已覆盖 quiz pending checkpoint → answered → grade used → learning_records writeback，以及 duplicate grade blocked；expectedAnswer 未出现在 checkpoint/event。
- 取消/sidecar stop 路径现在会把关联的 pending/answered mastery question 标记为 `cancelled`，避免用户取消后留下可再次评分的悬挂题目；构建与 Electron smoke 仍通过。
- Mastery assess/build 已补齐本地 HostTool vertical slice：`mastery_assess` 对 concept/design 写回显式 `mastery_attempt`，错误类型被 blocked；`mastery_build` 持久化 `ai_mastery_paths`，支持 replace/append、版本递增和 append ID 重映射；Electron smoke 已验证路径回读、无效输入拒绝和路径知识点评估。
- Mastery assess/build 已收紧为教师确认边界：HostTool 先返回 `pendingConfirmation`，main 创建 `save_mastery_state` confirmation，只有确认后才写入 `mastery_attempt`/`ai_mastery_paths`；Electron smoke 覆盖 confirm/reject。新增 `deeptutor-capability-evals.ts` 与脚本，48/48 通过，且不代表 live provider、跨进程恢复或生产性能已完成。
- 新增 sidecar 断线和 turn watchdog：`DeepTutorSidecarClient.onExit` 通知 main，main 将活动 run/checkpoint fail-closed 收敛并清理 binding；`maxWallMs` 超时写入 guardrail 并尝试取消 turn。`test:deeptutor-recovery` 和 `test:deeptutor-performance` 已通过，但性能数据仅为本地 dry-run。
- 本轮策略增量（2026-08-11）：`mastery_status` 现在在同一份 Omni 学习记录快照上返回 `omni.mastery.policy.v1` 的受限 next/dueReviews；新增 `evidenceKind`，concept/design 只有教师确认的 assess 正确证据才能 mastered，quiz 不得自动越过质性门禁。策略 8-case 回放、Electron smoke 和 48-case capability eval 均通过；仍不能宣称 live provider、跨进程恢复或真实负载完成。

- 本轮 E16 增量（2026-08-11）：新增 `analyze_learning_progress` 只读 HostTool 与 `learning-analytics.ts`，基于同一份 SQLite `learning_records` 生成 `omni.learning.analytics.v1`。支持时间/学科过滤、UTC 日历边界、事实/未知、来源 ID 与重算 key；跨学生、非法日期、>366 天和空数据均有明确边界。报告日期/来源会通过 sidecar bounded metadata 进入教师确认项，确认后 readback 才落库。E16 smoke 8/8、Electron、确认、Observability 通过；真实 provider/教师质量/高负载仍未完成。
- 本轮 E14 增量（2026-08-11）：新增 `classify_error_patterns` 与 `error-taxonomy.ts`，复用 DeepTutor `structural/deviation/application/metacognitive` taxonomy，新增 `unknown` 证据不足边界。分类优先显式 metadata/空答案，应用错误和未知保留教师复核；原文不进入结果，跨学生/日期/无数据/HostToolProxy/no-write 均有 smoke 证据。E14 专项 11/11、build 和既有回归通过；真实教师标注一致性仍未完成。
- E14 回归结果（2026-08-11）：专项 11/11、build、96/96 路由、48/48 capability、Electron smoke 和 Observability 全部通过；真实教师标注一致性、live provider 与持久化教师标签仍未完成。
- 本轮 E17 增量（2026-08-11）：新增 `analyze_geometry_figure` 与 `vision-solver.ts`，复用 DeepTutor Vision Solver 的单次分析/一次修复思路及 GeoGebra 常见语法修正；工具只在 `error_analysis` route 暴露，读取本地图片分析的脱敏状态，默认不上传原图、不写业务数据。GeoGebra 命令经白名单、脚本/URL/隐私文本过滤后才进入 `omni.vision.solver.v1` 草稿；专项 smoke 12/12、HostTool round-trip、no-image、no-write 通过。真实 vision provider、教师几何标注一致性和渲染组件仍未完成。
- 本轮 C01 增量（2026-08-11）：直接复用 DeepTutor NotebookManager 的 Notebook/NotebookRecord 结构，改为 Omni-Edu SQLite `teacher_notebooks`/`teacher_notebook_records`，并通过 main/preload IPC 提供 CRUD。备课本和记录均有乐观 `version`，删除为可恢复软删除；专项 smoke 14/14 通过，C02 按需分析/摘要仍未接入。
- 本轮 C02 增量（2026-08-11）：新增 `notebook-analysis.ts` 与 `analyze_notebook_context` HostTool。router 仅在用户明确提到笔记、备课本、历史草稿或 notebook 时加入 `teacher_notebook` context；执行器复用 DeepTutor Notebook Analysis/Summarize 的目录选择→有限细节→摘要草稿边界，最多扫描 200 条目录、读取 5 条细节、单条 1800 字符，查询/标题/摘要/用户问题/输出统一经 `sanitizeProblemText` 脱敏。`record_summary` 只返回 `requiresTeacherReview=true` 的草稿，不自动写回；指定 notebook/record 跨范围或不存在会 fail-closed。专项 smoke 14/14，route eval 扩展为 98/98；真实 provider 与教师人工摘要一致性仍未完成。
- 本轮 C03 增量（2026-08-11）：直接复用 DeepTutor Question Notebook 的 bookmarked/category/link/reference 语义，但把 `question_bank_items` 保持为唯一 canonical 题目真源，新增 SQLite overlay `question_notebook_bookmarks`、`question_notebook_categories`、`question_notebook_category_links`、`question_bank_usage`。main/preload 已提供收藏、分类 CRUD/软删除恢复、关联、usage 历史和分页检索；题组保存会为现有题库题记录去重 usage。新增 `question_notebook` router 子意图和 `search_question_notebook` 只读 HostTool；对抗复核发现 practice_design 默认会带学生/知识库上下文，已收紧为只含 question_notebook。专项 smoke 22/22，route eval 100/100；真实教师 UI、批量迁移与真实 provider 仍未完成。
### C05 Renderer

- `renderTeachingBookMarkdown` 与 `draft_teaching_book_markdown` 已支持受控 block、未知类型 fallback、bounded preview/no-write；专项 24/24，route eval 104/104。正式 artifact 导出仍待教师确认边界。
- C06 已完成首个 source explorer/spine/page planner 适配：`plan_teaching_book` 基于 bounded knowledge chunks 生成可审阅章节草案；专项 20/20，route eval 106/106；章节落库确认 UI 与高级多轮 spine synthesis 仍待后续。
- C07 已完成 fingerprint health 持久化：新增 teaching_book_invalidations 局部失效队列、refresh/list IPC 与 refresh_teaching_book_health tool；专项 24/24，route eval 108/108。仍待后台 watcher、教师 UI 队列操作和大规模知识库压测。
- C08 已完成首个 CoWriter patch vertical slice：teaching_book_patches 保存 before/after 与版本历史，draft/apply/undo/list 通过 main/preload 暴露；专项 24/24，route eval 110/110。
- C09 已完成 CoWriter automark/react-edit/stream 选区 vertical slice：selection patch 保存范围、原文 SHA-256、before/after 和 bounded stream events；版本或选区漂移 fail-closed；专项 28/28，route eval 112/112。真实 provider 流、教师 diff UI 和多块事务仍待后续。
- C10 已完成 Persona 策略 vertical slice：新增 teacher/peer/research_assistant 受控 profile，router/Agent trace 可见角色指导，但工具和上下文策略保持不变；专项 18/18，capability 48/48。可编辑 Persona 存储、治理和 UI 仍待后续。
- C11 已完成 L1 可检查记忆 vertical slice：复用 Omni SQLite run/event/tool 作为唯一真源，新增 `inspect_memory_trace`、memory_trace 路由隔离与 list/get IPC；专项 smoke 通过，覆盖 bounded 投影、敏感字符串不泄露、limit 和缺失 run fail-closed；C12-C15（L2/L3/graph/治理）仍待后续。
- C12 已完成第一条 L2 vertical slice：新增 surface 文档/条目/修订表、`memory_summary` 路由与两个只读工具、typed IPC 和教师记忆页面；候选摘要不写正式字段，教师采纳/编辑走证据引用校验与乐观锁。`npm run test:deeptutor-memory-summary` 36/36、ai-harness 116/116、build 通过；C13-C15 仍未完成。
- C13 已完成 L3 第一条 vertical slice：四槽位文档/条目、跨 surface bounded draft、`memory_synthesis` 路由与工具、typed IPC 和记忆页采纳入口；`npm run test:deeptutor-memory-synthesis` 18/18、ai-harness 118/118、build 通过。C14-C15（证据图、治理与生产化）仍待后续。
- C14 已完成记忆证据图第一条切片：动态生成 L3→L2→run/event bounded projection，新增 `inspect_memory_graph` 与 graph IPC；专项 smoke 扩展为 24/24，覆盖边完整性、敏感字段排除和 allowlist。C15（删除传播、审计治理、生产化）仍待后续。
- C15 已完成第一条记忆治理切片：新增 `memory-governance.v1` 报告、悬空 ref 检查、删除/停用统计、AI 写入关闭声明和治理 IPC/UI；综合 smoke 30/30。全项目仍未达到 live provider、真实教师样本、跨进程恢复和高并发生产 SLO 的最终验收。
- 记忆性能基线已补：100 L2 + 1 L3 fixture、20 次并发 graph/governance 查询 p95/max 2ms；这只覆盖本地记忆投影，不代表整个 Agent 或 provider 链路的 2 秒 SLO。
- C03 UI 增量（2026-08-11）：App 新增“题本”导航与教师工作区，支持搜索/分类/仅收藏、答案解析展开、来源/使用次数、收藏切换、分类创建与软删除；写入仍经 typed IPC，版本冲突会刷新并提示。`npm run build` 与 question-notebook smoke 22/22 通过；批量迁移、真实教师样本和 live provider 仍未完成。
- 备份完整性增量（2026-08-11）：`exportDataRoot` 现在写入 v1 manifest（相对路径/大小/SHA-256）并立即回读验证；新增 `verifyDataBackup` preload/main IPC 与设置页入口。专项 smoke 9/9 覆盖正常验证、篡改检测和源目录边界。当前只证明可验证备份，不包含自动恢复或跨磁盘灾备演练。
- C05 UI 增量（2026-08-11）：新增“讲义”导航、书目选择、来源健康警告、主进程 Markdown preview 与教师点击导出；未知 block fallback，预览不写文件，导出由 `document_artifacts` readback 验证。PDF/DOCX 导出已改为 Unicode CID 字体与标题/列表/代码样式，并由 `test:ai-document-export` 验证；复杂分页/多媒体、真实教师样本和完整高保真版式仍未完成。
- R10 预算与续写增量（2026-08-12）：`DeepTutorRunBinding` 新增 `maxEvents/persistedEvents`，主进程与 Python `TurnState.max_events` 双层硬终止；最多 3 代 continuation checkpoint，run 以 `blocked` 结算并发放一次性 token，达到上限的第 4 代以 `failed` 硬终止且不再发 token。新增 `deepTutorContinueTurn`，子 run 通过 `parent_run_id` 建立 lineage，重复/过期 token fail-closed；超出原事件/时间预算时先创建 `budget_approval` checkpoint，不消费 continuation token，老师审批后才继续；renderer 读取 checkpoint 时只得到可继续摘要，不得到 request/system prompt/host tool schema，AI 对话流提供真实继续与预算批准按钮；SQLite 抢占使用变更行数保证并发单赢家。`npm run test:deeptutor-r10-budget` 31/31 通过；token/费用预算和真实 provider 长输出恢复仍未实现。
- R11 运行变更增量（2026-08-12）：新增 `ai_agent_run_requests` 私有快照、`ai_agent_run_actions` 幂等 action 表和 `ai:deepTutorMutateRun` typed IPC。retry 只允许 failed/blocked/cancelled 源运行，branch/regenerate 可从终态运行创建带 `parent_run_id` 的子 run；重复/并发相同 key 复用已有子 run，assistant 消息提供重试/分支/重新生成入口。`npm run test:deeptutor-r11-lineage` 12/12 通过，覆盖 close/reopen lineage；复杂写工具事务回滚、多设备冲突、live provider 和完整结果合并仍未实现。
- 结构化回复容错增量（2026-08-12）：`parseStructuredReply` 新增 bounded JSON object 提取，支持模型返回代码围栏或少量前后说明后继续走同一 `xiazhi.reply.v2` 校验；不会补造缺失字段，截断 JSON 仍返回“模型没有返回合法 JSON”。`npm run test:ai-structured-reply` 通过，能力 eval 仍为 48/48。
- JSON 截断根因修复（2026-08-12）：DeepTutor `result` 此前与普通 trace 共用 8,000 字符 `detail` 上限，长但合法的 `xiazhi.reply.v2` 会在闭合大括号前被截断。新增 `deeptutor-event-bounds.ts`，普通事件仍限 8,000，最终结果限 64,000；12,553 字符长回复回归已通过 `test:ai-structured-reply`，项目 build 通过。未使用真实 API Key 做 live provider 复测。
- 前端接线增量（2026-08-12）：新增 `TeacherNotebookWorkspace` 并接入“备课本”导航，复用既有 notebook/record CRUD、版本锁、软删除和 notebook 恢复；同一 Electron smoke 覆盖创建、编辑、删除、审计显示、恢复及重启回读，前端清单提升为 26/26，组件状态为 6/6。记录删除尚无 restore IPC，只能审计查看。
- 当前回归异常（2026-08-12）：`test:deeptutor-capability-evals` 的 `context-09` 报 `unbounded tool output`，与本轮 JSON 事件截断修复不是同一链路，不能表述为 48/48 当前通过；需要后续单独定位最近 tool-output 边界变化。
- R04 多轮 Agent Loop 增量（2026-08-12）：`runDeepSeekChat` 从单轮工具调用改为最多 3 轮/8 次的 observation-driven loop；每轮都会重新经过 `executeAiToolCall` 主进程审核，工具结果 bounded 后再决定下一工具或最终 JSON，预算耗尽写入 guardrail 并强制 no-tool finalization。`npm run test:deeptutor-multi-round` 10/10 通过，覆盖两轮真实 SQLite 工具调用、第二轮 trace、预算硬终止和最终结构化收敛。
- R09 重启恢复增量（2026-08-12）：新增 pending `user_input` checkpoint 回读、原子 `claimAiUserInputCheckpoint` 和 typed `listPendingDeepTutorInputs` IPC；应用重启后答案不再丢失，而是基于私有 request snapshot 创建带 `parent_run_id` 的受限恢复 child run，移除 dry-run 的一次性 hostToolAuto，避免重复询问。`npm run test:deeptutor-r09-restart` 12/12 通过，覆盖 close/reopen、child 成功、checkpoint resolved 和重复提交拒绝。
- Console JSON 修复（2026-08-12）：ModelProxy 无工具的最终回合现在请求 `json_object`；console 对 provider 忽略 JSON 模式的情况最多执行一次受控 repair，并记录 bounded guardrail。repair 仍失败时，sidecar 已 succeeded 的 run 也会被结算为 failed。parser 对多个 JSON 外壳优先选择 `xiazhi.reply.v2` object；build、结构化 smoke、125-case harness 通过。
- R05 progressive tool disclosure（2026-08-12）：新增 `TOOL_GROUPS`、`load_tools` 和 `progressiveToolNames`；模型首轮只看到 loader/受限核心工具，加载工具族后下一轮才收到真实 schema。主进程按 router/context allowlist 审核且 `toolsExecuted=false`，Python `_SingleToolLookup` 只激活宿主返回的 `loadedToolNames`，不把完整 catalog 暴露给 AgentLoop。`npm run test:deeptutor-progressive-tools` 18/18、`npm run test:deeptutor-progressive-sidecar` 14/14、`npm run test:smoke` 通过；真实 provider、复杂 context compiler 和全量性能门禁仍未完成。
- 前端接线首片（2026-08-12）：新增 `apps/desktop/src/renderer/components/MistakesWorkspace.tsx`，把已有 `attachments`、`mistakeImages`、`sanitizeProblemText`、AI 对话和 confirmation preload 接到错题页面；支持本地图片元数据、`needs_ocr`、教师修正、脱敏预览、无 Key 失败态、三元题组编辑及拒绝写入。现有 `electron-smoke.mjs` 在同一用户路径验证 SQLite 回读，并在 1366×768 / 1920×1080 检查布局。`npm run test:smoke` 通过；原生文件选择器仍用受控 SQLite fixture，题本收藏和文档导出尚缺完整 renderer E2E。
- 结构化 JSON 容错（2026-08-12）：`schema.ts` 去除 UTF-8 BOM 后再提取 JSON；`runDeepSeekChat` 对 xiazhi.reply.v2 使用 `response_format=json_object`，最多两次受控 repair，repair 次数和 schema 错误写入真实 trace。`npm run test:ai-structured-reply` 通过；真实 provider 返回和 live schema gate 仍未验证。

## 2026-08-12 前端闭环增量

- `QuestionNotebookWorkspace.tsx` 已从 `App.tsx` 拆出，具备 loading/error/empty/no-hit/populated、来源标识、收藏和分类入口；renderer component state test 5/5。
- 现有 Electron smoke 新增前端 18/18：错题 needs_ocr/校正/脱敏/no-key、题组拒绝零写入与确认落库、题本 local/generated 来源、收藏与 hit/no-hit、Markdown 点击导出、SQLite/文件 hash、重启回读。
- 截图保存在 `apps/desktop/test-results/electron-e2e/`，已人工查看 1366×768 错题/题本及 1920×1080 AI 产物页；题本 checkbox 尺寸问题已修正，并由最后一次完整 smoke 重新生成截图后复核通过。
- 未完成事实：题组三元编辑没有更新 confirmation payload 的现有后端契约，确认仍保存原始生成载荷；UI 已警告，后续不得误报编辑保存完成。真实 DeepSeek/provider 仍未验证。
## 2026-08-12 专题讲义前端闭环

- 新增 `TeachingBookWorkspace.tsx` 并接入“讲义”导航，复用现有 typed preload/SQLite：讲义版本更新、章/页/块创建、来源锚点与健康、整块 patch、精确选区 patch、apply/undo、pending 重生成、安全 Markdown 预览、真实导出和归档审计。
- 修复既有契约断裂：选区 patch 使用 `payload.text`，renderer 现将其作为 text/chapter/section 的兼容正文来源；没有复制第二套编辑模型。
- Electron 生产构建验收 54/54，组件状态 8/8；同一数据目录重启后保留块 v7、教师定稿、pending 状态、归档记录与 artifact hash。双视口截图为 `frontend-teaching-book-1366x768.png`、`frontend-teaching-book-1920x1080.png`。
- Teaching Book 五组专项 smoke 分别 26/26、24/24、24/24、28/28、28/28，合计 130/130。真实 provider、归档恢复、非 manual 来源管理和并发 UI 冲突仍未宣称完成。

## 2026-08-12 Question Notebook 管理前端闭环

- `QuestionNotebookWorkspace` 已从 App props 编排改为独立自管理组件，复用既有 `questionBank:create` 与 `questionNotebook:*` typed preload；新增教师 canonical 录题、分类版本化重命名/软删除/恢复、usage 查看和课堂使用登记。
- 教师录题来源只开放 `local_bank/teacher_resource`，不能伪造 `generated`；操作错误与已有题目列表并存，空题干和 stale category version 均验证零覆盖。
- `npm run test:renderer-components` 9/9、完整 Electron 前端 66/66、Question Notebook 专项 22/22；重启后 UI 题目、恢复分类关联和 usage 仍可回读。真实 provider 自动题目质量与批量导入不在本轮完成声明内。

## 2026-08-12 L1/L2/L3 记忆治理前端闭环

- 新增自管理 `MemoryGovernanceWorkspace.tsx`，复用既有 L2/L3 revision、evidence graph、governance preload；`App.tsx` 已移除重复记忆状态和加载逻辑。
- UI 覆盖 L2/L3 候选零写入、教师采纳、版本修订、停用/恢复、L2 软删除、L2 修订历史、有界证据图与 `memory-governance.v1`。deleted L2 保持只读，因为现有 delete 契约没有专用恢复语义。
- 对抗性 Electron 用另一窗口 preload 更新制造 stale version；UI 显示中文“版本冲突”，拒绝覆盖且保留 SQLite readback。重启后 L2 并发最终值、deleted 状态和 L3 修订/恢复状态仍可见。
- `npm run test:renderer-components` 14/14、完整生产 Electron 87/87、L1 trace 30、L2 summary 36、L3 synthesis 30 均通过；100 L2 + 1 L3 本地查询 20 次采样 p95/max 3ms。1366×768 与 1920×1080 截图已人工检查。真实 provider 与真实教师样本仍未验证。

## 2026-08-12 错题附件选择器前端 E2E 闭环

- `attachments:import` 生产 handler 抽出 `selectAttachmentPaths()`；正常运行继续使用 `dialog.showOpenDialog`，仅未打包且显式 `OMNI_EDU_E2E_DIALOG_MODE=1` 时消费最多 20 组、每组最多 20 个绝对路径的测试队列。未新增 IPC、数据库或业务能力。
- `MistakesWorkspace` 新增导入中、成功、取消、部分失败/失败状态和逐记录 `data-testid`；`App.importAttachment` 现在捕获 dialog/复制异常，避免永久停在 copying。
- `electron-smoke.mjs` 已删除附件 SQL 直插：从按钮选择外部 1×1 PNG，回读源文件仍在、受管目录副本、字节数、真实 SHA-256 和 SQLite；第二次取消验证零写入，随后继续 needs_ocr/校正/脱敏/无 Key 降级与重启链路。
- 当前门禁：build 通过、组件 17/17、生产 Electron 91/91，1366×768 与 1920×1080 截图人工检查通过。该证据不等于 Playwright 操作 Windows 原生 dialog 窗口本身。

## 2026-08-12 “模型没有返回合法 JSON”根因修复

- 确认 parser 之前仍有两个截断点：`python/omni_edu_deeptutor_bridge/bridge.py` 的 AgentLoop 模型预算仅 512 tokens，且 sidecar `result` 再次硬切到 20,000 字符。确定性复现中 27,314 字符合法 JSON 被截成 20,000 字符并解析失败。
- AgentLoop 预算已统一为 8,000 tokens，最终 sidecar result 与主进程一致保留最多 64,000 字符。修复后同一 27,314 字符样本逐字符 round-trip 且可 `JSON.parse`；progressive sidecar 17/17、structured reply、model proxy、production build 均通过。
- 尚未使用真实 DeepSeek Key 复测用户当时的具体 provider 输出。`test:deeptutor-capability-evals` 当前另有既存 `context-09: unbounded tool output` 失败，与本次 Python bridge 修改无调用关系，不能误报全量 48/48。

## 2026-08-12 AI PDF/DOCX 前端闭环

- AI 历史消息的 Markdown、PDF、DOCX 产物现共用既有产物面板与 `exportDocumentArtifact`，没有新增 DB、IPC 或后端能力；空正文会在面板内进入 `failed` 并保持零写入。
- 生产 Electron `npm run test:smoke` 当前 100/100：从用户点击导出，验证 PDF `%PDF-`/Unicode CID bytes、DOCX ZIP/Heading 样式、超过 10,000 字符的 360 条中文正文、真实文件/SHA-256/SQLite，并在重启后回读；空正文同时验证可见错误和 `document_artifacts` 零写入。
- `npm run test:ai-document-export`、`npm run build`、`npm run test:renderer-components` 17/17、`npm run test:ai-observability` 均通过。人工复核截图为 `frontend-ai-artifact-pdf-1366x768.png`、`frontend-ai-artifact-docx-1920x1080.png`、`frontend-ai-artifact-error-1366x768.png`。
- 下一步继续按 typed preload 反向审计 renderer/E2E；复杂分页、嵌入字体、表格、图片、公式和真实 provider 不在本轮完成声明内。

## 2026-08-12 学习路径前端闭环

- 新增 `MasteryPathWorkspace.tsx` 与“学习路径”导航，只复用既有 `mastery:getPath` / `getAiMasteryPath` / `ai_mastery_paths`；支持 loading、empty、error、content、刷新和转交小智规划，不新增后端、IPC 或数据库实体。
- 页面显示教师已确认路径的 v2 版本、append/replace 模式、模块顺序与知识点类型；明确不展示或推导掌握百分比，所有修改继续经过小智草稿和教师确认。
- `npm run test:renderer-components` 22/22、`npm run test:smoke` 109/109、build 均通过。Electron 新增 9 个路径，覆盖学生切换隔离、无路径空态、AI 提示词交接、SQLite 重启回读和 1366×768 / 1920×1080 截图；截图已人工复核无裁切或重叠。
- 下一真实候选缺口是 typed preload `searchAll` 的全局聚合搜索，以及 `listExerciseSets` 的教师回读入口；先审计现有页面是否已有等价能力，避免重复实现。

## 2026-08-12 全局搜索前端闭环

- 反向审计确认原“搜索”页面只复用当前学生目录/时间线，未调用已存在的 `searchAll`。新增 `GlobalSearchWorkspace.tsx`，复用 `SearchResult`、`search:all` 和 `store.search`，未新增后端、IPC、数据库或独立 smoke。
- UI 覆盖 idle/loading/error/empty/results，输入最多 200 字符；学生结果打开学生工作区，记录结果选择所属学生并按标题过滤时间线，避免“只跳页不定位”。
- 最终门禁：组件 27/27、生产 Electron 118/118、build 通过。新增 9 个用户路径覆盖空查询、学生/记录命中、结果导航、无命中、SQLite 重启回读和双视口；两张搜索截图已人工复核无裁切、重叠或不可达操作。
- 下一项继续审计 `listExerciseSets`：当前确认后可保存并底层回读，但还需判断应并入题本还是错题流程，避免重复工作区。

## 2026-08-12 已确认题组教师回读闭环

- 反向审计确认 `exerciseSets:list / listExerciseSets / store.listExerciseSets` 已完整存在，但 renderer 仅在 E2E 中直接回读，教师没有查看入口。新增 `ExerciseSetLibrary.tsx` 并接入 `MistakesWorkspace` 第 5 步，没有新增后端、IPC、数据库或导航工作区。
- UI 区分 no-student/loading/error/empty/content，支持只读刷新；展开后显示原题/相似题/变式题、难度、答案、解析、教师观察和本地题库/教师资源/小智生成来源。切换学生先清空旧 state，避免跨学生短暂泄漏。
- 最终门禁：组件 32/32、生产 Electron 127/127、build 通过。新增 9 个用户路径覆盖确认后教师可见回读、只读边界、来源、刷新零写入、空态、学生隔离、重启 UI readback 和 1366×768 / 1920×1080 截图；两张截图人工复核无裁切、重叠或横向溢出。
- 未完成边界不变：renderer 三元题组编辑没有覆盖 confirmation payload；真实 DeepSeek/provider 质量仍需 live gate。下一轮继续按 typed preload 反向审计，先确认缺口再开发。

## 2026-08-12 复习提醒前端闭环

- 重新索引代码图并定向比对 140 个 preload 方法后，锁定 `getReviewReminder` 的真实前端缺口：原“今日”卡片已有展示，但把 IPC 失败吞成 null/“正在计算”，没有分离状态、只读刷新、主 Electron 双视口或学生隔离证据。
- 新增 `ReviewReminderPanel.tsx`，替换 App 内联逻辑；状态覆盖 no-student/loading/error/clear/upcoming/due，due/upcoming 均展示知识点清单，失败可重试，查看证据复用学生工作区。切换学生先清空旧提醒，明确不显示正文、答案、附件路径且不保存第二份队列。
- 最终门禁：组件 38/38、生产 Electron 137/137、build 通过。主 smoke 新增 10 个用户路径，覆盖 8 天前 mastery_attempt 的真实到期计算、隐私边界、刷新零写入、证据跳转、无记录学生 clear、隔离、重启和 1366×768 / 1920×1080；两张截图人工复核无裁切或重叠。
- 下一真实候选缺口是学生档案生命周期：编辑/归档/导出/打开目录已有 renderer 和 preload，但主 Electron 缺完整用户动作、文件 readback 与重启证据。先审计归档是否有恢复语义，再决定 UI/E2E 补法。

## 2026-08-12 JSON repair 输出预算补强

- 用户再次遇到“模型没有返回合法 JSON”后，复核确认最终 AgentLoop 已是 8,000 tokens，但两条 repair 路径没有统一：DeepTutor Console 只重试一次，repair 请求也没有显式 `max_tokens`，仍可能被 provider 默认输出上限二次截断。
- 新增共享 `requestStructuredReplyRepair()`：固定无工具、`temperature=0`、`response_format=json_object`、`max_tokens=8000`；`runDeepSeekChat` 与 `runDeepTutorConsole` 共用，Console 最多两次并逐次记录 schema errors，最终仍 fail-closed。
- 当前确定性验收：`test:ai-structured-reply`、`test:deeptutor-progressive-sidecar` 17/17、`test:deeptutor-model-proxy`、production build、完整 Electron 137/137 均通过。真实 DeepSeek Key 未注入，因此用户当次 provider 输出尚未 live 复测。

## 2026-08-12 学生档案生命周期前端闭环

- 新增 `StudentProfileLifecycle.tsx`，复用既有 update/archive/export/openFolder typed preload；修正原表单从未挂载、保存后错误切到排序首位、归档无确认、反馈不可见等前端问题。
- 归档学生在学生页可审计，但不再成为其他业务页的当前可写学生；没有 restore IPC，因此 UI 明确只读。
- 组件 49/49、生产 Electron 150/150、build 通过。Electron 新增 13 条用户路径，验证编辑同一 ID、真实 `metadata.json` 与记录、导出取消、归档取消/确认、SQLite/file readback、重启和双视口。截图 SHA-256：1366 为 `40825B93D7A3AD18C07A79BB336B64E9EBB1746FA9D068C99D840D267DCA7908`，1920 为 `884E5C2E479427CC3FAADE62140FD25E233A03B0AEAD0483A4D91EB270580591`。

## 2026-08-12 完整对象 JSON 语法漂移修复

- 用户再次遇到“模型没有返回合法 JSON”。确定性回归证明现有 parser 对完整对象中的尾逗号直接失败；同类字符串裸换行也未覆盖。此前的 8,000-token/64,000-character 与两次 provider repair 不能消除 provider 重复返回 JSON-ish 文本的风险。
- `parseStructuredReply` 现先严格解析，再对不超过 64,000 字符、具有完整 `{...}` 外壳的候选使用锁定版本 `jsonrepair@3.14.0` 做语法修复；随后仍走完整 `xiazhi.reply.v2` 校验。截断样本继续被拒绝。
- 修复前新增用例稳定失败，修复后 `test:ai-structured-reply`、build、harness 125/125 通过；`test:deeptutor-capability-evals` 当前独立失败于既有 `context-09: unbounded tool output`，没有误报 48/48。真实用户当次 provider 输出尚未保存，未做 live 重放。
- `npm audit --omit=dev` 需切换 npmjs 官方端点；审计报告为既有 `dompurify/mermaid/nanoid` 3 项（2 moderate、1 high），新增 `jsonrepair` 不在告警链。本轮未扩大范围升级无关依赖。

## 2026-08-12 完整数据备份前端闭环

- 新增 `DataBackupPanel.tsx` 并替换设置页匿名按钮，复用既有 `exportDataRoot/verifyDataBackup` typed preload；状态覆盖 idle、导出/校验中、导出成功、校验成功、校验失败、取消和错误，显示真实路径、文件数及缺失/修改/清单外问题。
- 主 Electron smoke 从设置页点击导出，直接解析 `omni-edu-backup-manifest.v1.json` 并核对相对路径、size、SHA-256；随后执行清洁校验、修改 `app.db`、加入清单外文件、恢复、取消零写入和第二次应用启动复验。测试 adapter 只在未打包显式 E2E 模式返回目录选择结果，生产仍使用系统 dialog。
- 门禁：组件 56/56、生产 Electron 162/162、备份底层专项 9/9、build 与 `git diff --check` 通过。截图 SHA-256：1366 成功态为 `6A9F7AE00C0AE1476219E1392543B8E5EEEF0419745A2BBC1DA355BB9E13AB27`；1920 篡改失败态为 `7FF69589A3EA1B70496A91C28DBCE21442664DA5B62B1F402A906957F1C3716B`。
- 当前边界：只完成完整备份与完整性校验，没有自动恢复、跨磁盘灾备演练或真实 Windows 原生 dialog 自动化。下一轮继续按 typed preload/handler 反向审计下一个教师可见缺口。

## 2026-08-12 AI 对话库前端与 E2E 闭环

- 反向审计发现 `aiConversations:create/list/get/append/move/rename/archive`、SQLite、preload 与旧 renderer 操作均存在，但主 Electron 仅用直调 fixture 保存文档产物，没有文件夹/会话用户动作证据；关键控件也没有稳定 testid，错误和进行中反馈只依赖全局状态。
- 新增 `AiConversationSidebar.tsx`，从 `App.tsx` 拆出对话库布局与交互状态；App 只负责调用既有 typed preload 并用返回 workspace 更新唯一状态。新增局部 idle/working/success/error、空名称拦截、拖放分类、右键重命名及归档二次确认，未新增后端、IPC、数据库或独立 smoke。
- 门禁：组件 63/63、生产 Electron 176/176、build 与 `git diff --check` 通过。新增 14 条用户路径覆盖空名称零写入、创建、会话/文件夹重命名、拖放移动、归档取消、单会话归档、文件夹级联归档、SQLite/设置页/重启 readback 和双视口。
- 截图 SHA-256：1366 对话库为 `55C7D8F4FD7793176253FF15ADBFB3ECA6A095BC1F27CA42B171E2F77ED8DC6E`；1920 文件夹归档确认为 `77AEB310F3E970C231EC85CDD5BD7C8BFFD63F96B0877BA6796A99FAB4463B0F`。当前归档没有 restore IPC/UI；下一轮继续反向审计，候选包括已有复盘报告入口的状态与 E2E 完整性，但须先核对其“草稿即写库/文件更新”边界，避免只补 UI 掩盖契约问题。
# 2026-08-12：小智 JSON 空响应/截断恢复

- 已在 `deepseek.ts` 增加 `recoverStructuredJsonCompletion`：JSON 模式遇到空 `content` 或 `finish_reason=length` 时受控重试一次，仍为空则明确失败。
- `index.ts` 的 DeepTutor ModelProxy 已接入恢复结果；`XiazhiModelResult.response.recovery` 回传有限诊断元数据。
- `bridge.py` 新增“宿主模型 JSON 状态”公开事件，不保存原始输出，只记录终止原因、content 是否存在与恢复原因。
- `parseStructuredReply` 已区分空 content、截断 JSON、普通非法 JSON；安全边界未放宽。
- 当前验证：`test:ai-structured-reply` 通过；`test:deeptutor-progressive-sidecar` 18/18；`test:deeptutor-model-proxy` 通过；`test:deeptutor-multi-round` 10/10；`test:deeptutor-progressive-tools` 18/18；`test:ai-harness` 125/125；`test:ai-observability` 通过；`npm run build` 通过。
- 未完成：没有使用真实 DeepSeek Key 做 live provider 回放，因此不能声明已覆盖真实供应商偶发空响应率。

## 2026-08-12 本地复盘报告前端与 E2E 闭环

- 反向审计确认后端、主进程、typed preload 和 SQLite 已有 `generateReview/updateReport/listReports`；本轮只新增并接线 `ReviewReportWorkspace.tsx` 的教师可见状态、样式和既有主 Electron 用户路径，没有新增后端、IPC、数据库或独立 smoke。
- 组件覆盖 no-student/idle/generating/draft/saving/saved/error、当前学生历史、来源记录、质量检查和文件边界；日期非法、空正文均零写入，学生切换清空活动报告并隔离历史。
- 真实契约已钉死：生成会立即写入 `review_reports` 并生成初始 Markdown 快照；后续“保存修改”只更新 SQLite 的内容、父摘要与质量结果，不重写初始快照。最终文件必须使用文档导出，UI 已明确提示。
- 门禁：`npm run test:renderer-components` 71/71；`npm run test:smoke` 生产 Electron 前端 191/191；build 通过。新增 15 条路径覆盖日期零写入、生成、SQLite 编辑保存、来源证据、质量检查、父摘要、历史、学生隔离、初始文件、重启和双视口。
- 截图 SHA-256：`frontend-review-report-draft-1366x768.png` 为 `619A0F462930363B5A08A87A3D59C3B0BABB3B8555FC0E21735AB3E5A521E910`；`frontend-review-report-saved-1920x1080.png` 为 `647B94AE16911386C49CF66986FB745C29CCA72683D657CB84D2BA7689C63059`。人工复核无裁切、重叠或不可达操作。
- 下一轮继续从 typed preload/主进程 handler 反向审计下一项教师入口、状态或 E2E 不完整的既有能力；仍不按 API 数量机械新增页面。

## 2026-08-12 小智质量评审前端与 Electron E2E

- 反向审计确认人工可用性评分、replay experiment、model grade 的 SQLite/main/preload 已存在；本轮只拆出并接线 `AiQualityReviewWorkspace.tsx`，未新增后端、IPC、数据库或独立 smoke。
- UI 覆盖 loading/idle/saving/importing/success/error、单条评分、CSV/TSV 文件或文本导入、失败样本回放、before/after 实验与只读 Grader。预检非法行零写入；运行中逐行 IPC 失败会提示按 `sampleId` 检查。
- 组件门禁 79/79，生产 Electron 前端 207/207，build 通过。新增 16 条用户路径，验证 typed preload→main→SQLite、两类零写入、summary、AI 输入回放、关联实验与 delta、合法 CSV、真实 `graderMode`、重启和双视口。
- 首次 E2E 使用旧隐藏 `.ai-prompt` 导致超时；改为当前 HeroUI PromptInput 的 `ai-prompt-input` 后全量通过。截图 SHA-256：1366 为 `46B583B32B4363EDAA4C5653C5AE064CD7FE73BA214E4AB332C14C00B879F5A4`，1920 为 `BCE546CD9997A69C4C5473A692CD936B350AD0807B541C7499BA7B45FAD25254`。
- 下一步继续从 typed preload/main handler 反向审计教师入口或主 Electron 用户路径不完整的既有能力，不按 API 数量新增页面。

## 2026-08-12 AI 控制台故障定位

- 当前 UI 入口 `App.tsx:1224` 调用 `runDeepTutorConsole`，请求在 `main/index.ts:1228` 固定声明 `capability: 'chat'`。
- `host-tool-proxy.ts:17` 的 `chat` 只允许 `general_qa/workspace_help/knowledge_retrieval`；当 router 将“查看小A学习进度”判定为 `student_diagnosis` 时，`resolve_student_reference`、`load_tools` 等宿主工具均被 `capability_route_mismatch` 阻断。
- 实际 Electron 临时数据 smoke 已复现：工具被阻断后模型请求 `ask_user`，主进程将 run 置为 `waiting_input`；`runDeepTutorConsole` 只等待 `status === running`，最终返回 `等待老师输入后继续。`、空 content、schema error `DeepTutor 未返回最终回复。`，另一条复现运行超过 120 秒后进入 `DeepTutor turn exceeded its wall-time budget.`。
- 当前构建通过；`test:deeptutor-agent-tool-call`、`test:deeptutor-model-proxy`、`test:deeptutor-progressive-sidecar`、`test:deeptutor-multi-round`、`test:deeptutor-progressive-tools` 和 `test:ai-structured-reply` 通过，但这些 smoke 未覆盖“控制台 chat capability + 非 chat route”的组合。
- 本轮只完成定位，未修改业务代码。下一步优先修复 capability/route 映射与同步控制台的 `waiting_input` 交接，再做真实 provider 回放。

## 2026-08-12 AI 控制台彻底打通与 Agent Harness v1

- 新增 `agent-harness-profile.ts`，以 `xiazhi.agent-harness.v1` 统一 route→capability、权限校验、运行时 Prompt 和 Console pending 状态；学生诊断/错因走 `deep_solve`，题组走 `deep_question`，备课/报告走 `deep_research`，帮助/普通问答走 `chat`。
- `runDeepTutorConsole` 改为 `executionProfile=omni_console`，所选 capability 保留权限语义但统一进入已接通 HostToolProxy 的 DeepTutor AgentLoop；`waiting_input` 不再提前返回，教师提交答案后同一 IPC 继续收敛。
- HostTool binding 保存 `CompiledAiContext`，`resolve_student_reference` 解析出的 studentId 可被后续工具复用；教育与可用性 grader 进入 Console 成功门禁，失败结算 run 为 failed。
- 真实环境额外发现并修复两处启动阻断：旧 `resource_chunks` 缺少 `subject` 时索引先于列迁移创建；从 `out/main` 启动时仓库根目录计算错误，Python 报 `No module named omni_edu_deeptutor_bridge`。Sidecar 现在提供脱敏 bounded stderr 诊断。
- 真实 DeepSeek 隔离数据回放 2/2 通过：学生进度 route=`student_diagnosis`、教育 100、可用性 95；工作台帮助 route=`workspace_help`、教育 100、可用性 100；两条均 schema valid 且返回非空内容。测试副本已删除，正式数据未写入。
- 确定性门禁已通过：Agent Harness 125/125、Capability eval 48/48、Host Proxy route/state/round-trip、Progressive Sidecar 18/18、Structured Reply、Agent Tool Call、TypeScript 与 production build。
- 主 Electron smoke 首次在既有讲义成功提示等待上偶发超时，直接重跑完整用户路径后 207/207 通过，覆盖 1366×768 与 1920×1080；该次波动未修改讲义业务代码。
- 新增 `docs/27_XIAOZHI_AGENT_HARNESS_ENGINEERING.md`；TRAE 正确入口是 `https://learn.traeai.com/agent-prompts/`，用户给出的 `agent-prompkts` 为拼写错误。

## 2026-08-30 小智 AI 轻量双路径优化

- 参考 Anthropic context engineering/advanced tool use/harness simplification、OpenAI conditional/deferred tools 和 Google 按需 context 的官方方向，将控制台改为 `direct | structured` 双路径。
- `decideExecutionMode()` 只放行寒暄、致谢、能力说明和独立通识问答；学生指代、记录/档案/附件/知识库、文档/题组/报告、写入、风险和歧义继续 structured。新增孩子/代词与“小数”中文边界样例。
- direct 在 `startDeepTutorTurn` 前调用无工具普通文本 DeepSeek 请求，不进入 AgentLoop、上下文、工具、JSON repair 或 grader；structured 旧链未移除。回滚开关为 `OMNI_EDU_AI_EXECUTION_MODE=structured` 或 `OMNI_EDU_DISABLE_DIRECT_AI=1`。
- renderer 对 direct 不保存或显示 thought/context/tools，发送前清空上一轮 DeepTutor events，等待态与完成态不再错误宣称 AgentLoop。SQLite `ai_tasks.result_json` 保存 executionMode 和 schema/grader applicability。
- 门禁：`npx tsc --noEmit` 通过；`test:ai-execution-routing` 13/13；`test:ai-enter-submit` 双 Enter 仅发送一次。Sol 第三轮独立验收通过。未做真实 provider 质量回放或完整 Electron E2E。

## 2026-09-16 最终产品与模块架构对齐

- 新增 `docs/28_FINAL_PRODUCT_MODULE_ARCHITECTURE.md`，作为后续产品和开发的最高优先级方案基线，不再以旧的阶段化扩张方案指导建设。
- 最终定位固定为面向 K-12 独立教师和小微教研团队的“教师个人知识库、题库与智能组卷工作台”，核心闭环为资料入库出卷和错题照片生成针对性练习。
- 文档确定 12 个模块、依赖关系、数据真源、技术选型、模块验收和上市否决项；明确 30–40GB 增量处理、来源谱系、教师确认、恢复演练和干净 Windows 安装是商业化硬门槛。
- OCR 选择确定为双层方案：RapidOCR + ONNX Runtime 处理普通中文文字，PaddleOCR PP-StructureV3 + PP-FormulaNet 处理复杂版面与公式；没有代表性评测集不得声称可商用。
- 本轮只修改方案与协作文档，没有改业务代码，也没有执行产品 build/E2E。

## 2026-09-16 下一次大规模代码修改约束已就绪

- 已将根 `AGENTS.md` 从旧的 Vue/MVP/docs 01–09 约束改为当前 Electron + React + TypeScript 事实，并接入 `docs/28_FINAL_PRODUCT_MODULE_ARCHITECTURE.md` 的 M01–M12 最终方案。
- 下一轮开始写代码前必须先冻结交付合同：模块编号、教师可见结果、当前能力矩阵、表/目录/协议/IPC/UI 变更、旧数据迁移与回滚、验收命令。
- 下一轮不得一次性推倒重写；应按依赖连续完成可独立验收的纵向切片，并优先解决已有 backend 与真实前端/E2E 之间的缺口。
- 架构收敛规则已明确：新业务不继续堆入 `App.tsx`、`db.ts`、`main/index.ts`，但只抽取本轮触及领域，保留 adapter 和旧入口直到新路径验收通过。
- 数据安全规则已明确：旧数据库副本和新库双验证，迁移幂等，原文件/确认题目/正式产物不被索引重建覆盖，任务可取消和恢复。
- 下一轮默认第一动作是只读盘点并输出交付合同，不是立即安装依赖或批量改表；合同确认后再按 M01 下层契约到上层用户能力推进。
- 本轮仍只更新约束与文档，没有修改业务代码，没有运行 build/E2E，也没有 commit/push。

# 4.Wiki.md

### 小智运行观测前端（2026-08-12）

- 入口：左侧“分析”（`ViewKey=analytics`），组件为 `apps/desktop/src/renderer/components/AiObservabilityWorkspace.tsx`。
- 数据源：`getAiTelemetrySnapshot`、`create/listAiRegressionReport`、`listAiAgentRuns`、`getAiMemoryTrace`，均经 typed preload 访问现有 main/SQLite 实现。
- 安全投影：run 列表只显示 route/subIntent/status/time；轨迹只显示 sequence/label/phase/tool/status/time 与 `bounded/rawPromptIncluded/hiddenReasoningIncluded` 边界。
- 本地验收：`npm run test:renderer-components` 当前 7/7；`npm run test:smoke` 当前前端 33/33，包含报告点击生成、SQLite readback、安全哨兵 prompt 不进 DOM、重启回读与 1366×768/1920×1080 截图。
- 状态口径：当 gate 缺少真实教师、live provider 或 grader 证据时，报告可为 warning/failed；这些状态必须原样保留。

## 定义

本文件是 Omni-Edu Agent 项目常识库，记录长期稳定的信息：项目背景、业务口径、架构原则、接口说明、数据安全规则。每轮工作开始前必须阅读；只有长期稳定事实发生变化时才更新。

## 项目背景

Omni-Edu Agent 面向 K-12 独立教师与小微教研团队，帮助老师把本地题库、学生错题、学习记录和 AI 分析组织成可持续复盘的工作流。

前期用户不是大型学校，也不是完整 LMS 管理方，而是独立教师、小微教研团队或小型工作室。产品必须轻量、可演示、可本地运行、数据安全优先。

## 项目协作约束

项目根目录有四份必须维护的协作文档：

- `1.Agent.md`：Agent 工作方式、代码风格、禁止事项。
- `2.Memory.md`：当前进度和下次继续位置。
- `3.Learning.md`：复盘记录和下次改法。
- `4.Wiki.md`：长期稳定的项目常识库。

每轮工作开始前必须阅读这四份文件；每轮工作结束后必须根据真实结果更新它们。

根目录 `README.md` 是项目对外入口文档，长期用于展示产品定位、UI 页面、已落地能力、快速启动和验证命令。README 必须与当前代码能力和 `2.Memory.md` 真实进度一致，不得夸大规划能力。

## 核心业务口径

- 本地优先：题库、学生档案、错题历史、附件默认保存在用户本机。
- 脱敏上云：云端模型只能接收脱敏后的题目文本和必要上下文。
- 教师可修正：OCR、模型输出、题目解析、复盘报告必须给教师保留修正入口。
- 强约束输出：DeepSeek 或其他模型返回应走 JSON Schema 或等价校验。
- 先完成教师真实闭环再扩展：后端、IPC、前端入口、失败状态、数据回读、重启和 Electron E2E 必须同步完成，不以演示页或低层 smoke 代替可用性。

## Book Workspace 适配口径

- Omni-Edu 的 DeepTutor Book Workspace 对外产品名为“专题讲义/单元备课包”，不是学校级 LMS；book/chapter/page/block/source 是本地 SQLite 五层实体。
- 讲义内容块支持 `text/chapter/quiz/card/figure/concept_graph/prompt` 等受控类型，C04 先完成结构与健康检查，渲染/导出在 C05 扩展。
- `inspect_teaching_book` 只读、local-only、bounded；sourceRefs 用于可追溯证据，来源删除或指纹变化必须可回读为 missing/stale。

## 最终双业务闭环

```text
教师资料 -> 解析与索引 -> 题库/知识库 -> 按蓝图组卷 -> 教师确认 -> 试卷与解析
学生错题照片 -> 本地 OCR -> 教师校正 -> 错因/知识点 -> 相似或变式题 -> 针对性练习
```

错题训练中的三元题组目标：

- 原题或错题：定位学生当前问题。
- 相似题：训练同一知识点或同类错误。
- 变式题：检查迁移能力和真正掌握情况。

## 当前桌面端架构

- 应用目录：`apps/desktop`
- 主进程：`apps/desktop/src/main`
- preload：`apps/desktop/src/preload`
- renderer：`apps/desktop/src/renderer`
- 共享契约：`apps/desktop/src/shared/contracts.ts`
- 桌面端构建：Electron + Vite + TypeScript + React。

## 本地数据架构

当前数据库：

- 使用原生 SQLite：`sqlite3`。
- 数据库文件位于本地数据目录：`app.db`。
- 启用 WAL，支持更好的本地读写表现。
- `sql.js` 已不再作为主数据库实现。

本地文件：

- 学生目录位于数据目录下的 `students/{studentId}`。
- 附件应复制进入学生记录目录，不直接引用外部原始路径。
- 任何从 renderer 传入的文件路径都不可信，主进程必须校验是否在本地数据目录内。

## Electron 安全口径

- `contextIsolation: true`
- `nodeIntegration: false`
- `sandbox: true`
- renderer 不直接访问 Node API。
- 所有 IPC 能力必须通过 preload 暴露。
- preload 当前以 CommonJS 构建，输出为 `index.cjs`。
- 外部链接只允许 `http:` 和 `https:`，其他协议默认拒绝。

## DeepSeek / AI 口径

- API Key 不能硬编码。
- 本地保存配置时，UI 只能展示 masked key。
- AI 上云必须默认最小上下文原则。
- 附件原始文件不上传；当前 AI 控制台只统计附件数量和元数据。
- 老师知识库、Docling / MinerU、知识图谱目前属于规划或待接入能力，不得在 AI 输出中编造为已接入。
- AI 控制台可以展示“思考过程摘要”，内容限于用户可验证的外部处理过程：任务识别、上下文读取、工具调用、证据边界、知识来源、产物判断。
- 思考过程摘要、上下文来源和工具调用必须按需显示；问候类或无本地上下文需求的输入不得展示学生档案、学习记录、知识库或图谱工具。
- 不展示模型内部隐式推理，不用“思考过程”编造真实 AI 未返回的结论。

## HeroUI Pro 本地源码口径

HeroUI Pro 组件需求遵循以下长期规则：

- 官网或 MCP 用于识别官方组件、区块或模板名称。
- 如果会员受限、没有 Personal Access Token、MCP 不可用或资源不可获取，直接使用本地源码。
- 本地源码根目录：`D:\WorkProject\HeroUIPro\herouipro-v3\src`
- 常用组件目录：`D:\WorkProject\HeroUIPro\herouipro-v3\src\components`
- CSS 和主题目录：`D:\WorkProject\HeroUIPro\herouipro-v3\src\css`
- 工具目录：`D:\WorkProject\HeroUIPro\herouipro-v3\src\utils`

当前 AI 控制台已使用的本地 HeroUI Pro 组件包括：

- `chain-of-thought`
- `chat-attachment`
- `chat-conversation`
- `chat-list-view`
- `chat-message`
- `list-view`
- `markdown`
- `prompt-input`
- `prompt-suggestion`

## AI 控制台当前交互口径

- 页面主空间优先给对话区。
- 左侧对话栏使用本地 HeroUI Pro `ChatListView`，参考 Codex / ChatGPT 的会话库设计。
- AI 对话数据持久化在本地 SQLite：文件夹、会话和消息分别存入 `ai_conversation_folders`、`ai_conversation_sessions`、`ai_conversation_messages`。
- 对话支持未归档、文件夹分组、新建文件夹、新建对话、打开历史对话、拖拽归类、右键重命名和右键归档。
- AI 文件夹和会话使用 `archived_at` 表示归档；归档内容从中控台左侧常规列表隐藏，并在设置页“已归档 AI 对话”栏目展示。
- 左侧任务上下文和输入框下方上下文已合并为输入框下方 5 个横向模块：学生档案、学习记录、附件元数据、老师知识库、知识图谱。
- 思考过程显示在 AI 回复内部，默认展开为“思考过程摘要”；内容来自主进程 harness trace。
- 右侧不再常驻思考链路；右侧只在点击 PDF、Word、Markdown 等产物时展开。
- PDF、Word、Markdown 产物入口在 AI 回复中显示为 `PDF 文件`、`Word 文件`、`Markdown 文件` 等文字链接；点击文字后右侧展开对应预览。
- 右侧产物预览面板与中间对话区之间有可拖拽分界线，默认宽度约 420px，可在 320px 到 720px 范围内调整。
- Markdown / artifact 预览中的 GFM 任务列表复选框固定为紧凑控件，不继承全局输入框样式；这是防止生成文档出现异常大空框的长期 UI 规则。
- 产物入口和思考摘要会保存到消息 metadata，历史对话重新打开后仍可查看。
- AI 回复底部不再渲染“上下文来源 / 上下文工具”折叠组件；真实上下文和工具状态统一压缩进 Agent loop 摘要。
- 附件选择入口使用 `ChatAttachmentInput` 真实组件。
- 涉及对话持久化、文件夹归类、附件和本地文件的验收必须使用 Electron 窗口；普通浏览器 dev URL 没有 preload，不能验证 `window.omniEdu` IPC。

## 小智 AI Harness 长期口径

- 小智是面向老师的教学决策与内容协作助手，不是学生端万能聊天机器人。
- 小智先识别任务路由，再按路由读取最小必要上下文；不能默认把当前学生的全部近期数据塞进每个问题。
- 稳定 system 规则、route 专属规则、动态上下文、工具 schema 和输出 schema 分层维护。
- 学生相关结论区分事实、推断和未知，重要判断关联真实来源；不自动形成永久学生标签。
- 工具注册、工具可用和工具已调用是三种状态；UI 只展示真实执行轨迹。
- 读取、草稿、写入使用不同权限等级；写入学生档案或报告前必须由老师确认。
- 模型回复使用版本化结构契约并在本地校验；产物状态由真实文件生成结果决定，不由关键词推测。
- 教育脚手架只在辅导、讲解和掌握检查等 route 中启用；教师知识问答和工作台操作应直接高效回答。
- 小智只展示可验证的 route、上下文、工具和边界摘要，不展示模型隐式推理或 `reasoning_content`。
- P0 harness 已接入确定性 router、subIntent、按需上下文、只读工具注册表、Agent loop trace、DeepSeek JSON Output、本地 `xiazhi.reply.v2` schema 校验和 100 条 router/subIntent/tool eval。
- 当前 8 类 route 为：`general_qa`、`student_diagnosis`、`error_analysis`、`practice_design`、`lesson_design`、`report_draft`、`knowledge_retrieval`、`workspace_help`。
- 当前工具执行器覆盖真实只读上下文读取：学生引用解析、学生档案、学习记录、附件元数据、老师知识库、知识图谱、本地题库相似题；写入动作仍必须走确认队列，不得声称已自动保存。
- 小智遇到“小A/某学生”等明确学生引用时，应先通过 `resolve_student_reference` 查本地学生列表；只有未命中时才要求老师补充或选择学生。
- 当前 Agent loop 事件顺序为：`route -> plan -> tool_call -> observe -> reflect -> finalize`；后续接入 LLM tool_calls 时仍应保留 guardrail、observe 和终止条件。
- `npm run test:ai-harness` 是小智 route / subIntent / 工具白名单变更的本地门禁，当前应保持 104/104 通过。
- 目标架构和后续实施路线以 `docs/22_XIAOZHI_EDU_AI_HARNESS_PLAN.md` 为准；该文档是小智教育 Agent 完整进化总纲。
- Education Grader v1、三元题组闭环 v1 第一段、错题图片脱敏链路 v1 第一段、知识库/图谱增强 v1 第一段、真实文档导出 v1、Observability / Regression v1 数据层已经接入；真实 OCR、模型错因分析、Graph RAG、复杂 PDF/DOCX 排版和除复盘报告/题组草稿外的 AI 写入动作不得宣称已完成。
- 后续小智开发顺序优先为：Observability/Regression 报告 UI 与失败样本回放 -> 教师可用性打磨；Phase 8/9/10 可并行补 UI、真实 OCR、更强解析器和文档排版增强。

## 数据规模目标

短期：面向中小团队，保证本地数据可靠、可备份、可恢复。

中长期：适配百人商业团队和上百 GB 甚至更多数据。底层方向包括：

- SQLite 查询分页和索引治理。
- 附件目录对象化管理。
- 后台任务队列和增量索引。
- 数据归档与压缩。
- 备份恢复与完整性校验。
- 本地加密配置和敏感字段保护。
- 可选局域网或团队同步，但不得破坏本地优先原则。

## 不做范围

- 不做学校级平台。
- 不做学生端、家长端、校园管理系统。
- 不做完整 LMS。
- 不做复杂多租户和复杂权限体系。
- 不做模型微调和私有模型部署。
- MVP 阶段不做 Graph RAG / Corrective RAG 等高级 RAG。

## 常用验证命令

桌面端：

```powershell
cd D:\WorkProject\EduProject\apps\desktop
npm run build
npm run test:smoke
npm audit --omit=dev --registry=https://registry.npmjs.org
```

仓库检查：

```powershell
cd D:\WorkProject\EduProject
git diff --check
rg "sql\.js|SqlJs|sql-wasm|@types/sql\.js|better-sqlite3" apps\desktop -n
```

## 小智 Agent Runtime v1 当前口径

- 每次 `ai:runDeepSeek` 请求都必须创建 `ai_agent_runs` 记录，核心字段包括 `session_id`、`prompt`、`route`、`sub_intent`、`status`、`model`、`student_id`、`error_message`、`created_at`、`completed_at`、`updated_at`。
- 小智可展示的外部过程由 `ai_agent_events` 持久化，事件序列按 `sequence` 升序，当前阶段包括 `route`、`plan`、`tool_call`、`observe`、`reflect`、`finalize`、`guardrail`。
- `AiAgentTraceStep` 可以携带 `inputSummary` 与 `outputSummary`，用于展示可验证摘要；不得把模型隐藏推理、chain-of-thought 或未经验证的推断写入这些字段。
- 空输入、缺少 DeepSeek API Key、模型调用失败、schema 校验失败等失败路径也必须完成 run 终态，并保留已发生事件。
- 当前 `npm run test:ai-agent-runtime` 是 Agent Runtime 持久化门禁；它不调用外部模型，只验证本地 run/event 落库、事件序列、工具调用事件和摘要字段。

## 小智 Tool Calling v1 当前口径

- 小智工具层的单一事实来源是 `apps/desktop/src/main/ai-harness/tool-registry.ts`。
- 每个工具必须声明：`name`、`label`、`description`、`contextKey`、`effect`、`privacy`、`allowedRoutes`、`parameters`、`maxOutputChars`。
- 模型能看到的工具必须通过 `getModelToolDefinitions(router)` 按 route 导出；不允许把所有工具无差别暴露给模型。
- 模型提出工具调用后，必须先走 `reviewModelToolCall()`，审核工具存在性、route allowlist、contextPolicy、effect/privacy 和参数 schema。
- Tool Calling v1 只允许只读工具；任何 `write` effect 工具必须等确认队列接入后才能执行。
- 工具执行统一走 `executeAiToolCall()`；审核失败时记录 `blocked`，不能执行真实读写。
- 发给模型的工具结果必须 bounded；超出工具 `maxOutputChars` 时标记 `truncated=true` 并只给 preview。
- 当前本地门禁是 `npm run test:ai-tool-calling`，覆盖 schema 导出、允许工具成功、越权 blocked、坏参数 blocked 和长输出截断。

## 小智 Confirmation Queue v1 当前口径

- AI 发起的本地写入先落 `ai_confirmation_items`，字段包含 run、session、student、action、status、preview、payload、result、error 和确认/拒绝时间。
- Confirmation Queue 当前开放 `create_review_report` 和 `save_exercise_set`，分别用于把 AI 复盘草稿保存到 `review_reports`、把 AI 三元题组草稿保存到 `exercise_sets`。
- `pending` 确认项不会写入业务表；`rejected` 不产生业务副作用；`confirmed` 必须在业务写入成功且 readback 到真实 `ReviewReport`、`ExerciseSet` 或对应业务记录后才能设置。
- `ai:runDeepSeek` 遇到结构化 `report_draft` artifact 且有当前学生时，会创建待确认项，并把 agent run 状态置为 `waiting_confirmation`。
- AI 页面会显示“待老师确认”队列，老师可确认保存或拒绝。
- 当前本地门禁是 `npm run test:ai-confirmation` 和 `npm run test:ai-exercise-set`，覆盖创建不写库、拒绝不写库、确认后写入且读回、重复确认失败。

## 小智 SubIntent Router + Structured Reply v2 当前口径

- `AiRouterDecision` 当前包含 `route`、`subIntent`、`slots`、`audience`、`actionLevel`、`riskLevel`、`clarificationQuestion`、`allowedTools` 和 `contextPolicy`。
- `slots` 当前抽取学生引用、多学生歧义、时间范围、学科、知识点和写入意图；明确学生引用如“小A”不要求老师手动切模块。
- `npm run test:ai-harness` 当前包含 100 条 eval，覆盖 8 类 route、28 类 subIntent、多学生歧义、时间范围、学科、知识点、写入意图、澄清策略、备课本和题目收藏工具边界。
- DeepSeek 最终结构化回复必须返回 `xiazhi.reply.v2`，字段包含 `facts`、`evidence`、`inferences`、`unknowns`、`risks`、`teacherConfirmations`、`artifacts`、`routeCheck` 和 `processSummary`。
- v2 本地 schema 校验会执行 route-specific 规则：学生诊断必须有事实或未知，学生推断必须有事实支撑，三元题组必须包含原题/相似题/变式题结构，写入型报告必须要求老师确认。
- v2 校验失败时最多允许两次受控 JSON repair；本地 parser 会安全去除代码围栏/有限外壳文本并重新走同一 schema 校验，但不会填造缺失字段。repair 固定不暴露工具、`temperature=0`、`response_format=json_object`、`max_tokens=8000`，仍失败则作为结构化回复失败返回。
- 当前本地门禁是 `npm run test:ai-structured-reply`，覆盖合法 v2、代码围栏/外壳 JSON 提取、截断 JSON fail-closed、route mismatch、缺 facts/unknowns、坏三元题组和写入报告缺确认提示。

## 小智 Education Grader v1 当前口径

- `xiazhi.reply.v2` schema 通过后仍必须执行 Education Grader。
- 当前 Education Grader 是确定性规则 grader，覆盖空泛上传 fallback、学生证据缺口、标签化语言、高风险缺处置、敏感号码泄露、无事实支撑推断和写入缺确认。
- Grader 未通过时，小智回复不得作为成功答案展示；harness 中记录 `educationGrade`、分数和 issue。
- 当前本地门禁是 `npm run test:ai-education-grader`。
- Phase 12 v1.5 已接入模型 grader deterministic proxy、`ai_model_grades` 和年级适切评分维度；真实外部 LLM-as-judge 仍未接入，不能宣称已经完成完整教育质量评测体系。

## 小智三元题组闭环 v1 当前口径

- 本地题库结构化表为 `question_bank_items`，字段覆盖学科、年级、知识点、题型、难度、题干、答案、解析、来源和标签。
- 本地题组保存表为 `exercise_sets`，字段覆盖学生、标题、学科、知识点、Markdown 正文、结构化题目 items 和来源题目 ID。
- `practice_design` route 当前包含 `question_bank` 上下文，工具白名单包含 `search_similar_questions`。
- `search_similar_questions` 是只读工具，会从本地题库召回相似题，并保留 `sourceKind`；本地题库题不得和模型生成题混淆。
- AI 生成 `exercise_set` artifact 时，只能创建 `save_exercise_set` 确认项；老师确认前不得写入 `exercise_sets`。
- 老师确认后必须 readback 真实 `ExerciseSet`，确认项才能变为 `confirmed`。
- 当前本地门禁是 `npm run test:ai-exercise-set`，覆盖本地题库召回、工具 used、确认前不写库、确认后题组 +1、重复确认失败。
- Phase 7 当前只完成“题库检索 + 题组确认保存”的第一段；完整题库导入 UI、embedding 召回和题组编辑器尚未完成。

## 小智错题图片与脱敏链路 v1 当前口径

- 错题图片原始文件默认只保存在本地学生数据目录，不能自动上传给云端模型。
- `mistake_image_analyses` 是当前错题图片分析状态表，记录图片附件、OCR/解析状态、原始解析文本、脱敏文本、脱敏命中项和老师修正文本。
- 当前 OCR 状态包括 `needs_ocr`、`sanitized`、`teacher_corrected`、`failed`。
- 无解析文本时必须进入 `needs_ocr`，不得展示为已完成分析。
- 有解析文本时必须先生成脱敏文本；云端模型后续只能接收脱敏文本和必要上下文。
- 老师修正 OCR/解析文本后必须重新生成脱敏文本，并保留新的脱敏记录。
- 当前脱敏规则覆盖手机号、邮箱、身份证号样式文本和学生姓名/显示名；它是规则版 v1，不等于完整 PII 识别体系。
- 当前本地门禁是 `npm run test:ai-mistake-image`，覆盖图片本地导入、`needs_ocr` 状态、脱敏、老师修正和原始图片不上云边界。

## 小智知识库/图谱增强 v1 当前口径

- 本地可解析文本资源导入后当前直接进入 `ready`，表示已经完成本地复制、文本切片、基础索引和轻量图谱抽取。
- PDF/DOCX/PPT/图片等复杂资源仍可能进入 `needs_parser`，等待后续 Docling/MinerU/OCR 接入。
- `resource_chunks` 当前保存学科、年级、知识点、题型、难度、sourceTrust、containsPersonalData、qualityScore、evidenceStrength。
- `knowledge_nodes` 保存 evidenceStrength；`knowledge_edges` 保存 evidenceStrength 和 evidenceKind。
- 知识库检索支持多 token 命中，优先返回非个人信息、高质量 chunk。
- 含个人信息的知识库 chunk 不会把正文预览发送给模型，只返回元数据和“正文已隐藏”提示。
- 图谱节点和边只作为关系背景；除非同时有 chunk/sourceId 支撑，否则不能当作正文直接证据。
- 当前本地门禁是 `npm run test:ai-knowledge-graph`，覆盖资源 ready、chunk metadata、个人信息隐藏、图谱 evidenceKind 和背景边界。

## 小智文档产物导出 v1 当前口径

- 小智文档产物表为 `document_artifacts`，字段覆盖 artifact/session/message、标题、产物类型、文件名、mime、description、Markdown 正文、真实文件路径、文件大小、sha256、状态、错误信息和时间戳。
- 当前文档产物类型为 `markdown`、`pdf`、`docx`；`report_draft`、`exercise_set` 等结构化产物在右侧预览中可按 Markdown 导出，但业务保存仍必须走确认队列。
- `exportDocumentArtifact()` 在主进程 Store 内执行真实文件写出、hash 计算和 SQLite readback；没有真实文件和 readback 不得显示为 `exported`。
- renderer 不能直接写文件或信任本地路径；导出和打开文件分别通过 `documents:exportArtifact`、`documents:showArtifact` IPC，由主进程处理。
- PDF v1 是最小真实 PDF 文件生成；DOCX v1 是最小 Office Open XML zip 包；二者已满足真实文件闭环，但复杂排版、中文字体嵌入、表格、图片和公式仍是后续增强。
- 当前本地门禁是 `npm run test:ai-document-export`，覆盖 Markdown/PDF/DOCX 真实文件、sha256/readback、session 列表和空正文失败路径。

## 小智 Observability / Regression v1 当前口径

- 小智可观测性快照由 `buildAiTelemetrySnapshot()` 从真实 SQLite 表汇总，覆盖 run 状态、route/model 分布、事件阶段、工具调用、文档产物状态、确认队列状态、延迟、token 和 context 预算。
- 小智回归报告写入 `ai_regression_reports`，字段覆盖标题、状态、摘要、snapshot、gates、完整 reportJson 和创建时间。
- 当前 gate 覆盖 Agent run 终态、事件轨迹、工具轨迹、文档导出状态、确认队列状态、延迟预算和可选 router eval 基线。
- 主进程 IPC 命名空间为 `aiObservability:*`，preload 暴露 `getAiTelemetrySnapshot`、`createAiRegressionReport`、`listAiRegressionReports`、`getAiRegressionReport`。
- token/context 指标只统计上游已写入的 usage 和 payload 摘要；缺失时必须显示未知或 warning，不能编造。
- 当前本地门禁是 `npm run test:ai-observability`，覆盖真实 run/event/tool/artifact/confirmation/report readback 和 gate passed。
- Phase 11 当前没有专门的报告 UI、失败样本回放和发布前一键门禁页面；这些属于后续工作。

## 小智 Observability + Regression v1 当前口径

- 小智回归报告表为 `ai_regression_reports`，字段覆盖 id、title、status、summary、snapshot_json、gates_json、report_json 和 created_at。
- `buildAiTelemetrySnapshot()` 从本地 SQLite 聚合 `ai_agent_runs`、`ai_agent_events`、`ai_tool_runs`、`document_artifacts`、`ai_confirmation_items` 和 `ai_tasks`，生成运行状态、route/model 分布、事件阶段、工具调用、产物状态、确认队列、P50/P95/avg 延迟、token budget 和 context budget。
- `createAiRegressionReport()` 当前 gate 包括 `agent_runs_terminal`、`event_trace_present`、`tool_trace_present`、`artifact_export_status`、`confirmation_queue_state`、`latency_budget_available` 和可选 `router_eval_baseline`。
- gate 聚合规则为：任一 `failed` 则报告 `failed`；否则任一 `warning` 则报告 `warning`；全部通过才是 `passed`。
- 回归报告 `reportJson` 当前声明 `schemaVersion: xiazhi.observability.v1` 和 `replySchemaVersion: xiazhi.reply.v2`，用于后续比较 prompt/schema/model 版本。
- 主进程 / preload 当前暴露 snapshot、创建报告、列表和按 id 读取报告能力；UI 仪表盘和报告文件导出仍是后续增强。
- 当前本地门禁是 `npm run test:ai-observability`，覆盖真实 seed run/event/tool/artifact/confirmation/task usage、报告生成、gate 全通过、SQLite readback 和列表读取。

## 小智教师可用性 v1 当前口径

- 小智可用性策略集中在 `apps/desktop/src/main/ai-harness/usability-policy.ts`，包含 route-aware profile、DeepSeek 可用性指令、确定性 Usability Grader 和教师可读 Markdown formatter。
- 当前 profile 包括 `brief_answer`、`workspace_steps`、`evidence_snapshot`、`error_action_plan`、`teaching_artifact`、`lesson_brief`、`draft_with_confirmation`、`source_brief`。
- `runDeepSeekChat()` 的成功链路为：`xiazhi.reply.v2` schema 通过 -> Education Grader 通过 -> Usability Grader 通过 -> 教师可读 Markdown 展示；任一 critical 失败都不得作为成功回复展示。
- “当前为普通问答模式”“无法自动切换”“请手动选择左侧导航”“请手动切换学生数据”“当前路由不支持自动”等退化话术属于 `manual_module_switch_fallback` critical。
- 教师可读 formatter 不再机械展示所有 schema 字段；普通问答/工作台帮助优先短答，学生诊断/错因/题组/报告按需展示依据、教学判断、下一步、老师确认和产物入口。
- AI 页快捷提示当前优先老师真实任务：学生错题与学习进度、三元题组草稿、家长沟通草稿、档案缺口、课堂导入和确认队列处理。
- Confirmation Queue UI 默认展示最近 2 个待确认项，剩余项折叠提示；确认前仍不得写入业务表。
- 当前代表性 proxy 样本集在 `apps/desktop/src/main/ai-harness/usability-eval-cases.ts`，共 25 条，覆盖 8 类 route、好样例、手动切模块退化、过长泛答、写入缺确认、模板流水线、下一步泛化和风险噪声。
- 当前本地门禁是 `npm run test:ai-usability`，覆盖 proxy 样本集、好样例通过、退化手动切模块失败、过长泛答、写入缺确认和自然产物入口。
- `recordAiConsoleRun()` 会把 `harness.usabilityGrade` 摘要写入 `ai_tasks.result_json`；`buildAiTelemetrySnapshot()` 汇总 `usability.sampleCount/passedCount/failedCount/averageScore/minScore/profileCounts/issueCounts`。
- `createAiRegressionReport()` 当前包含 `usability_quality_gate` 和可选 `usability_eval_baseline`；前者看真实 AI console task 的 usabilityGrade，后者看 proxy 样本集是否全通过。
- 真实教师人工评分 rubric 在 `docs/23_XIAOZHI_USABILITY_EVAL_RUBRIC.md`；必须记录 prompt、route、teacherScore、needsRewrite、roundsToUseful、mainIssueCode 和老师备注。
- Phase 12 v1/v1.1 仍是 deterministic grader、proxy 样本和回归整合，不等于已经完成教师人工评分平均 >= 4/5、返工量下降或真实任务 1-2 轮完成率验证。

## 小智教师人工评分 v1.2 当前口径

- 教师人工评分持久化表为 `ai_usability_reviews`，字段覆盖 sample/run/session、prompt、route、subIntent、model、teacherScore、needsRewrite、roundsToUseful、mainIssueCode、teacherNote、reviewedAt 和 createdAt。
- `OmniEduStore.createAiUsabilityReview()` 是当前唯一人工评分写入口；会校验 teacherScore 必须为 1-5，roundsToUseful 必须为 1-12。
- `buildAiUsabilityReviewSummary()` 从真实 SQLite rows 汇总 sampleCount、averageTeacherScore、minTeacherScore、passedCount、needsRewriteCount、averageRoundsToUseful、routeCounts、issueCounts 和 latestReviewedAt。
- `AiTelemetrySnapshot.humanUsability` 挂载人工评分汇总；`createAiRegressionReport()` 包含 `teacher_review_score_gate`。
- `teacher_review_score_gate` 默认：无人工样本为 warning；调用方要求最小样本数但不足为 failed；有样本但平均教师分低于阈值、平均有用轮次超过阈值或存在 needsRewrite 时为 failed。
- 主进程 / preload 暴露 `createAiUsabilityReview`、`listAiUsabilityReviews`、`getAiUsabilityReviewSummary`，后续 UI 和 CSV 导入必须复用这些 API。
- 当前本地门禁是 `npm run test:ai-human-review`，使用 seeded 样本验证写入、列表、summary、snapshot 和 gate readback；seeded 样本不等于外部真实老师验收。
- DeepSeek live 可用性回放命令是 `npm run test:ai-live-usability`；没有 `DEEPSEEK_API_KEY` 时只能 skipped，不能宣称 live API 已验证。

## 小智人工评分 UI v1.3 当前口径

- 设置页“小智质量评审”面板是当前人工评分操作入口，展示 `humanUsability` 样本数、平均教师评分、需重写数量和平均有用轮次。
- 单条评分表单与 CSV / TSV 导入都复用 preload `createAiUsabilityReview()`；renderer 只负责收集输入和解析文件，不直接写 SQLite。
- CSV / TSV 表头至少包含 `sampleId`、`prompt`、`route`、`subIntent`、`teacherScore`、`needsRewrite`、`roundsToUseful`、`mainIssueCode`；兼容部分 snake_case 字段。
- 失败样本回放列表包含 `needsRewrite=true`、`teacherScore <= 3` 或 `mainIssueCode != none` 的 review，可一键把 prompt 放回 AI 输入框重新运行。
- 当前 UI smoke 是 `npm run test:ai-human-review-ui`，通过真实 Electron 设置页验证表单保存、CSV 导入、失败样本回放和 SQLite summary readback。
- 失败样本回放 v1.3 是重新运行入口；v1.4 起支持把失败样本设为 before，再保存 after 人工评分形成 before/after replay experiment。

## 小智 before/after 回放实验 v1.4 当前口径

- 回放实验持久化表为 `ai_usability_replay_experiments`，字段覆盖 beforeReviewId、afterReviewId、replayPrompt、modelBefore、modelAfter、promptVersionBefore、promptVersionAfter、experimentNote 和 createdAt。
- 实验表只保存绑定和版本信息；scoreBefore、scoreAfter、scoreDelta、roundsDelta、issueBefore、issueAfter 和 improved 通过 join 两条 `ai_usability_reviews` 实时回读计算，避免老师修正评分后 delta 过期。
- `buildAiUsabilityReplaySummary()` 汇总 experimentCount、improvedCount、unresolvedCount、improvementRate、averageScoreDelta、averageRoundsDelta、issueTransitionCounts 和 latestCreatedAt。
- `AiTelemetrySnapshot.usabilityReplay` 挂载 replay 汇总；`createAiRegressionReport()` 包含 `usability_replay_improvement_gate`。
- `usability_replay_improvement_gate` 默认：无 replay 实验为 warning；调用方要求最小实验数但不足为 failed；改善率低于阈值为 failed；有实验且达标为 passed。
- 主进程 / preload 暴露 `createAiUsabilityReplayExperiment`、`listAiUsabilityReplayExperiments`、`getAiUsabilityReplaySummary`。
- 设置页“小智质量评审”可在失败样本中点击“设为 before”，保存下一条 after 人工评分时自动创建 replay experiment，并展示 replay KPI 与实验列表。
- 当前本地门禁是 `npm run test:ai-replay`、`npm run test:ai-human-review-ui` 和 `npm run test:ai-observability`。
- v1.4 replay smoke 仍是 seeded 工程验收，不等于外部真实老师样本验收；v1.6 已完成 seeded/live-ready evidence binding，真实返工量下降、真实 DeepSeek API 输出和真实 LLM-as-judge 仍是后续工作。

## 小智模型 grader proxy v1.5 当前口径

- 模型 grader 样本持久化表为 `ai_model_grades`，字段覆盖 sample、prompt、answerMarkdown、route、subIntent、targetGrade、modelUnderReview、graderModel、graderMode、runId、sessionId、promptVersion、totalTokens、六维分数、overallScore、passed、issueCodes、graderRationale、reviewedAt 和 createdAt。
- `graderMode` 当前支持 `deterministic_proxy` 与未来 `llm_judge`；现阶段完成的是 deterministic proxy，不等于真实外部模型裁判。
- 当前六个评分维度为 evidenceScore、actionabilityScore、safetyScore、gradeAppropriatenessScore、concisionScore、teacherControlScore，均按 1-5 分保存。
- `OmniEduStore.createAiModelGrade()` 是当前模型裁判样本写入口；`listAiModelGrades()` 和 `buildAiModelGradeSummary()` 从真实 SQLite rows 回读列表和 summary。
- `AiTelemetrySnapshot.modelGrader` 汇总 sampleCount、passedCount、failedCount、averageOverallScore、averageGradeAppropriatenessScore、runLinkedCount、tokenKnownCount、promptVersionCounts、routeCounts、issueCounts 和 latestReviewedAt。
- `createAiRegressionReport()` 包含 `model_grader_quality_gate`；无模型裁判样本默认 warning，调用方要求最小样本数但不足为 failed，平均整体分或年级适切分低于阈值为 failed，有样本且达标为 passed。
- 主进程 / preload 暴露 `createAiModelGrade`、`listAiModelGrades`、`getAiModelGradeSummary`。
- 设置页“小智质量评审”展示模型 grader KPI 与样本列表；它是评审 readback 入口，不是单独的临时 UI 状态。
- 当前本地门禁是 `npm run test:ai-model-grader`、`npm run test:ai-live-evidence` 和 `npm run test:ai-observability`；设置页面板变更还必须跑 `npm run test:ai-human-review-ui`。
- v1.5 seeded model grade 只证明工程链路、proxy 规则和 gate readback 正常；v1.6 已补 seeded/live-ready run 证据绑定，但外部真实老师样本、真实 DeepSeek API 输出、真实 LLM-as-judge、评分趋势图和真实返工量下降仍未完成。

## 小智 live evidence binding v1.6 当前口径

- live evidence binding 指同一次小智输出尽量使用同一条 `runId` 绑定 after 人工评分、before/after replay、模型 grader、telemetry 和 regression gate。
- replay readback 暴露 `beforeRunId`、`afterRunId`，`buildAiUsabilityReplaySummary()` 汇总 `liveLinkedCount`。
- model grader readback 暴露 `runId`、`sessionId`、`promptVersion`、`totalTokens`，`buildAiModelGradeSummary()` 汇总 `runLinkedCount`、`tokenKnownCount` 和 `promptVersionCounts`。
- `recordAiConsoleRun()` 会把 `harness.agentRunId` 写入 `ai_tasks.result_json`，用于从任务记录反查 agent run。
- `createAiRegressionReport()` 包含 `live_evidence_binding_gate`；无显式阈值且没有绑定证据时可 warning，传入最小 live-linked replay 或 run-linked model grade 数量不足时 failed。
- 当前本地门禁是 `npm run test:ai-live-evidence` 和 `npm run test:ai-observability`，前者 seed 同一条 runId 并验证 review/replay/model-grade/gate 证据链。
- v1.6 是 seeded/live-ready 工程验收，不等于真实 DeepSeek API、外部真实老师人工评分或真实 LLM-as-judge 已通过。

## DeepTutor 融合设计当前口径

- 调研基线：本地 `D:\WorkProject\DeepTutor`，DeepTutor v1.5.11，commit `456f9c24226e008f1ff07a7e3455d7b4d39f6221`。
- 总体架构与接口设计：`docs/24_DEEPTUTOR_OMNI_EDU_INTEGRATION_DESIGN.md`；77 项完整功能台账、逐项处置和生产验收矩阵：`docs/25_DEEPTUTOR_FEATURE_INVENTORY_AND_PRODUCTION_MATRIX.md`。
- 当前台账口径：17 项整体集成、38 项二次开发、8 项裁剪替换、14 项延后/不集成；台账完整不等于全部默认开启。
- DeepTutor 的 Tool 是单步动作，Capability 是接管整轮任务的执行策略；Omni route 继续表示教师业务意图，两者是不同层。
- 推荐运行边界：Electron main 是唯一宿主控制面，DeepTutor Python 是本地 Capability sidecar，通过双向 NDJSON/JSON-RPC 发事件和 HostToolProxy 请求。
- Omni SQLite、学生目录、原始附件、确认队列和文档落盘不向 sidecar 直接开放；sidecar 只接收最小脱敏上下文和 bounded tool result。
- 首批迁移顺序：chat loop → deep_solve → deep_question → teacher-supervised mastery → deep_research/visualize。
- 首轮不迁移：DeepTutor Next.js UI、PocketBase/多用户、Partners/IM、第二套 Notebook/Memory/Question Bank 存储、默认 MCP/CLI/exec 工具面、GraphRAG/LightRAG 和 Manim。
- DeepTutor StreamBus 的 raw thinking 不是小智可展示思考过程；可展示内容必须经过事件适配器转为 stage/tool/source/budget/termination 摘要。
- DeepTutor 源码为 Apache-2.0；正式 vendoring/发布时必须固定上游 commit、保留 LICENSE、适用 THIRD_PARTY_NOTICES 和修改说明。
- 生产性能口径把 run 创建、首事件、首 token 与最终完成分开；“常规并发不超过 2 秒”用于常规在线问答首 token 和 sidecar 冷启动上界，不用于承诺深研究/讲义等长任务两秒完成。

### DeepTutor DT-0 实际复用事实（2026-08-10）

- 上游源码已机械复制到 `python/vendor/deeptutor`，来源固定为 v1.5.11 / `456f9c24226e008f1ff07a7e3455d7b4d39f6221`；许可证与第三方声明位于 `python/vendor/` 根目录。
- `python/omni_edu_deeptutor_bridge/bridge.py` 默认把 vendor 目录加入 `sys.path`，只有显式 `DEEPTUTOR_SOURCE_ROOT` 才允许做源代码对照测试；生产运行不依赖 `D:\WorkProject\DeepTutor`。
- sidecar handshake 使用上游 `CapabilityRegistry.get_manifests()` 生成能力清单；chat dry-run 直接运行上游 `AgentLoop`，deep_solve/mastery_path 直接运行上游 Capability，并仅替换模型客户端为离线 Fake client。
- Fake client 只用于 deterministic contract smoke，不是线上模型适配；HostToolProxy request/result、业务工具 blocked 审计和 `ai_agent_events` readback 已完成第一段，真实模型与真实业务工具仍未接入。
- 已验证：`npm run test:deeptutor-bridge`（handshake、非法帧、重复启动、取消、chat AgentLoop、deep_solve Capability、sequence/done）和 `node apps/desktop/scripts/electron-smoke.mjs`（Electron→preload→main→sidecar handshake/start/stop）。
- HostToolProxy 第一段已接通：`sidecar-client` 识别 `host_tool_request` 后调用 main 的 `routeAiPrompt()` / `executeHostToolRequest()`，回写 bounded `host_tool_result`；`npm run test:deeptutor-host-proxy` 和 Electron smoke 已验证 blocked round-trip。

### DeepTutor DT-0 事件证据闭环（2026-08-11）

- `apps/desktop/src/main/index.ts` 的 `persistDeepTutorEvent()` 是 sidecar event 的唯一入库适配器：先映射为 `AiAgentTraceStep`，再按 turn promise queue 写入 `ai_agent_events`；`done` 会调用 `completeAiAgentRun()`。
- `apps/desktop/src/preload/index.ts` 暴露 `onDeepTutorEvent()`、`getAiAgentRun()`、`listAiAgentEvents()`；renderer 只接收去重后的安全事件摘要，不接收 raw thinking。
- SQLite sequence 是宿主事件序列，不等同于 sidecar sequence；sidecar sequence 只保存在 `outputSummary.sidecarSequence` 诊断字段中。
- `deeptutor-bridge-contract-smoke.mjs` 验证协议错误、连续 sequence、终态和隐藏思维标记过滤；`electron-smoke.mjs` 验证 run/event readback、blocked observation、secret-like 文本不入库。
- 事件订阅和 readback 已完成 DT-0 第一段，AgentLoop 原生 tool-call parity 也已通过；普通 AI 页面仍未默认切到 DeepTutor runtime，真实模型、断线恢复和其他 Capability 仍属于后续阶段。

### DeepTutor AgentLoop 原生工具调用 parity（2026-08-11）

- `python/omni_edu_deeptutor_bridge/bridge.py` 的 `_SingleToolLookup` 和 `_HostProxyTool` 只为当前 parity/只读宿主边界提供最小工具；它们不绕过 Electron main 的 `routeAiPrompt()`、审核和执行。
- 仅在 `context.dryRun === true` 的 parity 测试上下文中，`hostToolAuto` 才会使 fake provider 返回原生 OpenAI `tool_calls`；上游 `AgentLoop` 解析并调用 DeepTutor `dispatch_tool_calls()`，工具执行再通过 `xiazhi.host_tool.request.v1` 等待宿主结果。
- `_HostProxyTool` 会过滤 DeepTutor dispatcher 注入的 `event_sink` 等非业务 kwargs，只传 schema 允许的标量参数；未知/复杂参数不得直接跨进程序列化。
- `npm run test:deeptutor-agent-tool-call` 通过：8 个事件，包含 native tool call、agent_loop HostToolProxy request、blocked result、final result 和 done。
- 该 parity smoke 证明 loop/协议/边界，而非真实模型或真实业务工具已上线；普通 AI 页面切换和真实 provider 仍需单独验收。

### DeepTutor ModelProxy 当前口径（2026-08-11）

- `xiazhi.model.request.v1` / `xiazhi.model.result.v1` 是 sidecar 与 Electron main 的模型边界；sidecar 发送 bounded messages/tools，main 负责读取 DeepSeek 设置、网络调用和错误归一化。
- API Key 只存在于 Electron main 的运行时，不下发 Python、不进入 renderer、不写入 sidecar event；无 Key 返回 `MISSING_MODEL_CREDENTIAL` 并将 run 标记 failed。
- `apps/desktop/src/main/deepseek.ts` 的 `requestDeepSeekCompletion()` 是共享 provider 请求函数，旧的结构化 DeepSeek 流程和 DeepTutor ModelProxy 共用超时/响应解析边界。
- `npm run test:deeptutor-model-proxy` 只证明本地协议 round-trip；当前机器没有 `DEEPSEEK_API_KEY`，不得把它写成 live provider 通过。

### DeepTutor 普通问答入口（2026-08-11）

- 普通 AI 页面默认调用 `window.omniEdu.runDeepTutorConsole()`，而不是直接调用旧的 DeepSeek handler；main 负责 router、system prompt、host tool schema、sidecar 生命周期和 SQLite 运行记录。
- 结构化结果必须从 `finalize` 且 `outputSummary.sidecarPhase === 'result'` 的事件解析；没有合法 reply 时返回失败，不生成占位答案。
- 失败消息优先从最近的非 `done` failed event 读取；这样缺少 provider credential 时会返回 `DeepSeek API Key 未配置...` 等可行动提示，而不是泛化失败文案。
- 当前验收边界：入口切换、sidecar 和 no-key fail-closed 已通过 Electron smoke；真实 provider、真实业务工具和其它 DeepTutor Capability 仍未完成。
- 成功 console 结果的 `toolRuns` 来自持久化 AgentLoop tool_call/observe 事件，`sources` 来自 `xiazhi.reply.v2` evidence；无事件或无合法 reply 时不得伪造工具/来源。
- `xiazhi.host_tool.result.v1.status` 的成功枚举是 `used`；sidecar `_HostProxyTool` 必须以 `status == "used"` 构造成功的 DeepTutor `ToolResult`。
- DeepSolve/Mastery 适配当前使用受控互斥保护 upstream 全局 pipeline 注入；这保证并发正确性，但会串行化该 legacy 注入窗口，后续应改为无全局 monkey-patch 的注入接口再优化吞吐。

### DeepTutor 用户输入 checkpoint（2026-08-11）

- 协议：`xiazhi.user_input.request.v1`（sidecar → main/renderer）与 `xiazhi.user_input.result.v1`（main → sidecar）。请求包含 `requestId/turnId/prompt/questions/expiresAt`；结果包含 `status/text/answers`。
- 持久化：`ai_capability_checkpoints` 保存 `runId/capabilityName/checkpointType/state/status/expiresAt/createdAt/resolvedAt`；状态为 `pending/resolved/expired/cancelled`。
- 生命周期：`ask_user` → `waiting_input` + pending checkpoint → IPC 提交 → `resolved` + `running` → AgentLoop `done`。重复 requestId 不会二次 resolve。
- IPC：`deepTutorSubmitUserInput`、`getPendingAiCapabilityCheckpoint`、`getAiCapabilityCheckpoint`；renderer 通过 `onDeepTutorInputRequest` 展示补充信息卡片，raw hidden reasoning 不进入卡片或 checkpoint。
- 恢复边界：checkpoint 记录可跨进程读取，但当前 sidecar waiter 是内存对象；重启后必须按产品策略安全过期或重新建 turn，不能假设原 AgentLoop 自动恢复。
### DeepTutor capability integration boundary (2026-08-11)

- Vendored DeepTutor v1.5.11 is executed in the Python sidecar; Electron main owns credentials, host tools, SQLite evidence, and business writes.
- `deep_question` uses upstream question pipeline/result parsing. Its generated `qa_pair` items are adapted to Omni `ExerciseSetDraftPayload` only through pending `save_exercise_set` confirmation.
- `onDeepTutorConfirmation` is a preload event used to refresh the renderer confirmation queue; it is not a renderer-side database write path.
- `data/user/settings/main.yaml` and `agents.yaml` are safe local defaults. Real provider runs require explicit credentials and separate live evidence.
- DeepResearch requires explicit `mode` + `depth` and emits an outline preview before long research; Visualize accepts `render_mode` and its first safe adapter output is Mermaid. HTML/SVG/Manim remain separately gated artifact paths.
- Mastery integration boundary: Electron main reads Omni SQLite records, sends only bounded point/module/boolean-attempt evidence to the sidecar, and the sidecar reuses DeepTutor policy arithmetic. The DeepTutor LearningStore is not a second source of truth for Omni students.
- E07/E08 宿主能力口径（2026-08-11）：`research_workspace` 映射到 `knowledge_retrieval`，context 仅允许 `teacher_knowledge`；`generate_research_outline` 返回 `omni.research.outline.v1`，不联网、不读学生、不写文件。
- E07/E08 宿主能力口径（2026-08-11）：`visualization` 映射到 `lesson_design`，context 仅允许 `knowledge_graph`；`render_learning_mermaid` 返回 `omni.visualization.mermaid.v1`，Mermaid 仅作为受限草案，禁止脚本/外链，必须教师复核。
- 这两个工具的权限、证据和副作用均由 main-process tool registry 审核；sidecar dry-run 或能力清单不能被当作 live provider 或生产完成证明。
- C05 高级 block 口径（2026-08-11）：`timeline/code/deep_explanation/user_note` 在 `renderTeachingBookMarkdown` 中做 bounded 编译；`interactive/animation` 永不执行 payload，仅输出安全占位并计入 fallback。所有讲义导出仍以 `document_artifacts` readback 和教师确认作为完成条件。
- `mastery_status` 是当前 Mastery 只读工具契约：`contextKey=learning_records`、`effect=read`、`privacy=local_only`，仅由 `student_diagnosis` 和 `practice_design` 路由开放；实现位于 `apps/desktop/src/main/ai-harness/tool-registry.ts`，快照实现位于 `mastery-snapshot.ts`。
- 工具事件审计采用最小摘要：`resultKeys`、`rawRecordsIncluded`；不会持久化 mastery 原始学习记录、答案或题干。Electron smoke 已验证真实 HostToolProxy round-trip 与 `used` 状态。
- Mastery 互动数据模型：`ai_mastery_questions` 保存 run/turn/student/knowledgePoint/stem/options/expectedAnswer/status/answer；expectedAnswer 仅主进程可读。`mastery_quiz` → `ask_user` → `mastery_grade` 是第一条教师工作台闭环，grade 成功后写入 `learning_records` 的显式 `mastery_attempt` JSON。
- `mastery_quiz` 与 `mastery_grade` 是受控 managed write，只能通过 `mastery_path` HostToolProxy 触发；普通只读工具审核仍拒绝 draft/write，重复 grade 或答案不一致会 blocked。
- `deepTutorCancelTurn` 与 `deepTutorStop` 会同步取消关联的 `ai_mastery_questions`，保证 checkpoint 和题目业务状态一致。
- Mastery 路径数据模型：`ai_mastery_paths` 按 student 保留 active path、version、mode、modules_json；`mastery_build` 返回 bounded module summary，路径可通过 preload `getAiMasteryPath` 回读。路径点优先于学习记录点供 `mastery_assess/quiz` 解析，但学习记录仍是掌握证据唯一来源。
- Mastery 写入契约：`mastery_assess`/`mastery_build` 先产生 `save_mastery_state` confirmation，教师确认后才执行数据库写入；确认结果必须包含 `masteryAttempt` 或 `masteryPath` readback。`npm run test:deeptutor-capability-evals` 的 48 个用例是本地合同/护栏验收，不等于真实模型或外部教师验收。
- Sidecar 生命周期契约：`DeepTutorSidecarClient` 异常 exit 触发 main-owned `onExit`；main 必须完成活动 `ai_agent_runs`、取消挂起输入 checkpoint、清理内存 binding。`stop()` 为有意终止，不重复 crash recovery。每个 turn 的 `maxWallMs` 是 watchdog 上限，超时不可留下 `running`。
- Mastery policy 契约：`mastery_status` 在 `buildMasterySnapshot` 的唯一快照上计算 `omni.mastery.policy.v1`；`evidenceKind=quiz|assess|unknown` 参与门禁，concept/design 只允许确认后的 assess 正确证据晋级；sidecar 事件最多保存 policyVersion/nextAction/nextKnowledgePointId/dueReviewCount。
- E16 学情分析契约：`analyze_learning_progress` 只读 `learning_records`，输出 `omni.learning.analytics.v1`；日期使用 `UTC-calendar`，最多 366 天，统计结果含 `sourceRecordIds`/`recalculationKey`，原始正文不出 sidecar。报告草稿的窗口与来源通过 bounded event metadata 绑定到 `create_review_report` confirmation，确认后才写 `review_reports`。
- E14 错因分类契约：`classify_error_patterns` 只在 `error_analysis` route 读取绑定学生的错题/订正记录，复用 DeepTutor 四类 taxonomy 并保留 `unknown`；输出 `omni.error.taxonomy.v1` 的 counts/classifications/facts/unknowns/sourceRecordIds，重算指纹不得包含正文。该工具只读，不自动写学生档案；应用错误和未知必须提示教师复核。
- E17 Vision Solver 契约：`analyze_geometry_figure` 只在 `error_analysis` route 使用 `attachment_metadata` context，复用 vendored DeepTutor `agents/vision_solver` 的单次分析/一次修复边界和 `ggb_validator` 的语法修正思想；输出 `omni.vision.solver.v1`，原图/本地路径/原 OCR 不进模型结果，命令白名单和隐私过滤后才进入 `ggbScript`，始终需要教师预览且不自动写回。专项命令为 `npm run test:deeptutor-vision-solver`。
- R13 附件探索契约：`attached_source_exploration` 只在显式附件分析请求中启用，`explore_attached_sources` 从当前绑定学生的 `learning_records.attachments` 与 `mistake_image_analyses.sanitized_text` 构造授权 source handles；输出 `omni.attached.sources.exploration.v1` 的 bounded snippets/source metadata，`rawFilesIncluded=false`、`localPathsIncluded=false`，越权 attachmentId、无学生、无匹配和无 OCR 均 fail-closed。专项命令为 `npm run test:deeptutor-attached-source`，18/18。
- C01 备课本契约：`teacher_notebooks` 与 `teacher_notebook_records` 是 Omni-Edu 本地 SQLite 单一真源；结构参考 DeepTutor `NotebookManager`，删除使用状态/时间戳软删除并支持恢复，写入更新必须匹配乐观 `version`。main/preload IPC 已暴露 CRUD，专项命令为 `npm run test:deeptutor-notebook-crud`。
- C02 备课本分析契约：`analyze_notebook_context` 只有在 router 检测到笔记/备课本/notebook 触发词时才加入 `teacher_notebook` context；目录/细节 bounded，所有模型可见字段先经 `sanitizeProblemText`，record summary 只产出需教师复核的本地草稿，不自动写回。专项命令为 `npm run test:deeptutor-notebook-analysis`。
- C03 Question Notebook 契约：`question_bank_items` 是 canonical 题目真源，`question_notebook_bookmarks`、`question_notebook_categories`、`question_notebook_category_links`、`question_bank_usage` 只保存收藏/分类/引用 overlay。题目收藏请求由 router 进入 `practice_design/question_notebook`，只读取 `search_question_notebook`，不携带学生或教师知识库上下文；工具输出 `omni.question.notebook.v1`，正文统一脱敏且只读。专项命令为 `npm run test:deeptutor-question-notebook`。
### C05 Renderer 口径

- `draft_teaching_book_markdown` 只生成预览草稿，正式导出必须经过教师确认；renderer 不执行 figure/code payload，只输出安全 fenced Markdown。
- 通用 `document_artifacts` 导出已支持 Markdown、PDF（Unicode CID 字体）和 DOCX（标题/列表/代码样式）基础真实文件；成功必须以 `status=exported`、真实 path/size/hash 和 SQLite readback 为准。复杂分页、图片/动画和完整 office 版式仍不属于已完成能力。
- `plan_teaching_book` 是章节规划草稿，不是自动建书；只读 bounded 知识片段和来源元数据，输出必须带 sourceRefs、unknowns 和教师确认要求。
- `refresh_teaching_book_health` 是本地健康元数据刷新工具：基于 teaching_book_sources 的原始 fingerprint 更新 available/stale/missing，并把受影响 page/block 写入 teaching_book_invalidations；不修改正文、不带学生 context。专项命令为 `npm run test:deeptutor-book-health`。
- `draft_teaching_book_patch` 只生成教学书 block 的 before/after 草稿；apply/undo 需要版本条件和教师确认，历史状态保存在 teaching_book_patches。专项命令为 `npm run test:deeptutor-book-cowriter`。
- `draft_teaching_book_selection_patch` 复用 DeepTutor CoWriter 的 automark/react-edit 语义，对 text payload 的明确选区生成 `replace_selection`/`automark_selection` 草稿；保存 selectedTextHash 与 bounded stream events，apply 同时校验 block 版本和选区指纹。专项命令为 `npm run test:deeptutor-book-cowriter-selection`。
- `role-profile.ts` 提供三种内置教学策略：`teacher`、`peer`、`research_assistant`；由显式措辞推断，未识别回退 `teacher`。profile 只影响 Agent trace 的指导语句，不改变工具、上下文或权限。专项命令为 `npm run test:deeptutor-persona-profile`。
- `inspect_memory_trace` 是 C11 的 L1 只读入口：数据来自 SQLite `ai_agent_runs`/`ai_agent_events`/`ai_tool_runs`，router 只加入 `memory_trace` context，IPC 为 `aiAgent:listRuns` 与 `aiAgent:getMemoryTrace`；返回 bounded 阶段/状态/工具名/证据键，不返回 prompt、detail、学生正文或 hidden reasoning。专项命令为 `npm run test:deeptutor-memory-trace`。
- C12 L2 入口：`listAiMemoryDocuments`/`getAiMemoryDocument`/`draftAiMemorySummary`/`createAiMemoryEntry`/`updateAiMemoryEntry`/`deleteAiMemoryEntry` 均通过 preload IPC；surface 为 chat/notebook/quiz/kb/book/partner/cowriter。`draft_memory_summary` 只读，正式写入需教师动作；条目 refs 必须命中 `ai_agent_runs` 或 `ai_agent_events`，版本冲突和禁用状态由 SQLite 审计。专项命令为 `npm run test:deeptutor-memory-summary`。
- C13 L3 入口：`listAiMemoryL3Documents`/`getAiMemoryL3Document`/`draftAiMemoryL3`/`createAiMemoryL3Entry`/`updateAiMemoryL3Entry` 通过 preload IPC；slot 为 recent/profile/scope/preferences。`draft_memory_synthesis` 只读，来源仅为 active L2 surface；正式写入需教师采纳，版本冲突 fail-closed。专项命令为 `npm run test:deeptutor-memory-synthesis`。
- C14 graph 入口：`getAiMemoryEvidenceGraph(limit)` 和 `inspect_memory_graph` 只读返回 bounded `nodes/edges`；节点类型为 l3_entry/l2_entry/run/event，边为 derived_from/evidence，来源自 SQLite active L2 refs 与 L3 source surfaces。默认上限 200 节点/400 边，不返回正文或 hidden reasoning。
- C15 governance 入口：`getAiMemoryGovernanceReport`/`inspect_memory_governance` 返回 `memory-governance.v1`、L2/L3 计数、danglingEvidenceRefs、disabled/deleted 计数、graphNodes/graphEdges 与 `writableByAi=false`；只读，active-only graph 不带回停用/删除条目。
- 记忆性能命令：`npm run test:deeptutor-memory-performance`，当前 fixture 为 100 L2 + 1 L3、20 次并发查询，p95/max 2ms；只作为局部基线。
- C03 renderer 入口：`ViewKey=question_notebook`，所有数据访问经 `window.omniEdu` typed preload。筛选只传 query/categoryId/bookmarked，收藏更新必须携带 overlay version；分类按钮为幂等 add/remove，分类删除是软删除。该页面不是批量迁移器，也不改变 canonical 题库正文。
- 备份契约：`exportDataRoot(destinationRoot)` 只能写到当前数据目录之外，返回 `manifestPath/fileCount/verified`；manifest schema 为 `omni-edu-backup-manifest.v1`，每项包含相对路径、size、sha256。`verifyDataBackup(path)` 返回 `missingFiles/changedFiles/unexpectedFiles`，设置页通过 typed preload 调用；当前是校验而不是自动恢复。
- C05 讲义契约：`previewTeachingBookMarkdown(id)` 通过 `teachingBook:previewMarkdown` 调用主进程 `renderTeachingBookMarkdown`，返回 `omni.teaching.book.markdown.v1`、fallback/source 统计和 `writesFile=false`；讲义页导出只能调用 `exportDocumentArtifact`，成功条件是 `document_artifacts.status=exported` 且有真实 path/size/hash。来源健康异常必须显示警告，未知 block 不执行 payload。
- E15 复习调度契约：`review-scheduler.ts` 是 DeepTutor `learning/scheduler.py` 的 Omni 纯函数适配，间隔序列为 memory `0/1/3/7/14/30/60`、concept `3/7/14/30`、procedure `3/7/14`、design `14/28` 天；`get_review_queue` 输出 `omni.review.queue.v1`，按 priority/order/id 稳定排序，只读 bounded、epoch due 不随 timezone 变化，不写业务表或返回原始记录。
- E15 证据边界：普通记录的 title/summary/free text 不得建立知识点；`buildMasterySnapshot` 只有显式 `metadata.knowledgePoint` 或 mastery signal 的受控标签才生成 point，重启后从 SQLite 重新计算，不持久化第二份 review truth。
- E15 类型边界：`mastery_grade` 写回前从同一 snapshot 读取 question point 的 `knowledgeType`，不允许 quiz 的默认题型覆盖 procedure/memory；因此 procedure 的 `mastery_assess` 始终返回 `wrong_gate`。
- E15 事件摘要：sidecar `tool_result` 仅额外保留 bounded `reason/knowledgeType` 门禁诊断，不携带完整 model result、题干或原始学习记录。
- E15 提醒契约：`review:getReminder` 由 main 从 `buildMasterySnapshot` + `buildMasteryPolicy` 即时重算 `omni.review.reminder.v1`，仅返回 due/upcoming 计数和最多 3 个知识点摘要；不写 reminder 表、不暴露原始记录、不提供 renderer 确认/删除动作。今日工作台只做展示和证据跳转。
- E15 性能证据：`test:deeptutor-review-performance` 覆盖真实 native SQLite 8×200/16 并发、3 轮及 close/reopen；`test:deeptutor-review-performance-electron` 覆盖同规模 Electron IPC 两轮。阈值为每轮 p95/max 或批次耗时 ≤ 2000ms；这些是本地硬件基线，不等同 provider 网络 SLO。
- E10/E12 待作答题契约：`mastery_status` 从 `ai_mastery_questions` 恢复最早 `pending/answered` 题并只返回 `id/knowledgePoint/stem/options/status/answerSubmitted`；`expectedAnswer` 与答案永不出公共投影。`mastery_grade.questionId` 可省略但只能使用当前绑定学生最早可评分题目，跨学生必须 fail-closed；关闭并重启 SQLite 后状态仍可回读。专项命令为 `npm run test:deeptutor-mastery-pending`，14/14 仅证明单题恢复边界，不等于多题或 live provider 完成。
- E10/E12 多题队列契约：`mastery_quiz.questionCount` 仅允许 1-5，main 用事务批量写入同一学生的 `ai_mastery_questions`；同一学生存在 pending/answered 时拒绝第二组，`mastery_grade` 按 oldest-first 逐题消费并拒绝 `question_order_mismatch` 跳题，公共结果带 `pendingQuestionCount` 但不带答案。专项命令为 `npm run test:deeptutor-mastery-quiz-queue`，23/23 仅证明本地连续队列与恢复边界。
- R10 预算契约：每个 DeepTutor turn 的 sidecar 事件由 main binding 串行计数，Python `TurnState.max_events` 在 sidecar 写出前提供第二道闸门；`maxEvents` 归一化范围为 1-256。超限输出 `terminationReason=budget_exhausted`、`hardStop=true`；最多 3 代续写 checkpoint，run 为 `blocked` 并发一次性 token，达到第 4 代时 `failed` 且不再发 token。`deepTutorContinueTurn` 创建带 `parent_run_id` 的子 run；请求提高事件/时间预算时先创建 `budget_approval` checkpoint，批准后才消费 continuation token；重复或过期 token 必须拒绝，SQLite 更新必须以变更行数保证并发单赢家。renderer 查询 checkpoint 时只返回可继续摘要，不返回原始 request/system prompt/工具 schema；AI 对话流提供真实继续与预算批准按钮。`npm run test:deeptutor-r10-budget` 是当前真实 Electron/SQLite 验收命令，31/31 通过；token/费用预算尚未提供。
- R11 运行变更契约：主进程将完整 capability request 保存在 `ai_agent_run_requests` 私有表，`ai_agent_run_actions` 以 source/action/idempotencyKey 唯一约束记录 retry/branch/regenerate 子运行；renderer 只能通过 typed preload 发起变更，不能提交新的 system prompt、工具定义或绕过原 route。retry 仅允许 failed/blocked/cancelled，成功源必须 branch/regenerate；子运行保留 `parent_run_id`，close/reopen 后仍可回读。`npm run test:deeptutor-r11-lineage` 12/12 通过；复杂写事务回滚、多设备冲突、live provider 和完整结果合并尚未提供。
- R04 AgentLoop 契约：`runDeepSeekChat` 最多执行 3 轮/8 次模型工具调用；每轮持久化 plan/tool_call/observe 摘要，工具结果先经主进程审核和 bounded，再回给模型决定下一步。预算耗尽后写入 guardrail 并强制 no-tool finalization；`npm run test:deeptutor-multi-round` 10/10 是确定性 SQLite/fake-model 证据，不等于真实 provider 质量完成。
- R09 恢复契约：pending `user_input` checkpoint 在应用重启后由 `listPendingDeepTutorInputs` 回读，renderer 只能通过 preload 提交；主进程用 `claimAiUserInputCheckpoint` 原子消费一次答案，再从私有 request snapshot 创建恢复 child run，旧 run 以 blocked 接管原因结算并保留 `parent_run_id`。`npm run test:deeptutor-r09-restart` 12/12 通过；这不是跨进程恢复同一个 Python Promise。
- Console JSON 契约（2026-08-12）：ModelProxy 的无工具最终请求必须使用 `json_object`；`runDeepTutorConsole` 对非 JSON provider 输出执行一次 bounded repair，并为 repair 起止写入可审计 guardrail。最终 schema 失败必须将 run 结算为 failed；`parseStructuredReply` 在多 JSON 外壳中优先选择 `schemaVersion=xiazhi.reply.v2`，仍执行完整 route/教育/可用性校验。
- R05 progressive tool 契约（2026-08-12）：`router.allowedTools` 是硬总许可；初始模型 schema 由 `progressiveToolNames()` 生成，仅含 `load_tools`、受 route 允许的核心工具。`load_tools` 通过 `TOOL_GROUPS` 解析工具族，主进程复核 route/context allowlist 后返回 `omni.tool.catalog.v1` 与 `loadedToolNames`，并保证 `toolsExecuted=false`；下一轮才把加载 schema 发给模型。Python bridge 的 `_SingleToolLookup` 仅向 AgentLoop 暴露 initial names，收到宿主 catalog 后激活，不承担业务执行。专项命令为 `npm run test:deeptutor-progressive-tools`（18/18）和 `npm run test:deeptutor-progressive-sidecar`（14/14）；真实 provider 和复杂上下文质量仍未完成。
- 前端能力映射口径（2026-08-12）：以 `docs/26_FRONTEND_E2E_COVERAGE_MATRIX.md` 为准。后端入口存在时，renderer 必须通过 typed preload 接线并显示真实状态；错题主闭环由 `MistakesWorkspace.tsx` 负责视图编排，业务校验、脱敏、写入和确认仍归主进程。Electron smoke 同时验证 1366×768 与 1920×1080；原生附件选择器采用受控 SQLite fixture 的事实必须保留，不能误报为系统对话框 E2E。
- JSON 回复契约口径（2026-08-12）：DeepSeek 请求默认带 `response_format: { type: 'json_object' }`；最终输出仍必须通过 `parseStructuredReply` 的 `xiazhi.reply.v2` 校验。允许最多两次受控 repair，repair 只修语法/字段，不新增工具或证据；BOM 会在提取前移除。超过次数仍返回 blocked/failed，不能用 Markdown 或猜测性回退伪造结构化成功。
- DeepTutor 事件长度口径（2026-08-12）：`boundDeepTutorEventDetail` 对普通 plan/tool/observe 事件使用 8,000 字符上限，对 `phase=result` 的最终结构化回复使用 64,000 字符上限。该差异用于避免宿主把合法 JSON 截断；两者仍是 bounded，超过最终上限不会自动补造。

### 前端验收真源（2026-08-12）

- `docs/26_FRONTEND_E2E_COVERAGE_MATRIX.md` 是 backend→preload→renderer→用户动作→证据的当前真源；组件状态门禁为 `npm run test:renderer-components`，完整点击/重启门禁为 `npm run test:smoke`。
- 题本 renderer 位于 `apps/desktop/src/renderer/components/QuestionNotebookWorkspace.tsx`，canonical 正文仍来自 `question_bank_items`；bookmark/category/usage 只是 overlay。
- 教师备课本 renderer 位于 `apps/desktop/src/renderer/components/TeacherNotebookWorkspace.tsx`，通过既有 typed preload 管理 notebook 与 record。notebook 可软删除/恢复；record 当前可软删除和 include-deleted 审计查看，但没有 restore IPC。
- AI 文档产物提供“导出到本地工作目录”（`<dataRoot>/exports/ai-artifacts`）与“选择位置导出”两条前端路径；成功必须回读 `status/filePath/fileSize/contentHash` 并确认文件存在。
- 三元题组 `tripletDraft` 是 renderer 预览状态，不是 confirmation 真源。现有 `confirmAiConfirmation(id)` 不接受覆盖载荷，所以编辑后的题干/答案尚不能作为已保存能力。
### 专题讲义前端（2026-08-12）

- 入口：`apps/desktop/src/renderer/components/TeachingBookWorkspace.tsx`，由 `App.tsx` 的“讲义”导航渲染；所有写入均经 `window.omniEdu` typed preload。
- 正文契约：text/chapter/section 支持 `payload.body/markdown/content/text`；局部协作以 `payload.text`、字符边界和选区 hash 为事实源。
- 导出契约：预览只读且 `writesFile=false`；教师点击后写入 `<dataRoot>/exports/ai-artifacts`，必须回读 `document_artifacts` 与真实文件 SHA-256。
- 当前验收：renderer components 8/8，Electron 前端 54/54，Teaching Book 专项 130/130；归档恢复和真实 provider 不在本轮完成声明内。

### Question Notebook 管理前端（2026-08-12）

- 入口：`QuestionNotebookWorkspace.tsx` 自管理题目、分类和 usage 状态，`App.tsx` 只负责导航与全局状态提示。
- 教师录题写入 `question_bank_items` canonical 真源；收藏、分类、关联和 usage 继续只写 overlay 表。教师 UI 不提供 `generated` 来源选项。
- 分类重命名携带 version，删除/恢复保留关联；操作错误显示后仍渲染最近 SQLite readback。当前门禁为组件 9/9、Electron 66/66、专项 22/22。

### L1/L2/L3 记忆治理前端（2026-08-12）

- 入口为 `apps/desktop/src/renderer/components/MemoryGovernanceWorkspace.tsx`，由“L2 记忆”导航渲染；所有数据只经既有 typed preload，renderer 不复制证据校验或写入规则。
- L2 支持 draft/adopt/edit/disable/restore/soft-delete/revision list；L3 支持 draft/adopt/edit/disable/restore。候选必须教师点击后才写库，所有正式操作携带 entry version。
- evidence graph 只显示 `l2_entry/l3_entry/run/event` 短标签和 `evidence/derived_from` 关系；governance 显示 active/disabled/deleted/dangling 计数并固定 `writableByAi=false`。raw prompt 与 hidden reasoning 始终不渲染。
- 当前门禁：组件 14/14、生产 Electron 87/87、L1 30、L2 36、L3 30；本地 100 L2 + 1 L3 的 20 次治理查询 p95/max 3ms。该性能是本机 SQLite 基线，不是 provider 或生产并发 SLO。

### 错题附件 dialog 与导入验收（2026-08-12）

- 正式入口保持 `MistakesWorkspace → App.importAttachment → window.omniEdu.importAttachments → attachments:import → store.importAttachments`；业务结果为 source item、受管目录副本、SHA-256 与 SQLite attachment readback。
- `OMNI_EDU_E2E_DIALOG_MODE`/`OMNI_EDU_E2E_ATTACHMENT_DIALOG_QUEUE` 只控制未打包测试进程的 dialog 返回值，不是产品配置或新 IPC；打包应用强制拒绝适配器并继续使用系统 dialog。
- 当前 Electron 证据从按钮开始覆盖复制成功和取消零写入，组件 17/17、完整前端 91/91。它验证生产 handler，不声称自动操控 Windows 原生窗口。

### DeepTutor 结构化结果预算契约（2026-08-12）

- Python bridge 的 `_DryRunPipeline.loop_max_tokens` 固定为 8,000，并由 Electron model proxy 继续夹取到最多 8,000；该预算同时服务普通最终回答与 `xiazhi.reply.v2`，不改变工具轮数或权限。
- sidecar `phase=result` 最多保留 64,000 字符，Electron main 的 `boundDeepTutorEventDetail('result', ...)` 同样为 64,000；普通事件仍为 8,000。超过最终上限继续 fail-closed，不自动补造闭合括号或字段。
- 当前专项证据为 `npm run test:deeptutor-progressive-sidecar` 17/17：三个模型回合均请求 8,000 tokens，24,452 字符最终 JSON 跨越旧 20K 阈值后仍可解析。该证据不等于真实 DeepSeek provider 已验收。

### AI 文档产物前端契约（2026-08-12）

- 入口为 AI 历史消息产物标签与右侧通用产物面板；renderer 只调用 typed preload 的既有 `exportDocumentArtifact`，正式真源仍是本地文件和 `document_artifacts`。
- 支持类型为 Markdown、PDF、DOCX。成功态必须包含 `status=exported`、真实 `filePath/fileSize/contentHash`；空正文必须显示 `failed` 且不写文件、不写 artifact row。
- 当前生产 Electron 门禁为 100/100，文档子项验证 PDF Unicode CID、DOCX ZIP/Heading/长中文正文、SQLite/file hash 与重启 readback；1366×768 PDF/失败态及 1920×1080 DOCX 截图已人工复核。
- v1 边界仍是基础 PDF/DOCX：复杂分页、嵌入中文字体、表格、图片、公式及 Office 像素级保真不属于已完成能力。

### 学习路径前端契约（2026-08-12）

- 入口为 `apps/desktop/src/renderer/components/MasteryPathWorkspace.tsx` 和“学习路径”导航；读取链路固定为 `getAiMasteryPath → mastery:getPath → ai_mastery_paths`，按当前学生隔离。
- 页面只呈现已确认路径的 version、mode、module order 与 knowledge point type；不存在可从这些字段可靠推导的掌握百分比，因此 UI 固定禁止显示伪百分比。
- 路径页面只读，修改入口转交小智并保留教师确认边界。当前门禁为组件 22/22、生产 Electron 109/109；学习路径专项新增 9 个用户路径和两张双视口截图。

### 全局搜索前端契约（2026-08-12）

- 入口为 `apps/desktop/src/renderer/components/GlobalSearchWorkspace.tsx` 和“搜索”导航；数据链为 `searchAll → search:all → store.search → SearchResult(students, records)`。
- 查询仅在本机 SQLite 执行，输入上限 200 字符；学生结果进入学生工作区，记录结果以 `studentId` 选择所属学生并复用时间线关键词过滤，不创建搜索专属数据副本。
- 状态区分 idle/loading/error/empty/results。当前门禁为组件 27/27、生产 Electron 118/118；全局搜索新增 9 个用户路径与 1366×768、1920×1080 两张截图。

### 已确认题组前端契约（2026-08-12）

- 入口为 `MistakesWorkspace` 第 5 步中的 `ExerciseSetLibrary.tsx`；唯一读取链为 `listExerciseSets → exerciseSets:list → store.listExerciseSets → exercise_sets`，按 `studentId` 隔离并按创建时间倒序。
- 正式题组是教师确认后的只读产物，不等同于 Question Notebook canonical 单题。页面展示 role、sourceKind、difficulty、stem、answer、analysis、teacherObservation；`generated` 必须显示为“小智生成 · 待教师复核”。
- 刷新仅重新读取，学生切换先清空旧集合；修改题组必须回到草稿和 confirmation，不允许 renderer 直接写库。当前门禁为组件 32/32、生产 Electron 127/127，专项新增 9 个用户路径和两张双视口截图。

### 复习提醒前端契约（2026-08-12）

- 入口为“今日”工作台中的 `ReviewReminderPanel.tsx`；读取链固定为 `getReviewReminder → review:getReminder → buildReviewReminder`，数据源是本地 `mastery_attempt` 学习记录，提醒本身不持久化第二份队列。
- 状态为 no-student/loading/error/clear/upcoming/due。due/upcoming 只显示知识点、模块、类型、dueAt 和调度状态；`rawRecordsIncluded=false`，正文、答案、附件路径不得进入 DOM。
- 刷新只重新计算，证据按钮进入学生工作区；学生切换清空旧结果。当前门禁为组件 38/38、生产 Electron 137/137，复习提醒新增 10 个用户路径与 1366×768、1920×1080 两张截图。

### 学生档案生命周期前端契约（2026-08-12）

- 入口为 `StudentProfileLifecycle.tsx`；调用链固定为 update/archive/export/openFolder typed preload。归档后只读，没有 restore IPC，其他业务视图只选择 active 学生。
- export 的正式真源为导出目录与 `metadata.json`，包含学生、记录、报告及附件副本；取消选择目录必须零文件。打开目录在打包应用继续调用 `shell.openPath`。
- 当前门禁为组件 49/49、生产 Electron 150/150；新增 13 个用户路径和两张双视口截图，覆盖 SQLite/file/restart readback。E2E 测试适配器只替代未打包进程的系统选择结果与 Explorer 打开动作，不等于自动操作 Windows 原生窗口。

### 小智结构化 JSON 本地语法修复契约（2026-08-12）

- `parseStructuredReply` 按“严格 JSON → bounded 完整对象语法修复 → 完整 `xiazhi.reply.v2` 语义校验”执行；语法修复使用精确锁定的 `jsonrepair@3.14.0`。
- 可修复输入最多 64,000 字符且必须具有 `{...}` 完整外壳，典型范围为尾逗号、字符串裸换行等 JSON-ish 漂移。截断 JSON、非对象或缺字段对象仍失败关闭。
- 该层不替代 provider 两次 repair，也不放宽 route、evidence、education grader、write confirmation 或 artifact 安全边界。

### 完整数据备份前端契约（2026-08-12）

- 入口为设置页 `DataBackupPanel.tsx`；调用链固定为 `exportDataRoot/verifyDataBackup → app:exportDataRoot/app:verifyDataBackup → OmniEduStore`，renderer 只组织状态与展示，不复制数据或重算 manifest。
- 导出目录必须在 live data root 之外；manifest schema 为 `omni-edu-backup-manifest.v1`，文件项保存相对路径、非负 size 与 64 位十六进制 SHA-256。校验结果区分 `missingFiles/changedFiles/unexpectedFiles`，任何非空集合都显示失败。
- UI 状态为 idle/exporting/verifying/export_success/verify_success/verification_failed/cancelled/error；取消不写文件，成功不代表自动恢复。当前门禁为组件 56/56、生产 Electron 162/162、底层备份专项 9/9，并有 1366×768 成功态和 1920×1080 篡改失败态截图。
- 未打包 E2E 可通过显式环境队列替代 Windows 目录选择结果，`$last` 只在同一进程指向刚导出的路径；生产与跨重启复验都使用真实绝对路径。该适配器不等于原生 Windows dialog 自动化。

### AI 对话库前端契约（2026-08-12）

- 入口为 AI 页面 `AiConversationSidebar.tsx`；持久化链固定为 `aiConversations:* typed preload → main handler → OmniEduStore → ai_conversation_folders/sessions/messages`。组件接收 IPC 返回的 folders/sessions，不建立 renderer 专属数据副本。
- 文件夹和会话支持创建、重命名、拖放移动和归档；空名称零写入。会话归档只设置归档状态，文件夹归档同时归档其中会话；消息行保留。设置页展示 `archivedFolders/archivedSessions` 作为审计入口。
- UI 状态为 idle/working/success/error，归档另有 confirmation/cancel。没有 restore IPC 时不得展示恢复。当前门禁为组件 63/63、生产 Electron 176/176；新增 14 个用户路径及 1366×768 对话库、1920×1080 归档确认截图。
- `data-testid` 稳定前缀为 `ai-conversation-*`：sidebar/new/folder-new/folder-save/session-{id}/folder-{id}/drop-{id}/context-rename/context-archive/archive-confirm/archive-cancel。E2E 从这些控件出发验证 SQLite 与重启，不用直接 API 调用替代用户路径。
# DeepSeek JSON Output 恢复契约（2026-08-12）

- 正常最终请求：`response_format={type:'json_object'}`，无工具，输出上限 8,000 tokens。
- 可恢复条件：首个 choice 的 content 为空，或 `finish_reason=length`。
- 恢复动作：最多一次 `temperature=0`、无工具、8,000 tokens 的压缩/重生成请求；不得新增事实与工具调用。
- 最终真源：恢复后的文本仍由 `parseStructuredReply` 执行 `xiazhi.reply.v2` 语法与语义校验；本地 `jsonrepair` 只处理 64,000 字符内的完整对象外壳，不修复截断语义。
- 公开观测字段：`finishReason`、`contentPresent`、`jsonRecoveryAttempted`、`jsonRecoveryReason`。连续空 content 返回明确 provider 错误。

### 本地复盘报告前端契约（2026-08-12）

- 入口为“复盘”导航中的 `ReviewReportWorkspace.tsx`；持久化链固定为 `generateReview/updateReport/listReports typed preload → reports:* main handler → OmniEduStore → review_reports`，renderer 不直连文件或数据库。
- UI 状态为 no-student/idle/generating/draft/saving/saved/error；历史、编辑器、`sourceRecordIds` 证据、质量检查和父摘要都按当前学生隔离。稳定 `data-testid` 前缀为 `review-report-*`。
- `generateReview` 会立即插入 SQLite 草稿，并在 `<dataRoot>/students/{studentId}/reports/{reportId}.md` 写入初始 Markdown 快照；`updateReport` 当前只更新 SQLite 内容、父摘要和质量结果，不重写该文件。初始快照不等于最终交付，最终文件走 `exportDocumentArtifact` 文档导出链路。
- 当前门禁为组件 71/71、生产 Electron 191/191；复盘报告新增 8 个组件状态和 15 个 Electron 用户路径，覆盖零写入、SQLite/file readback、学生隔离、历史、重启及 1366×768/1920×1080 双视口。

### 小智质量评审前端契约（2026-08-12）

- 入口位于设置页 `AiQualityReviewWorkspace.tsx`；调用链固定为 typed preload 的 usability review/replay experiment/model grade 方法 → Electron main → `OmniEduStore` SQLite，renderer 不建立第二套统计真源。
- 教师可保存 1–5 分人工评分、需重写标记、有用轮次和 issue code；失败样本可预填当前 AI 输入，选择 before 后保存 after 会创建有真实 review ID 关联和 score delta 的 replay experiment。
- CSV/TSV 支持本地文件或粘贴文本，全部行先预检；预检失败零写入。逐行 IPC 运行中失败不保证事务回滚。模型 grade 为只读并展示 `graderMode`，proxy 不等于真实 `llm_judge`。
- 当前门禁为组件 79/79、生产 Electron 207/207；新增 8 个组件状态和 16 个 Electron 用户路径，覆盖 SQLite readback、重启与 1366×768/1920×1080 双视口。

### DeepTutor 控制台调用边界（2026-08-12）

- `runDeepTutorConsole` 是当前 AI 中控台入口；它构造 `xiazhi.capability.request.v1`，由 `selectDeepTutorCapability()` 根据 route/subIntent 选择能力，不再固定为 `chat`。
- Electron main 的 HostToolProxy 先校验 capability 到 router route 的映射，再调用 Omni 工具注册表；映射不通过时工具不会执行，返回 blocked。
- `waiting_input` 是持久化可恢复中间状态，不是最终回复；当前同步控制台保持 pending，renderer 经 IPC 提交答案后同一 AgentLoop 继续收敛。
- 当前映射：`chat` 对应普通问答/工作台帮助/知识检索，`deep_solve` 对应学生诊断/错因，`deep_question` 对应题组，`deep_research` 对应备课/报告，visualization/research subIntent 可覆盖默认能力。
- Console 使用 `executionProfile=omni_console`，表示保留 capability 权限与身份但统一进入 Omni 已接通的 AgentLoop；未声明该 profile 的 `deepTutorStartTurn` 仍可走原生高级 Capability。

### 小智 Agent Harness v1 长期口径（2026-08-12）

- 当前版本为 `xiazhi.agent-harness.v1`，分层控制身份/边界、协作协议、route/capability、上下文预算、工具权限和最终交付门禁。
- `agent-harness-profile.ts` 是 route→capability 与 capability→route 的单一事实来源；`deepseek.ts` 注入版本化运行指令，不能把全部项目规则无差别塞进每轮上下文。
- 同一 DeepTutor turn 的 HostTool 状态保存在 main binding；学生引用解析成功后，后续 profile/records 等工具复用该 studentId。
- Console 最终成功必须同时有非空结构化回复、`xiazhi.reply.v2` 校验、Education Grader 和 Usability Grader 通过；结果暴露 harnessVersion、selectedCapability、runId 和安全 trace。
- Sidecar 根目录需有界探测 Python 模块；stderr 只保留 4,000 字符脱敏尾部用于失败诊断。旧库的迁移列必须先于依赖索引创建。
- 设计与验收基线位于 `docs/27_XIAOZHI_AGENT_HARNESS_ENGINEERING.md`。

### 小智 direct / structured 执行契约（2026-08-30）

- 控制台 IPC 仍为 `runDeepTutorConsole → ai:deepTutorRunConsole`，但 main 在启动 sidecar 前调用 `decideExecutionMode()`。
- `direct`：无本地上下文、无工具、无 DeepTutor AgentLoop、无 `xiazhi.reply.v2` 和 grader；返回普通 Markdown，`selectedContext=[]`、`toolRuns=[]`、`schemaApplicable=false`、`graderApplicable=false`。
- `structured`：保留 route→capability、progressive tools、真实证据、两次受控 JSON repair、Education Grader、Usability Grader 和教师确认边界。
- direct 只适用于寒暄、致谢、能力说明和独立通识问答。学生/孩子/代词、记录、档案、附件、知识库、题库、报告、文档、写入、风险或复合意图必须 structured；歧义默认升级。
- renderer 对 direct 只保存答案与 executionMode，不写 thoughtSteps/contextSources/toolRuns；审计表 `ai_tasks.result_json` 持久化 executionMode、schemaApplicable 和 graderApplicable。
- 回滚：`OMNI_EDU_AI_EXECUTION_MODE=structured`（或 `full`），或 `OMNI_EDU_DISABLE_DIRECT_AI=1/true/yes`。

### 最终产品与技术基线（2026-09-16）

- 最终产品是面向 K-12 独立教师与小微教研团队的本地优先“教师个人知识库、题库与智能组卷工作台”。核心业务只有两条：教师资料入库后按蓝图出卷；学生错题照片经 OCR、校正和检索后生成针对性练习。
- 最终模块、数据模型、技术选型和上市否决项见 `docs/28_FINAL_PRODUCT_MODULE_ARCHITECTURE.md`，与旧规划冲突时以该文档为准。
- 业务真源为 SQLite + 本地文件系统；SQLite FTS5 与 LanceDB 都是可重建索引。原文件、题图和导出物不进入 SQLite BLOB。
- 文档解析使用 Docling；普通中文 OCR 默认 RapidOCR 2.x + ONNX Runtime CPU；公式、表格和复杂版面使用 PaddleOCR 3.x PP-StructureV3 + PP-FormulaNet；图像预处理使用 OpenCV。
- 本地语义检索默认使用 `bge-small-zh-v1.5` ONNX + LanceDB embedded，高质量模式可选 `bge-m3`，不同模型索引不得混用。
- 组卷先走确定性过滤、召回、去重和规则校验，AI 只负责缺口改编/生成与解析草稿；原题、改编题、AI 题、教师题必须保留来源类型和父子谱系。
- 商业生产门槛包括 30–40GB 可恢复导入、真实 OCR 评测、结构化试卷编辑器、恢复演练、无开发环境安装和关键用户路径 Electron E2E。

### 大规模代码修改长期执行契约（2026-09-16）

- 根 `AGENTS.md` 是下一轮 AI 的执行入口，`docs/28_FINAL_PRODUCT_MODULE_ARCHITECTURE.md` 是最终产品和模块真源；当前实现状态只由代码、数据库/文件 readback 和真实测试证明。
- 大规模修改必须先产出模块交付合同，包含当前能力矩阵、数据与协议变更、迁移/回滚和验收；然后按 M01–M12 依赖交付纵向切片。
- Electron main 是 SQLite、文件、权限、隐私、工具审核和审计的唯一宿主。renderer 只经 typed preload；Python Worker 不直接写业务表；FTS/LanceDB 只存可重建索引。
- 新业务按领域拆分，不继续扩大 `App.tsx`、`db.ts`、`main/index.ts`，但禁止无测试保护的全量重写。旧入口在新路径完成 readback、重启与 Electron E2E 前保留。
- 模块完成证据固定为契约、数据迁移、后端/Worker、preload、前端完整状态、用户路径、持久化和真实性分层；build、后端 smoke、proxy 或 fixture 不能互相冒充。
- 未经明确授权，不进行 commit、push、发布、外部上传、凭证修改或子 Agent 派发。

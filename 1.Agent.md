# 1.Agent.md

## 小智运行观测 UI 规则（2026-08-12）

- renderer 只允许渲染 `getAiMemoryTrace` 返回的 bounded L1 投影；即使 `listAiAgentRuns` 的私有数据包含 prompt，也不得显示、搜索、放入 DOM 或保留在 React state，必须先做字段白名单投影。
- 回归报告的状态和 gate 必须直接来自 SQLite 计算结果；`warning` / `failed` 不得在 UI 中改写为通过。
- 1366×768 下全局导航必须可滚动到所有模块；DOM 存在但被裁切到视口外不算可用入口。

## 小智 Agent Runtime 规则

- 小智每次运行必须创建 `ai_agent_runs`，并把可展示的外部执行过程写入 `ai_agent_events`；不得只把 trace 保存在 React state 或单次 IPC 返回值里。
- 任何 early return、配置失败、工具失败、模型失败和 schema 失败都必须写入明确终态，不能留下永久 `running` 状态。
- `ai_agent_events.inputSummary` / `outputSummary` 只能保存可验证摘要，不得保存隐藏推理链、敏感原文或未经老师确认的写入结果。

## 小智 Tool Calling 规则

- 模型提出的任何工具调用都必须先经过主进程 `reviewModelToolCall()` 审核；不得由 renderer 或模型输出直接触发工具执行。
- 工具审核必须覆盖工具存在性、route allowlist、contextPolicy、effect/privacy 和参数 schema。
- Tool Calling v1 只允许只读工具；写入类工具必须等待老师确认队列，不得直接落库。
- 发给模型的 tool result 必须 bounded，不能把本地长文本、附件原文或未脱敏敏感数据整包返回。

## 小智 Confirmation Queue 规则

- AI 发起的本地写入必须先创建 `ai_confirmation_items` 待确认项；确认前不得写入业务表或本地报告文件。
- Confirmation Queue 当前只开放 `create_review_report` 和 `save_exercise_set`，分别用于把 AI 复盘草稿保存到 `review_reports`、把 AI 三元题组草稿保存到 `exercise_sets`。
- 拒绝确认项不得产生任何业务写入；确认成功后必须 readback 真实业务记录并写入确认项 `result_json`。
- 已确认、已拒绝或失败的确认项不得重复执行，避免重复创建报告。

## 小智 Observability / Regression 规则

- 小智回归报告必须从真实 SQLite 运行数据生成，至少读取 `ai_agent_runs`、`ai_agent_events`、`ai_tool_runs`、`document_artifacts`、`ai_confirmation_items` 和 `ai_tasks`；不得用手写通过结论替代数据库 readback。
- route、subIntent、工具白名单、结构化回复 schema、文档导出或确认队列发生变化时，必须运行对应 smoke，并至少运行 `npm run test:ai-observability`。
- 回归报告只有在所有 release-critical gate 都没有 `failed` 时才可视为通过；存在 `warning` 时必须在交付说明里写明边界，不能包装成完全通过。
- token/context/latency 指标属于可观测预算，不是装饰数字；如果上游没有写入 usage 或 payload，只能显示 unavailable / warning，不能编造估算值。

## 定义

本文件定义 Omni-Edu Agent 的工作方式、产品边界、代码约束和长期执行规则。每轮工作开始前必须阅读；每轮工作结束后如有规则变化必须更新。

## 当前产品边界

- 项目路径：`D:\WorkProject\EduProject`
- 桌面端路径：`apps/desktop`
- 产品方向：面向 K-12 独立教师与小微教研团队的本地优先、端云混合桌面客户端。
- MVP 闭环：本地题库索引 -> 错题图片解析 -> 脱敏上云分析 -> 本地相似题召回 -> 三元题组输出。
- 不扩展成学校级平台、学生端、家长端、校园管理系统或完整 LMS。

## 每轮工作流程

1. 开始前阅读根目录四份文档：`1.Agent.md`、`2.Memory.md`、`3.Learning.md`、`4.Wiki.md`。
2. 先确认当前工作区脏改动，未知来源的修改不得回滚。
3. 涉及代码发现时优先使用 codebase-memory-mcp；不足时再用 `rg` 和文件读取。
4. 修改前说明要改哪些文件和原因。
5. 修改后运行对应检查，至少覆盖被改动前端面的 `npm run build`。
6. 结束前把关键进度、踩坑和长期规则压缩写回四份文档。
7. 提交并 push 后必须再次执行 `git status --short --branch`，确认远端同步状态和本地是否还有未提交改动；若仍有改动，必须说明是继续提交还是刻意保留。

## README 维护规则

- `README.md` 是项目对外入口，功能展示必须区分“已落地”“v1 已落地”“后续边界”，不得把规划写成已完成。
- README 中的 UI 页面、验证命令和功能清单必须与当前代码和 `2.Memory.md` 记录一致。
- 新增重大功能、页面或 smoke 命令后，若会影响新读者理解项目状态，必须同步更新 README。

## HeroUI Pro 组件规则

- 需要 HeroUI Pro 组件、模板、区块或示例时，先用官网或 MCP 识别官方组件名称。
- 没有 Personal Access Token、会员受限、MCP 不可用或资源无法获取时，不停止、不写占位组件。
- 直接使用本地源码：`D:\WorkProject\HeroUIPro\herouipro-v3\src`。
- 优先按 kebab-case 组件名搜索 `src\components`，例如 `chain-of-thought`、`chat-attachment`、`chat-conversation`、`prompt-input`。
- 复制或复用组件时必须连同依赖组件、CSS、utils 和必要 npm 依赖一起处理。
- 不能留下“以后接真实组件”的坑；当前项目里要能真实 import、build、render。

## AI 控制台规则

- AI 控制台的思考过程必须显示在 AI 回复内部，不放独立右侧栏。
- AI 回复下方不再渲染“上下文来源 / 上下文工具”折叠组件；真实工具轨迹应压缩进回复内部的可验证 Agent loop 摘要。
- 思考过程必须来自主进程真实 harness trace，不能由前端按模板事后拼装；问候类输入不能铺开无关上下文。
- AI 控制台对话、消息和文件夹分类必须持久化到本地 SQLite，不能只保存在 React state 或临时上下文里。
- 左侧对话栏应参考 Codex / ChatGPT 的会话库设计，支持新建对话、新建文件夹、按文件夹分组、拖拽归类、右键重命名和归档。
- 可展示的是用户可验证的处理过程摘要：任务识别、上下文读取、工具调用、证据边界、产物判断。
- 不展示或编造模型内部隐式推理；如果没有真实 AI 返回，不伪造最终答案。
- 附件原始文件不上传；AI 上下文只纳入附件数量和元数据。
- PDF、Word、Markdown 等产物入口在 AI 回复中显示为“PDF 文件 / Word 文件 / Markdown 文件”等文字入口，点击后右侧展开预览，对话区左收。
- 右侧文档预览面板与中间对话区之间必须提供可拖拽分界线，支持鼠标左右拖动调整宽度，至少保留键盘方向键可访问调整。
- Markdown 渲染区域必须隔离全局表单样式；任务列表中的 `input[type="checkbox"]` 不得继承普通输入框的宽高、padding、min-height 等样式，避免生成文档中出现异常大空框。
- 已归档的 AI 对话和文件夹不在中控台左侧常规列表显示，必须能在设置页的归档栏目找到。

## C04 Book Workspace 规则

- DeepTutor Book Engine/Compiler/KB health 只复用状态机、逐块持久化、来源 fingerprint 和漂移定位思想；Omni-Edu 以 SQLite 结构化“专题讲义/单元备课包”承载，不复制文件存储或通用 LMS。
- `inspect_teaching_book` 是 local-only/read-only；必须显式讲义请求才允许 `teaching_book` context，严禁自动把学生数据、知识库全文或原始路径带入。
- page/block 重生成必须带 version，来源漂移只能定位受影响实体，禁止整本盲目重建；没有 DeepSeek live key 时不得声称 provider 已验收。
- 小智不能用一条固定流水线处理所有问题；先识别任务路由，再按路由装配最小必要上下文。
- 小智遇到“小A/某学生”等明确学生引用时，应先尝试本地学生引用解析，不能要求老师手动切换左侧模块后才查询。
- 工具只有真实执行后才能显示为 `used`；工具已注册或已准备不能冒充已调用。
- 学生判断必须区分事实、推断和未知；AI 不得自动形成永久学生标签。
- 读取与草稿可以按权限执行；任何本地写入必须先给老师预览并确认。
- AI 回复应先通过版本化结构契约校验，前端不得只靠关键词猜测产物类型或执行结果。
- 小智 route / subIntent / 工具白名单 / 安全边界变更必须同步更新 `apps/desktop/src/main/ai-harness/eval-cases.ts`，并保持当前 100 条 eval 全部通过；新增能力不得减少既有门禁。
- DeepSeek 结构化回复必须使用 `xiazhi.reply.v2` 本地校验；允许安全提取代码围栏或少量外壳文本中的单个 JSON object，但不修补缺失语义；校验失败最多触发两次受控 repair，每次固定无工具、`temperature=0`、`json_object`、8,000 tokens，仍失败不能当作成功答案展示。
- `xiazhi.reply.v2` 通过后仍必须经过 Education Grader；标签化语言、隐私泄露、无证据学生推断、写入缺确认等 critical 问题不得作为成功回复展示。
- `xiazhi.reply.v2` 和 Education Grader 通过后仍必须经过 Usability Grader；“请手动切换学生数据/当前普通问答模式/无法自动切换”等退化话术属于 critical，不得作为成功回复展示。
- 小智回复必须按 route 控制长度和结构：普通问答/工作台帮助优先短答，学生诊断/错因/题组/报告只保留对老师有用的依据、教学判断、下一步、确认和产物入口，不得机械展开所有 schema 字段。
- 教师可用性代表性样本集必须维护在 `apps/desktop/src/main/ai-harness/usability-eval-cases.ts`；新增或改变 usability 规则时必须运行 `npm run test:ai-usability`，并确认 Observability 回归报告仍包含 `usability_quality_gate`。
- 真实教师人工评分口径以 `docs/23_XIAOZHI_USABILITY_EVAL_RUBRIC.md` 为准；proxy 样本全通过不能替代“教师人工评分平均 >= 4/5”的最终验收。
- 真实教师人工评分必须进入 `ai_usability_reviews` 并可通过 preload/IPC 回读；只在脚本终端打印或只保存在临时变量中，不能算作真实评分录入。
- 人工评分 UI、CSV 导入和失败样本回放必须复用 `createAiUsabilityReview` / `listAiUsabilityReviews` / `getAiUsabilityReviewSummary`，不得在 renderer 另建临时评分口径；相关 UI 改动必须跑 `npm run test:ai-human-review-ui`。
- 失败样本 before/after 回放实验必须进入 `ai_usability_replay_experiments` 并可通过 preload/IPC 回读；不得只在 renderer state 中显示“已改善”。相关改动必须跑 `npm run test:ai-replay`、`npm run test:ai-human-review-ui` 和 `npm run test:ai-observability`。
- 模型 grader 样本必须进入 `ai_model_grades` 并可通过 preload/IPC 回读；deterministic proxy 与未来真实 `llm_judge` 必须用 `graderMode` 区分，不得把 proxy 门禁通过写成真实 LLM-as-judge 已通过。相关改动必须跑 `npm run test:ai-model-grader` 和 `npm run test:ai-observability`。
- live evidence binding 必须把同一次小智输出的 `runId`、`sessionId`、`promptVersion`、`totalTokens` 尽量贯穿 after 人工评分、before/after replay、模型 grader、telemetry 和 regression gate；新增或修改该链路必须跑 `npm run test:ai-live-evidence` 和 `npm run test:ai-observability`。
- `npm run test:ai-live-evidence` 只能证明 seeded/live-ready 工程证据链路，不等于真实 DeepSeek API、外部真实老师样本或真实 LLM-as-judge 已通过。
- 年级适切评分属于小智教育质量门禁的一部分；新增或修改评分维度时必须保留 evidence、actionability、safety、gradeAppropriateness、concision、teacherControl 的可回读分项，不能只保存一个总分。
- DeepSeek live 可用性回放必须通过 `DEEPSEEK_API_KEY` 显式执行；没有 API Key 时只能标记 skipped，不得写成 live 已通过。
- AI 页提示词快捷入口应优先覆盖老师真实工作流，如学生诊断、三元题组、家长沟通草稿、档案缺口、课堂导入和确认队列处理；不得只展示能力宣传型问题。
- Confirmation Queue UI 默认只展示最近关键待确认项，剩余项可折叠提示，避免打断老师当前对话；折叠不等于自动确认或忽略。
- 三元题组相关任务必须优先尝试本地题库相似题召回；生成题必须标注为 generated，不得冒充本地题库命中。
- AI 保存三元题组必须走 `save_exercise_set` 确认项；老师确认前不得写入 `exercise_sets`。
- 错题图片原始文件必须留在本地托管数据目录内，不得自动上传给云端模型；云端只允许接收脱敏后的题目文本和必要摘要。
- 错题图片没有 OCR/解析文本时必须进入 `needs_ocr` 状态，不能伪装成已分析成功。
- 老师修正 OCR/解析文本后，必须重新生成脱敏文本和脱敏记录；不得复用旧脱敏结果。
- 知识库 chunk 必须带可审查元数据和证据强度；引用时区分 `direct`、`indirect`、`background`，不能把弱证据写成直接证据。
- 知识图谱节点/边只作为关系背景，除非同时有明确 chunk/sourceId 支撑，否则不得冒充正文直接证据。
- 知识库 chunk 检测到个人信息时，发给模型的工具结果必须隐藏正文预览，只返回必要元数据和边界提示。
- 小智后续进化开发以 `docs/22_XIAOZHI_EDU_AI_HARNESS_PLAN.md` 为总路线；每次偏离路线必须先说明原因，并把新口径同步回该文档。
- 小智 PDF/Word/Markdown 产物只有在主进程写出真实文件、记录 `document_artifacts`、生成 `filePath/fileSize/contentHash/status` 并 readback 后，才能显示为已导出；导出失败不得展示成功状态。
- DeepTutor 融合开发以 `docs/24_DEEPTUTOR_OMNI_EDU_INTEGRATION_DESIGN.md` 和 `docs/25_DEEPTUTOR_FEATURE_INVENTORY_AND_PRODUCTION_MATRIX.md` 为共同实施基线；Electron main 必须继续作为数据、权限、隐私、工具审核、确认和审计的唯一宿主控制面。
- DeepTutor sidecar 只能执行允许的 Capability 和发起 HostToolProxy 请求，不得直接读取 Omni-Edu SQLite、学生目录、原始附件或执行业务写入。
- DeepTutor 的 `THINKING` / 模型草稿事件不得原样展示或持久化；只能通过安全事件适配器转成可验证的 stage、tool、source、budget 和 termination 摘要。
- sidecar event 进入 SQLite 前必须经过 turn 级串行队列、连续序列校验和 bounded/redaction；sidecar sequence 只能作为诊断字段，不能覆盖宿主审计序列。
- DeepTutor native `tool_calls` 只有在上游 AgentLoop 真实解析、经过 dispatcher、HostToolProxy 宿主审核并落成 tool/observation 证据后才算工具调用；`hostToolAuto` 只可作为 parity fixture，不能冒充真实模型能力。
- DeepSeek live tool loop 当前最多 3 轮/8 次工具调用；每一轮都必须重新经过主进程工具审核，达到预算后写入可验证 guardrail 并强制要求模型只返回结构化答案，禁止无限工具循环。
- DeepTutor ModelProxy 必须由 Electron main 持有 provider 凭证；sidecar 只能发送 bounded request 并接收 bounded response/error，缺少凭证、超时或 provider 异常必须落成 failed run，不得继续发 succeeded done。
- DeepTutor 上游代码必须固定 commit，保留 Apache-2.0 LICENSE、适用的 THIRD_PARTY_NOTICES 和修改说明；不得在运行时依赖 `D:\WorkProject\DeepTutor` 绝对路径。
- 当前固定 vendor 位于 `python/vendor/deeptutor`；允许直接复用上游 `AgentLoop`、`Capability`、`StreamBus` 和 `CapabilityRegistry`，适配代码放在 bridge/host proxy，不在 vendor 文件内偷偷改写行为。Fake model 只能用于 deterministic smoke，必须与真实模型验收分开。
- DeepTutor 台账中的模块只有在可运行代码、配置、部署说明、关键测试、性能/安全证据全部满足统一 DoD 后才能标记完成；设计稿、mock、skipped live 或首个进度事件不得冒充功能已上线或深任务已在 2 秒内完成。

## 禁止事项

### E07/E08 新增禁止事项

- 不得把 Deep Research dry-run、来源标题拼接或 Mermaid 字符串生成伪装成联网研究、已核验引用或可执行图表。
- 不得为了命中“可视化”关键词覆盖显式讲义/题本路由；显式工作区意图优先，新增关键词必须加入 router eval 防回归。
- 研究和可视化工具不得携带学生上下文、原始附件、外部 URL 或写入副作用；结果必须标注证据边界、未知项和教师复核要求。

### C05 高级 block 规则

- 讲义编译器不得执行 `interactive`、`animation`、HTML、iframe、SVG event handler、JavaScript 或本地文件路径；只能安全降级并提示受控查看器/教师复核。
- `code` block 只允许 bounded fenced text，禁止把“代码可展示”解释为“代码已运行或验证”。
- 新 block 必须同时具备共享类型、运行时 renderer、异常 fallback、对抗性 smoke 和回归记录，不能只增加类型声明。

### E15 复习调度规则

- 复习调度必须是主进程纯函数，输入仅限显式知识点与正确/错误证据；不得从自由文本推断掌握度或在 renderer 侧计算。
- 间隔序列、连续正确/错误回退和优先级必须复用固定 scheduler；epoch due 判断不依赖机器本地时区，timezone 只作为展示元数据。
- `get_review_queue` 只读、local-only、bounded，不写 `learning_records`，不返回原始题干/答案；任何掌握度写回仍走教师确认。
- 普通记录标题、摘要或自由文本不得创建知识点；只有显式结构化 `metadata.knowledgePoint` 或受控 mastery 标签才可进入 scheduler。
- quiz/grade 写回必须保留 snapshot 中知识点的显式 `knowledgeType`；禁止用通用 concept 默认值把 procedure/memory 重新分类，否则会绕过 `mastery_assess` gate。
- 新增 scheduler 或 review queue 必须同时更新 route/subIntent/tool eval、确定性/时区/恶意输入 smoke、native tool log 和四份协作文档。
- E15 提醒只允许在应用打开时从 SQLite `learning_records` 重算 bounded 摘要；不得新增 reminder 队列表或后台 cron 真源。renderer 只能展示/跳转，不能确认、删除或改写复习证据。

- 禁止使用不存在的包别名，例如未配置或未安装的 `@heroui-pro/*`。
- 禁止只做结构相似的自制占位替代本地 HeroUI Pro 源码。
- 禁止硬编码 API Key。
- 禁止在 renderer 直接访问 Node API。
- 禁止把临时猜测写成已确认事实。

### 2026-08-11 增量约束：普通问答入口与错误证据

- 普通 AI 页面提交必须进入 `runDeepTutorConsole`，由 DeepTutor AgentLoop 统一承载；页面不得再把旧的固定 `runDeepSeek` 流程当作默认入口。
- console 入口失败时必须优先展示 SQLite 事件中最具体的失败原因（例如 `MISSING_MODEL_CREDENTIAL`），不能被通用 `done` 摘要覆盖；没有 API Key 时必须可行动地提示配置入口。
- 入口切换的本地验收只能证明 sidecar、路由、结构化解析和 fail-closed；没有 live provider、真实工具执行或多 Capability 证据时，不得标成生产完成。

### 2026-08-11 增量约束：可暂停 AgentLoop 与用户输入

- `ask_user` 是宿主拥有的本地工具；sidecar 只能发出 `xiazhi.user_input.request.v1`，不得自行假设答案或直接读取 renderer/SQLite。
- 等待老师输入时，运行必须进入 `waiting_input`，并持久化 `ai_capability_checkpoints`；回答只允许通过主进程 IPC 提交，且必须有长度、数量、turn 归属校验。
- 用户输入结果必须是 `xiazhi.user_input.result.v1`，状态只允许 `answered/cancelled/expired`；重复提交必须幂等拒绝，错误结果不得唤醒等待中的 AgentLoop。
- 应用重启后，`waiting_input` checkpoint 必须可由主进程回读并展示；回答先原子 claim checkpoint，再由安全恢复子 run 接管并保留 `parent_run_id`，不能伪装成原 sidecar Promise 已跨进程恢复。
- UI 可以显示补充信息卡片，但只能显示 prompt/questions，不得显示 raw hidden reasoning；提交后重新进入 `running`，最终以真实 `done` 事件收敛。
- DeepTutor capability 的上游结果必须经过 main-process schema/confirmation 边界；dry-run 只能证明 vendored pipeline 与持久化链路，不得当作 live provider 或生产质量证据。
- Mastery/掌握度只能基于明确的知识点与正误证据计算；自由文本记录必须标记为 unknown，不能为了生成百分比而猜测掌握度，也不能让 DeepTutor LearningStore 成为 Omni 学生数据的第二真源。
- `mastery_status` 是 Mastery 第一条真实 HostTool 边界：只读、local_only、仅允许 `student_diagnosis/practice_design`，必须复用 `mastery-snapshot.ts`，输出不得包含原始学习记录正文。
- `mastery_status` 的审计事件只能记录 bounded result keys 与 `rawRecordsIncluded` 等安全摘要；不能把完整题干、答案或学生记录正文写入 sidecar/事件流。
- Mastery 互动题必须走 `ai_mastery_questions` 私有表：公开 checkpoint 只允许 questionId/stem/options，expectedAnswer 永远不能进入 renderer、checkpoint 或 sidecar event；评分必须校验已提交答案与 question row 一致，并幂等阻断重复写回。
- `mastery_quiz.questionCount` 最多 5 道，按 SQLite 创建顺序形成单一连续队列；同一学生存在 pending/answered 题时不得创建第二组，评分必须逐题推进，重启后仍按 oldest-first 恢复。
- `mastery_assess` 仅允许 concept/design 知识点，procedure/memory 必须走 quiz/grade；`mastery_build` 必须全量校验模块和知识点，禁止静默丢弃坏节点，并通过 `mastery:getPath` 做主进程回读。
- `mastery_assess` 与 `mastery_build` 只允许生成 `save_mastery_state` 待确认变更；任何 `mastery_attempt` 或 `ai_mastery_paths` 写入都必须经过教师确认并以主进程 SQLite readback 为准。能力验收运行 `npm run test:deeptutor-capability-evals`，不得把无 API Key 的 dry-run 当成 live 通过。
- sidecar 异常退出或 turn 超时必须 fail-closed：活动 run 写入失败/超时 guardrail 并完成，挂起 checkpoint 取消，内存 binding 清理；正常 stop 不得重复执行 crash recovery。恢复验收使用 `npm run test:deeptutor-recovery`，性能 dry-run 不得表述为真实 provider SLO。
- 掌握策略必须区分 `evidenceKind=quiz|assess|unknown`：concept/design 只能由教师确认的 assess 正确证据通过定性门禁；`mastery_status` 对外只暴露 bounded policy 元数据，不得把完整 due-review 或原始学习记录写入事件流。
- Mastery 待作答状态必须以 SQLite `ai_mastery_questions` 为真源：`mastery_status` 只公开最近一题的 bounded projection（不得含 `expectedAnswer`/答案），重启后仍可恢复 `pending/answered`；`mastery_grade.questionId` 可省略但只能回退到当前绑定学生，跨学生题目必须在状态校验前 fail-closed。验收使用 `npm run test:deeptutor-mastery-pending`。
- E16 学情分析必须是只读、可重算的统计层：日期使用 UTC 日历边界且跨度不超过 366 天，跨学生请求必须 blocked，空数据必须显式 unknown；报告窗口和来源 ID 通过安全元数据绑定 confirmation，不能用默认日期覆盖老师指定范围。
- E14 错因分类必须优先复用 DeepTutor 四类 taxonomy，但证据不足只能输出 `unknown`；空答案、粗心、概念/理解/应用信号不得被同一规则吞并，应用错误和未知需标记教师复核。分类工具只读、结果 bounded、重算指纹不得包含原始正文，不能自动写入学生档案。
- E17 Vision Solver 只能生成教师预览草稿：默认不上传原图或返回本地路径；GeoGebra 命令必须过白名单、脚本/URL/隐私文本阻断，拒绝命令不得原样回显；无图必须正常结束为 `no_image`，不伪造几何事实。
- R13 附件探索必须复用 DeepTutor `source_manifest/read_source` 的按需原则：只允许当前绑定学生记录上的 attachment handle，优先读取脱敏 OCR 片段；不得读取任意路径、原始文件或未授权 attachmentId。无学生、无附件、无匹配文本和未解析 OCR 均须 bounded blocked/unknown。验收运行 `npm run test:deeptutor-attached-source`。
- C01 教师备课本必须使用本地 SQLite 单一真源；笔记与备课本删除只能软删除并可恢复，更新必须携带版本号，版本冲突不得覆盖老师的新修改。
- C02 备课本分析必须按需触发：只有明确提到笔记/备课本/notebook 才能启用 `teacher_notebook` context；目录与正文读取必须 bounded，文本统一本地脱敏，摘要只能是教师复核草稿，不自动写回。
- C03 Question Notebook 必须是现有 `question_bank_items` 之上的索引层，不得复制第二份题目正文；收藏、分类和历史引用修改要经过 main/preload、版本校验和安全回读。题目收藏请求只允许 `question_notebook` context 与 `search_question_notebook`，不得顺带读取学生或教师知识库上下文；模型只能读，不能直接修改收藏/分类。

### 2026-08-12 增量约束：Console 结构化回复

- DeepTutor ModelProxy 在工具回合使用 `responseFormat=none`，无工具的最终回合必须使用 `json_object`；最终文本仍需经过 `xiazhi.reply.v2` 本地校验，失败最多执行一次主进程受控 repair。repair 仍失败必须把原本已 succeeded 的 sidecar run 结算为 failed，不能把普通 prose 当作成功答案。
- Agent 轨迹摘要与最终结构化结果必须使用不同的有界长度：普通事件 `detail` 保持 8,000 字符上限，`result` 可保留至 64,000 字符。禁止先按轨迹上限截断最终 JSON 再交给 parser；超过最终上限仍须 fail-closed，不能拼接或猜测缺失字段。

### 2026-08-12 增量约束：前端闭环证据

- 前端完成必须从用户点击经过 typed preload/main 到 SQLite 或真实文件回读；fixture 可用现有 IPC/隔离 SQLite 准备，但必须在验收矩阵中标明，不能冒充用户录入步骤。
- renderer 的可编辑预览如果没有对应的更新/覆盖确认载荷契约，必须明确标注“仅本地预览”，不得让确认按钮暗示编辑值会保存。
- 1366×768 与 1920×1080 截图应持久化到 Git 忽略的测试产物目录，测试结束不得随临时数据库一并删除。
- 教师备课本 renderer 必须复用现有 typed preload/SQLite CRUD；笔记记录当前没有 restore IPC，界面只能把已删除记录作为审计只读项展示，不得伪装成可恢复能力。
### C05 Renderer 规则

- Markdown 预览编译器只能产生 draft，不得绕过确认队列写入 `document_artifacts` 或发布；未知 block 类型必须 fallback，不能执行任意 payload code。
- C06 章节规划只允许 bounded source exploration + draft：必须显式 bookId，最多读取受控知识片段，chapter/page/block 候选必须教师确认后才能落库。
- C07 来源健康刷新必须保留原始 fingerprint 基线，只持久化 source status 与按 source ref 的局部失效队列；来源恢复可关闭队列但不得自动改写正文或整本重生成，必须显式 bookId 且不得带学生 context。
- C08 CoWriter 只能提交 patch 草稿：必须保留 before/after、baseVersion、reason 和历史状态；apply/undo 需显式教师确认与版本条件更新，老师新修改后旧 patch 必须拒绝，不能覆盖正文。
- C09 automark/react-edit 只能提交明确选区 patch：必须带 block 版本、起止位置、原文和候选替换文本；保存选区哈希与 bounded 草稿事件，apply 时版本或选区漂移均拒绝，不能自动改全文或伪造真实 provider 流。
- C10 Persona 只能改变教学语气和组织策略，不能改变小智的统一身份、route 工具 allowlist、context policy、隐私边界或确认队列；未知 profile 必须回退 teacher。
- C11 L1 记忆只认 Omni SQLite 的 `ai_agent_runs`、`ai_agent_events`、`ai_tool_runs` 为唯一真源；`inspect_memory_trace`/IPC 只能返回 bounded 阶段、状态、工具名和证据键，禁止 prompt 原文、事件 detail、学生正文和隐藏推理；显式缺失 run 必须 fail-closed，L2/L3 不得冒充已完成。
- C12 L2 记忆必须按 surface 保存为可编辑条目和修订历史；候选摘要只能由本地 run/event 证据派生，AI 不得直接写正式记忆；教师采纳/编辑必须经过 typed IPC、证据引用校验和版本号乐观锁，禁止 raw prompt、hidden reasoning、学生正文及绝对化掌握结论进入条目。
- C13 L3 只能综合 active L2 条目到 recent/profile/scope/preferences 槽位；候选必须只读且等待教师采纳，正式 L3 写入仍需来源 surface allowlist、文本边界、readback 和版本锁；不得把 L3 综合误当学生事实或自动升级上下文权限。
- C14 记忆证据图只能是 bounded、只读的 `L3 -> L2 -> run/event` 投影；节点只保留短标签/状态，禁止复制原文、prompt、detail 或 hidden reasoning；图工具必须继续受 `memory_synthesis` allowlist 与节点/边上限约束。
- C15 记忆治理报告必须显式声明 AI 不可写、active-only 图查询和悬空 evidence ref 数；停用/删除不能重新进入 L3 综合，任何“治理通过”都必须以 SQLite 回读为证据。
- C03 题本 renderer 只能展示 `question_bank_items` 的 canonical 正文与 overlay 元数据；搜索/分类/收藏必须走 preload→main，收藏带版本锁，分类删除为软删除，冲突不得覆盖老师新修改。题本 UI 的空结果和错误必须给出可恢复提示，不能伪造题目或批量迁移已完成。
- 教师从题本 UI 录入题目时只允许 `local_bank` 或 `teacher_resource` 来源；`generated` 必须来自可审计的小智产物链。操作错误不得清空已经 readback 的题目列表，空题干和 stale version 必须零写入并保留可见错误。
- 记忆治理 renderer 必须通过现有 `aiMemory:*`/`aiMemoryL3:*` typed preload 工作：候选生成不得写库，采纳/修订/停用/恢复/删除必须以 SQLite readback 为成功条件；版本冲突要阻止覆盖并保留上次成功读取的数据。deleted L2 在没有专用恢复契约时只做只读审计，不得伪造恢复按钮。
- 附件 Electron 自动化不得再用 SQL 直插冒充用户导入。受控 dialog 适配只能在 `app.isPackaged=false` 且显式 E2E 开关下启用，输入必须是有界绝对路径队列；生产/打包路径继续调用原生 dialog。验收必须从导入按钮开始，回读复制目标、大小、SHA-256 和 SQLite，取消必须零写入。
- C05 讲义页面只能通过 `teachingBook:*` preload 获取主进程编译的 Markdown preview；预览 `writesFile=false`，未知 block fallback，stale/missing source 必须警告。只有教师明确点击导出后才写 `document_artifacts`，并以 status/filePath/fileSize/contentHash readback 作为成功条件。
- PDF/DOCX 基础导出必须保留 Unicode 文本与标题/列表/代码层级，并经真实文件字节和 `document_artifacts` readback 验证；复杂分页、图片、动画或 Office 版式不得凭 smoke 伪称高保真完成。
- 本地数据备份必须先复制到源数据目录之外，再生成 `omni-edu-backup-manifest.v1.json` 并逐文件 SHA-256 回读；缺失、篡改、额外文件和非法清单都必须 fail-closed。验证成功不等于自动恢复成功，不能在没有恢复演练时宣称灾备完成。
- R10 预算与续写：主进程必须把 `maxEvents` 归一化到 `1..256`，按 turn 串行计数 sidecar 事件；超限写入 `budget_exhausted` guardrail、取消 sidecar 并删除 binding。未达续写上限时只能发放一次性 continuation token，run 进入 `blocked` 并通过 `parent_run_id` 建立子 run lineage；token 必须 bounded、可过期、单次消费，预算增加必须先创建 `budget_approval` checkpoint 并由老师批准，SQLite 抢占必须依据变更行数保证并发单赢家，renderer 只能看到可继续摘要，不能拿到 request/system prompt/工具 schema；AI 对话必须提供真实继续与预算批准入口，禁止把受限续写描述成无限恢复或完整 settlement。
- R11 运行变更：retry/branch/regenerate 必须从本地私有 request snapshot 重建，不得让 renderer 传入任意系统提示或工具定义；retry 只允许失败/阻断/取消源运行，成功源使用 branch/regenerate。子 run 必须持久化 `parent_run_id`，action 使用 `(source_run_id, action, idempotency_key)` 唯一约束与主进程串行队列；重复变更只能复用已有子 run，不能重复执行写工具。UI 入口必须走 typed preload，不能把 raw request snapshot 回显给教师。
- R05 按需工具披露：`router.allowedTools` 是不可扩大的硬总许可；模型首轮只可见 `load_tools`、受 route 允许的 `ask_user/resolve_student_reference`，工具族 schema 必须由主进程审核后下一轮加载。`load_tools` 只返回 schema 与 `toolsExecuted=false`，不得执行业务工具；Python sidecar 不得把完整 catalog 直接暴露给 AgentLoop，越权工具族必须 blocked。
- 前端接线验收：后端已有能力必须在 [docs/26_FRONTEND_E2E_COVERAGE_MATRIX.md](docs/26_FRONTEND_E2E_COVERAGE_MATRIX.md) 中逐项标记 backend/frontend/e2e/live，renderer 不得继续显示“未接入”来掩盖已有入口；业务规则仍归主进程和 typed preload，不能在 renderer 复制写入或脱敏逻辑。
- 教师主闭环的 UI 必须显式呈现 loading、empty、success、failed、needs_ocr、needs_review 和 no-key 状态；Electron smoke 至少覆盖 1366×768 与 1920×1080。单独的主进程 smoke、proxy 或 deterministic 结果不得冒充前端 E2E 或真实模型验收。

### 2026-08-12 增量约束：专题讲义前端

- `TeachingBookWorkspace` 只能通过 typed preload 复用现有 book/chapter/page/block/source/health/patch 能力；直接编辑必须携带版本，协作 patch 草稿必须零正文写入，只有教师点击应用后才变更内容。
- 选区 patch 的 `selectedText` 必须从当前 SQLite readback 正文按边界计算，不能信任前端自由输入；漂移或版本冲突必须失败关闭。
- 安全预览保持 `writesFile=false`；Markdown 导出先取得本地 data root 并写入受控 `exports/ai-artifacts`，回读真实路径、大小与 SHA-256。归档无 restore IPC 时不得伪造恢复按钮。

### 2026-08-12 增量约束：结构化结果预算

- DeepTutor AgentLoop 的模型输出预算不得低于完整 `xiazhi.reply.v2` 所需的受控上限；当前统一为 8,000 tokens。sidecar 的最终 `result` 必须保留最多 64,000 字符并与 Electron main 的最终事件上限一致，普通 trace 仍保持较小边界。禁止在 provider 与 parser 之间用 512-token 或 20,000-character 隐式切片把合法 JSON 人为截断。

### 2026-08-12 增量约束：AI 文档产物前端验收

- Markdown/PDF/DOCX 必须复用同一产物面板和既有 `exportDocumentArtifact` 链路；空正文必须在面板内显示失败并保持文件与 `document_artifacts` 零写入，成功则验证真实文件、SHA-256、SQLite 与重启 readback。
- PDF/DOCX 验收必须包含中文/Unicode；DOCX 长文样本须以实际字符长度断言，不得用“行数很多”替代超长正文证据，也不得为通过测试降低阈值。

### 2026-08-12 增量约束：学习路径前端

- 学习路径 renderer 只能按当前选择学生调用既有 `getAiMasteryPath`，切换学生时必须先清空旧路径再读取，禁止短暂显示上一名学生的数据。
- `ai_mastery_paths` 只证明教师确认的路径顺序、模式和版本，不证明掌握百分比；UI 不得从模块数量或知识点类型推导学习程度。
- 路径修改继续由小智生成草稿并进入教师确认，学习路径页面不得新增直接写入或绕过确认的 renderer 逻辑。

### 2026-08-12 增量约束：全局搜索前端

- “搜索”页面必须调用既有 `searchAll → search:all → store.search`，不得把当前学生的局部时间线筛选伪装成全局检索。
- 全局搜索只读取本地 SQLite，不调用模型；renderer 输入需有界，空查询、无命中、IPC 失败必须显示不同状态。
- 点击学习记录结果必须选择其 `studentId` 并用既有时间线筛选定位记录；不得在搜索页复制第二套学习记录编辑或写入逻辑。

### 2026-08-12 增量约束：已确认题组回读

- 已确认题组必须复用 `listExerciseSets → exerciseSets:list → SQLite`，并入错题主流程作为教师确认后的只读第 5 步；不得为展示再建题组表、重复工作区或题本副本。
- 学生切换时必须先清空旧题组，loading/empty/error/content 分离；刷新只能重新读取，不能产生写入。每题必须把 `local_bank/teacher_resource/generated` 转成教师可理解且不丢失语义的来源标签。
- 正式题组页面不得暗示可直接编辑；当前 confirmation 不接受覆盖载荷，修改仍须重新生成草稿并再次经过教师确认。

### 2026-08-12 增量约束：复习提醒前端

- “今日”页复习提醒只能调用 `getReviewReminder → review:getReminder`，从本地学习记录实时派生；刷新不得写学习记录或保存第二份提醒队列。
- UI 必须区分 no-student/loading/error/clear/upcoming/due。IPC 失败不得被吞成永久“正在计算”；学生切换时先清空上一名学生结果。
- renderer 只展示知识点、类型、模块和调度时间；不得显示或缓存学习记录正文、答案、附件路径。查看证据必须进入既有学生工作区，不复制记录详情页面。

### 2026-08-12 增量约束：模型 JSON 语法修复

- `response_format=json_object` 只是 provider 提示，不能替代本地校验；对完整对象外壳可执行有界语法修复，但不得补造 `xiazhi.reply.v2` 字段或放宽 route、证据、教育质量与教师确认规则。
- 本地语法修复输入最多 64,000 字符，且必须以 `{` 开始、以 `}` 结束；截断 JSON、非对象输出或修复后契约不完整继续 fail-closed。
- 新增 parser 依赖必须锁定精确版本、接受构建与结构化回复回归，并单独说明现存依赖审计告警，不能借 bug 修复升级无关依赖。

### 2026-08-12 增量约束：学生档案生命周期

- 学生档案新建、编辑、打开目录、导出和归档必须集中在独立生命周期组件；归档为不可恢复语义时，UI 只能只读展示，不能伪造恢复入口。
- 归档必须二次确认，取消保持零写入；导出取消保持零文件，成功必须回读 `metadata.json`、真实记录与文件数。归档学生不得继续进入其他可写业务视图。

### 2026-08-12 增量约束：完整数据备份前端

- 设置页完整备份只能复用 `exportDataRoot/verifyDataBackup` typed preload；renderer 不复制文件、不计算第二套 manifest，也不新增恢复语义。
- 导出目标必须位于当前数据目录之外，取消选择目录必须零写入；校验必须分别报告缺失、被修改和清单外文件，成功也只能称为“校验通过”，不能称为已恢复或灾备完成。
- Electron E2E 可在未打包且显式 `OMNI_EDU_E2E_DIALOG_MODE=1` 时注入系统目录选择结果；生产/打包应用仍调用原生目录选择。主验收必须从 UI 点击后核对真实文件、manifest、SHA-256、篡改、取消与跨重启复验。

### 2026-08-12 增量约束：AI 对话库前端

- AI 对话文件夹、会话、移动、重命名和归档只能调用既有 `aiConversations:*` typed preload；renderer 不维护第二份持久化真源，操作完成后必须使用 IPC 返回的完整 workspace 更新界面。
- 空文件夹名和空重命名必须在前端拦截且零写入；移动、重命名与归档需要局部 working/success/error 状态。归档必须二次确认，取消零写入，文案明确“隐藏但不删除消息”。
- 当前没有对话/文件夹 restore IPC，因此设置页只能审计归档项，不得伪造恢复按钮。主 Electron 验收必须覆盖真实点击/右键/拖放、SQLite readback、级联归档、重启与双视口。
# 小智 JSON 模式恢复规则（2026-08-12）

- DeepSeek JSON 模式的空 `content` 与 `finish_reason=length` 必须在 Electron main 模型代理边界识别；仅允许一次无工具、`temperature=0`、`json_object` 的受控重试。
- 重试不得补造事实、证据或工具结果；恢复后仍必须通过完整 `xiazhi.reply.v2`、route、教育质量、可用性与写入确认校验。
- 连续空响应必须明确失败；截断 JSON 必须报告为“不完整/可能被长度上限截断”，不得统一退化成无法诊断的“没有合法 JSON”。
- sidecar 只记录可公开诊断字段：`finishReason`、`contentPresent`、`jsonRecoveryAttempted`、`jsonRecoveryReason`，不得持久化原始模型隐式推理。

### 2026-08-12 增量约束：本地复盘报告前端

- 复盘报告 renderer 只能调用既有 `generateReview`、`updateReport` 和 `listReports` typed preload；不得在 renderer 新建文件、直连 SQLite 或维护第二份持久化真源。
- 日期范围非法、未选择学生或正文为空时必须前端拦截并保持零写入；学生切换必须清空上一名学生的活动报告，历史列表只显示当前学生报告。
- `sourceRecordIds`、质量检查和父摘要必须以主进程/SQLite 返回值为准，不得由 UI 推测通过；证据缺失时如实显示缺口。
- 教师主动“生成报告”会立即创建 SQLite 草稿及初始 Markdown 快照，不属于模型自动写入确认队列；后续“保存修改”当前只更新 SQLite，不重写初始快照。UI 不得把该快照称为最终文件，正式交付必须走既有文档导出。

### 2026-08-12 增量约束：小智质量评审前端

- 设置页质量评审只能复用既有 typed preload、main IPC 与 SQLite；renderer 不重算 summary、score delta 或 Grader 结论。
- 单条表单和 CSV/TSV 必须先完整校验；预检失败保持零写入。逐行 IPC 导入不具备事务原子性，运行中失败必须明确提示按 `sampleId` 核对，不能宣称全部回滚。
- 模型评审必须展示真实 `graderMode`；deterministic proxy 不得冒充真实 `llm_judge`。失败样本“回放到 AI”只预填当前输入框，不自动发起模型调用。
- 当前 AI 输入使用稳定的 `ai-prompt-input` 验收标识，不能继续依赖已隐藏的旧输入框选择器。

### 2026-08-12 增量约束：DeepTutor 控制台故障定位

- 当前 AI 中控台用户路径调用 `runDeepTutorConsole`；旧 `runDeepSeek` smoke 不能证明当前控制台链路可用。
- 修改或诊断 Capability/route 时，必须同时核对 `CAPABILITY_TO_ROUTES` 与控制台请求中的 `capability`，不能只看 router route 或 HTTP 200。
- 同步控制台遇到 `waiting_input` 时必须有明确的用户输入交接或可解释失败；不能让调用一直等待到 wall-timeout 后再返回空结果。

### 2026-08-12 增量约束：小智 Agent Harness v1

- AI 中控台必须先由 route/subIntent 选择 capability；映射单一事实来源是 `agent-harness-profile.ts`，HostToolProxy 不得维护另一份漂移表。
- 中控台请求必须声明 `executionProfile=omni_console`，保留 capability 权限语义并统一进入已接通 Omni 工具、结构化回复和质量门禁的 DeepTutor AgentLoop；不得把该 profile 冒充原生高级 Capability 的完整执行管线。
- 同一 turn 的学生引用解析状态必须跨 HostTool 调用保留；已经成功解析学生后，后续工具不得因参数中没有重复携带 studentId 而退回 `ask_user`。
- `waiting_input` 是可恢复中间状态：renderer 提交答案前 Console IPC 继续等待，提交后必须收敛到 succeeded/failed/blocked/cancelled，不能提前返回空回复。
- Sidecar 仓库根目录必须通过有界探测确认 Python 模块存在；异常 stderr 只能保留脱敏、bounded 尾部用于可行动错误，不得发给模型或保存密钥。
- 旧 SQLite schema 的列迁移必须先于依赖新列的索引创建；主窗口启动是 AI live 验收的前置门禁。
- 详细架构、映射和验收命令以 `docs/27_XIAOZHI_AGENT_HARNESS_ENGINEERING.md` 为准。
